{"entity":"initiative","version":1,"id":"browser-softphone","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Browser softphone 0.1\n\n## Outcome\n\nDeliver a standalone browser softphone that registers a SIP account and carries two-way audio, built\non the `codewandler/sipx` browser SDK, in the shape `todo` established for a reference service:\none ESS domain, one deployment unit, its own planning store.\n\n## Constraints\n\nsipx is a user agent, not a media engine. Its README states it is \"not a proxy, registrar, PBX,\nbrowser media engine, or video stack\", and its M15 milestone assigns `RTCPeerConnection`, ICE,\nDTLS-SRTP, capture and render to the browser. This project therefore owns the media plane in\nJavaScript and takes signalling and dialog state from the sipx WebAssembly kernel.\n\nAudio only. Video and data channels are out of scope, as they are upstream.\n\n## Cited from\n\n- sipx `v1.0.0-rc.23`, rev `004ac534b8b222060ad2d2308763efe6e1dedc10`, pinned at\n  `connectors/crates/driver-sip/Cargo.toml:21-25`, checked out at\n  `~/.cargo/git/checkouts/sipx-65343d777b6000c5/004ac53`.\n- `README.md` \"Does it fit?\" — the user-agent boundary and the `browser-audio` composition profile.\n- `docs/roadmap.md` M15 \"Browser-embeddable audio\" — the six stories and their order.\n- `todo/service.yaml` and `todo/.engineering/planning/` — the reference-service shape being copied.\n","summary":"A standalone browser softphone on the codewandler/sipx browser SDK, in the shape todo established.","title":"Browser softphone 0.1"},"args":{"command":"create-entity","data":{"body":"# Browser softphone 0.1\n\n## Outcome\n\nDeliver a standalone browser softphone that registers a SIP account and carries two-way audio, built\non the `codewandler/sipx` browser SDK, in the shape `todo` established for a reference service:\none ESS domain, one deployment unit, its own planning store.\n\n## Constraints\n\nsipx is a user agent, not a media engine. Its README states it is \"not a proxy, registrar, PBX,\nbrowser media engine, or video stack\", and its M15 milestone assigns `RTCPeerConnection`, ICE,\nDTLS-SRTP, capture and render to the browser. This project therefore owns the media plane in\nJavaScript and takes signalling and dialog state from the sipx WebAssembly kernel.\n\nAudio only. Video and data channels are out of scope, as they are upstream.\n\n## Cited from\n\n- sipx `v1.0.0-rc.23`, rev `004ac534b8b222060ad2d2308763efe6e1dedc10`, pinned at\n  `connectors/crates/driver-sip/Cargo.toml:21-25`, checked out at\n  `~/.cargo/git/checkouts/sipx-65343d777b6000c5/004ac53`.\n- `README.md` \"Does it fit?\" — the user-agent boundary and the `browser-audio` composition profile.\n- `docs/roadmap.md` M15 \"Browser-embeddable audio\" — the six stories and their order.\n- `todo/service.yaml` and `todo/.engineering/planning/` — the reference-service shape being copied.\n","status":"draft","summary":"A standalone browser softphone on the codewandler/sipx browser SDK, in the shape todo established.","title":"Browser softphone 0.1"},"entity_type":"aep.initiative/v1","locator":"ep://planning/store/initiative/browser-softphone"},"payload":{"actor":"human:timo","at":1788494297185,"causation":"cmd-new-initiative-browser-softphone","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"initiative:browser-softphone@1#0~81bc1e03666736ab","executor":null,"recorded_at":"2026-09-04T03:58:17Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, and show call state in\na browser page. Signalling and dialog state come from the sipx WebAssembly session kernel\n(`crates/sipx-wasm`, story S-41) driven over the WebSocket binding (`browser/src/`, story T-33), both\npresent at the pinned revision. Media is negotiated by the browser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project:\n`ess/system.yaml`, `ess/components.yaml`, `ess/domains/session.yaml`. Two entities —\n`softphone.session.Registration` and `softphone.session.Call` — ten commands, ten events, three\nviews. `ess validate --path ess` reports `softphone v1 — 3 file(s), valid`.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it. `decision-blocker:media-adapter-owner` holds the\nquestion of whether that stays here or goes upstream.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n","summary":"Register over WSS and carry audio calls in a browser page, with media owned by RTCPeerConnection.","title":"Browser softphone"},"args":{"command":"create-entity","data":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, and show call state in\na browser page. Signalling and dialog state come from the sipx WebAssembly session kernel\n(`crates/sipx-wasm`, story S-41) driven over the WebSocket binding (`browser/src/`, story T-33), both\npresent at the pinned revision. Media is negotiated by the browser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project:\n`ess/system.yaml`, `ess/components.yaml`, `ess/domains/session.yaml`. Two entities —\n`softphone.session.Registration` and `softphone.session.Call` — ten commands, ten events, three\nviews. `ess validate --path ess` reports `softphone v1 — 3 file(s), valid`.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it. `decision-blocker:media-adapter-owner` holds the\nquestion of whether that stays here or goes upstream.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n","status":"draft","summary":"Register over WSS and carry audio calls in a browser page, with media owned by RTCPeerConnection.","title":"Browser softphone"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/browser-softphone"},"payload":{"actor":"human:timo","at":1788494297359,"causation":"cmd-new-epic-browser-softphone","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:browser-softphone@1#0~1ea2166dc34c2ae1","executor":null,"recorded_at":"2026-09-04T03:58:17Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"initiative:browser-softphone"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000004","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788494297367,"causation":"cmd-epic-browser-softphone-derived_from-initiative-browser-softphone","change":{"change":"related","relation":"derived_from","target":"initiative:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"epic:browser-softphone@1#0~88743c0732f7ff5a","executor":null,"recorded_at":"2026-09-04T03:58:17Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.session.Register` through to `Registered` or `Failed`.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\n- `browser/src/transport.mjs`, `platform.mjs`, `records.mjs`, `index.mjs` vendored from the pinned\n  sipx revision — no edits, so the upstream `scripts/check-browser-binding.sh` cases still describe it.\n- The WASM module built from `crates/sipx-wasm` and `wasm/`.\n- The ABI glue that `browser/README.md` says `test/kernel.test.mjs` writes by hand today, because\n  `A-17` has not generated it.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n","summary":"Load the WASM kernel, open WSS, and drive REGISTER to a terminal state.","title":"Register over secure WebSocket"},"args":{"command":"create-entity","data":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.session.Register` through to `Registered` or `Failed`.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\n- `browser/src/transport.mjs`, `platform.mjs`, `records.mjs`, `index.mjs` vendored from the pinned\n  sipx revision — no edits, so the upstream `scripts/check-browser-binding.sh` cases still describe it.\n- The WASM module built from `crates/sipx-wasm` and `wasm/`.\n- The ABI glue that `browser/README.md` says `test/kernel.test.mjs` writes by hand today, because\n  `A-17` has not generated it.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n","status":"draft","summary":"Load the WASM kernel, open WSS, and drive REGISTER to a terminal state.","title":"Register over secure WebSocket"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/signalling-over-wss"},"payload":{"actor":"human:timo","at":1788494331341,"causation":"cmd-new-story-signalling-over-wss","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:signalling-over-wss@1#0~46b02044651f9e24","executor":null,"recorded_at":"2026-09-04T03:58:51Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000009","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788494331366,"causation":"cmd-story-signalling-over-wss-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:signalling-over-wss@1#0~5bc0b3c34d6edd43","executor":null,"recorded_at":"2026-09-04T03:58:51Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package.\n\n`decision-blocker:media-adapter-owner` holds whether this lands here permanently or is contributed\nupstream; the work is the same either way, the licence and the test home are not.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n","summary":"Audio-only RTCPeerConnection adapter; media failures never present as SIP failures.","title":"Adapt browser-native audio"},"args":{"command":"create-entity","data":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package.\n\n`decision-blocker:media-adapter-owner` holds whether this lands here permanently or is contributed\nupstream; the work is the same either way, the licence and the test home are not.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n","status":"draft","summary":"Audio-only RTCPeerConnection adapter; media failures never present as SIP failures.","title":"Adapt browser-native audio"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/browser-media-adapter"},"payload":{"actor":"human:timo","at":1788494331756,"causation":"cmd-new-story-browser-media-adapter","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:browser-media-adapter@1#0~f03bb34bfda22708","executor":null,"recorded_at":"2026-09-04T03:58:51Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000014","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788494331767,"causation":"cmd-story-browser-media-adapter-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:browser-media-adapter@1#0~ab86b8454367a56d","executor":null,"recorded_at":"2026-09-04T03:58:51Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Place, answer and end calls\n\n## Outcome\n\nThe ten commands in `softphone.session` are wired to kernel events in both directions: user intents\nbecome commands, kernel events become the signalling-driven commands, and every call reaches a\nterminal state.\n\n## Acceptance\n\nAn outbound call moves `Proceeding -> Ringing -> Established -> Ended` and each move appends its\nevent. A command issued from a state its transition does not allow returns\n`softphone.session.CallStateConflict` and appends nothing.\n\n## Scope\n\n- `ess/domains/session.yaml` — the entity lifecycles this story realises.\n- The `softphone.session.SignallingKernel` actor's five commands are reachable only from kernel\n  output records, never from the page.\n\n## Cited from\n\n- `ess/domains/session.yaml` in this project, validated by `ess validate --path ess`.\n- `docs/specs/browser-sdk.md` §5.4 \"Kernel call states\" — the states this domain mirrors.\n","summary":"Wire the ten softphone.session commands to kernel events in both directions.","title":"Place, answer and end calls"},"args":{"command":"create-entity","data":{"body":"# Place, answer and end calls\n\n## Outcome\n\nThe ten commands in `softphone.session` are wired to kernel events in both directions: user intents\nbecome commands, kernel events become the signalling-driven commands, and every call reaches a\nterminal state.\n\n## Acceptance\n\nAn outbound call moves `Proceeding -> Ringing -> Established -> Ended` and each move appends its\nevent. A command issued from a state its transition does not allow returns\n`softphone.session.CallStateConflict` and appends nothing.\n\n## Scope\n\n- `ess/domains/session.yaml` — the entity lifecycles this story realises.\n- The `softphone.session.SignallingKernel` actor's five commands are reachable only from kernel\n  output records, never from the page.\n\n## Cited from\n\n- `ess/domains/session.yaml` in this project, validated by `ess validate --path ess`.\n- `docs/specs/browser-sdk.md` §5.4 \"Kernel call states\" — the states this domain mirrors.\n","status":"draft","summary":"Wire the ten softphone.session commands to kernel events in both directions.","title":"Place, answer and end calls"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/call-lifecycle"},"payload":{"actor":"human:timo","at":1788494332045,"causation":"cmd-new-story-call-lifecycle","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:call-lifecycle@1#0~c279d6b1f8751056","executor":null,"recorded_at":"2026-09-04T03:58:52Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000019","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788494332062,"causation":"cmd-story-call-lifecycle-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:call-lifecycle@1#0~59227cba53e60b50","executor":null,"recorded_at":"2026-09-04T03:58:52Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# The softphone page\n\n## Outcome\n\nA page with a registration indicator, a dial field, an incoming-call prompt, and in-call controls for\nhang up and mute.\n\n## Acceptance\n\nWith a registered account, a user can dial a SIP URI and reach an established call, answer an\ninbound call, and end either; the three `softphone.session` views are the only source of what the\npage displays.\n\n## Scope\n\n- Reads `RegistrationById`, `CallById` and `ActiveCalls`; holds no call state of its own.\n- No build step, matching the constraint `browser/README.md` records for the binding it sits on.\n\n## Cited from\n\n- `ess/domains/session.yaml` — the three views.\n- `docs/roadmap.md` M15 story `A-18` — the upstream demo this page is the local equivalent of.\n","summary":"Registration indicator, dial field, incoming prompt, in-call controls.","title":"The softphone page"},"args":{"command":"create-entity","data":{"body":"# The softphone page\n\n## Outcome\n\nA page with a registration indicator, a dial field, an incoming-call prompt, and in-call controls for\nhang up and mute.\n\n## Acceptance\n\nWith a registered account, a user can dial a SIP URI and reach an established call, answer an\ninbound call, and end either; the three `softphone.session` views are the only source of what the\npage displays.\n\n## Scope\n\n- Reads `RegistrationById`, `CallById` and `ActiveCalls`; holds no call state of its own.\n- No build step, matching the constraint `browser/README.md` records for the binding it sits on.\n\n## Cited from\n\n- `ess/domains/session.yaml` — the three views.\n- `docs/roadmap.md` M15 story `A-18` — the upstream demo this page is the local equivalent of.\n","status":"draft","summary":"Registration indicator, dial field, incoming prompt, in-call controls.","title":"The softphone page"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/softphone-page"},"payload":{"actor":"human:timo","at":1788494332318,"causation":"cmd-new-story-softphone-page","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:softphone-page@1#0~236b5a34c9e83f55","executor":null,"recorded_at":"2026-09-04T03:58:52Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000024","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788494332327,"causation":"cmd-story-softphone-page-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:softphone-page@1#0~bc6f073a23977304","executor":null,"recorded_at":"2026-09-04T03:58:52Z"}}
{"entity":"decision-blocker","version":1,"id":"media-adapter-owner","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# Who owns the browser media adapter\n\n## The question\n\n`story:browser-media-adapter` implements what upstream calls `M-52`. It can live in this project, or\nbe contributed to `codewandler/sipx` and consumed from there.\n\n## Why it is not mine to decide\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so upstream\nhas no date. Writing it here is faster and is reversible for this prototype; writing it upstream is\nslower, needs the sipx contribution gates in `AGENTS.md`, and is the only version that ends with an\ninstallable `@sipx/browser` for every other consumer.\n\nNothing in the repository settles which, and the answer changes where the tests live and which\nlicence applies.\n\n## What clears it\n\nA decision from the operator. On silence the work is written here, in this project, against the\nnormative spec — that is the reversible option and the story is written for it.\n","title":"Nobody has decided whether the browser media adapter lands here or upstream in sipx"},"args":{"command":"create-entity","data":{"body":"# Who owns the browser media adapter\n\n## The question\n\n`story:browser-media-adapter` implements what upstream calls `M-52`. It can live in this project, or\nbe contributed to `codewandler/sipx` and consumed from there.\n\n## Why it is not mine to decide\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so upstream\nhas no date. Writing it here is faster and is reversible for this prototype; writing it upstream is\nslower, needs the sipx contribution gates in `AGENTS.md`, and is the only version that ends with an\ninstallable `@sipx/browser` for every other consumer.\n\nNothing in the repository settles which, and the answer changes where the tests live and which\nlicence applies.\n\n## What clears it\n\nA decision from the operator. On silence the work is written here, in this project, against the\nnormative spec — that is the reversible option and the story is written for it.\n","status":"open","title":"Nobody has decided whether the browser media adapter lands here or upstream in sipx"},"entity_type":"aep.decision-blocker/v1","locator":"ep://planning/store/decision-blocker/media-adapter-owner"},"payload":{"actor":"human:timo","at":1788494332548,"causation":"cmd-new-decision-blocker-media-adapter-owner","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"decision-blocker:media-adapter-owner@1#0~f8666a633ce1fde7","executor":null,"recorded_at":"2026-09-04T03:58:52Z"}}
{"entity":"decision-blocker","version":1,"id":"media-adapter-owner","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:browser-media-adapter"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000029","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788494332560,"causation":"cmd-decision-blocker-media-adapter-owner-blocks-story-browser-media-adapter","change":{"change":"related","relation":"blocks","target":"story:browser-media-adapter"},"correlation":"protocol-artifact-relate","event_id":"decision-blocker:media-adapter-owner@1#0~4a0c669ae38c7fcb","executor":null,"recorded_at":"2026-09-04T03:58:52Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:signalling-over-wss"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000013","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788494340889,"causation":"cmd-story-call-lifecycle-depends_on-story-signalling-over-wss","change":{"change":"related","relation":"depends_on","target":"story:signalling-over-wss"},"correlation":"protocol-artifact-relate","event_id":"story:call-lifecycle@1#0~d61e609f24a7b6a1","executor":null,"recorded_at":"2026-09-04T03:59:00Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:signalling-over-wss"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000010","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788494340978,"causation":"cmd-story-browser-media-adapter-depends_on-story-signalling-over-wss","change":{"change":"related","relation":"depends_on","target":"story:signalling-over-wss"},"correlation":"protocol-artifact-relate","event_id":"story:browser-media-adapter@1#0~cc7f08af4050cc06","executor":null,"recorded_at":"2026-09-04T03:59:00Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:call-lifecycle"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000019","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788494341060,"causation":"cmd-story-softphone-page-depends_on-story-call-lifecycle","change":{"change":"related","relation":"depends_on","target":"story:call-lifecycle"},"correlation":"protocol-artifact-relate","event_id":"story:softphone-page@1#0~3913d4628df2c0a4","executor":null,"recorded_at":"2026-09-04T03:59:01Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:call-lifecycle"},{"depends_on":"story:browser-media-adapter"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000019","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788494341144,"causation":"cmd-story-softphone-page-depends_on-story-browser-media-adapter","change":{"change":"related","relation":"depends_on","target":"story:browser-media-adapter"},"correlation":"protocol-artifact-relate","event_id":"story:softphone-page@1#0~1ff05d627ff2ab31","executor":null,"recorded_at":"2026-09-04T03:59:01Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-adapter-lands-here","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"proposed","changed":{"body":"# The browser media adapter lands in this project\n\n## Status\n\nAccepted 2026-09-04. Decided by the operator, in session, in answer to\n`decision-blocker:media-adapter-owner`.\n\n## Context\n\n`story:browser-media-adapter` implements what `codewandler/sipx` calls `M-52` — the audio-only\n`RTCPeerConnection` adapter. At the pinned revision `004ac534b8b222060ad2d2308763efe6e1dedc10`\n(`v1.0.0-rc.23`) that story has not shipped, and `docs/roadmap.md` states its milestone M15 \"is\ntracked but is **not** part of the selected M13 wave\", so upstream has no date for it.\n\nTwo homes were available: this project, or a contribution to `codewandler/sipx` consumed back as\n`@sipx/browser` (upstream story `A-17`, also unshipped).\n\n## Decision\n\nThe adapter lands here.\n\n## Consequences\n\n- The adapter's code and its tests live in this repository, under this repository's licence.\n- It is written against `docs/specs/browser-sdk.md` §5.4, §6.2, §8.2 and §8.5, which are normative,\n  rather than against a package API that does not exist yet.\n- No upstream contribution gate, review or release is on this project's path.\n- `@sipx/browser` remains unavailable to other consumers; this decision does not change that, and it\n  is the cost of the faster path.\n- If `M-52` later ships upstream, this adapter becomes the thing to retire, not the thing to keep.\n  Whoever does that reads this record first.\n\n## Cited from\n\n- `docs/roadmap.md` M15 — the story order and the unscheduled state.\n- `docs/specs/browser-sdk.md:854` — `M-52` owns the media adapter surfaces.\n- `docs/specs/browser-sdk.md:855` — `A-17` owns the package and lifecycle layer.\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n","summary":"Write the audio-only RTCPeerConnection adapter here against the sipx spec, not upstream.","title":"The browser media adapter lands in this project"},"args":{"command":"create-entity","data":{"body":"# The browser media adapter lands in this project\n\n## Status\n\nAccepted 2026-09-04. Decided by the operator, in session, in answer to\n`decision-blocker:media-adapter-owner`.\n\n## Context\n\n`story:browser-media-adapter` implements what `codewandler/sipx` calls `M-52` — the audio-only\n`RTCPeerConnection` adapter. At the pinned revision `004ac534b8b222060ad2d2308763efe6e1dedc10`\n(`v1.0.0-rc.23`) that story has not shipped, and `docs/roadmap.md` states its milestone M15 \"is\ntracked but is **not** part of the selected M13 wave\", so upstream has no date for it.\n\nTwo homes were available: this project, or a contribution to `codewandler/sipx` consumed back as\n`@sipx/browser` (upstream story `A-17`, also unshipped).\n\n## Decision\n\nThe adapter lands here.\n\n## Consequences\n\n- The adapter's code and its tests live in this repository, under this repository's licence.\n- It is written against `docs/specs/browser-sdk.md` §5.4, §6.2, §8.2 and §8.5, which are normative,\n  rather than against a package API that does not exist yet.\n- No upstream contribution gate, review or release is on this project's path.\n- `@sipx/browser` remains unavailable to other consumers; this decision does not change that, and it\n  is the cost of the faster path.\n- If `M-52` later ships upstream, this adapter becomes the thing to retire, not the thing to keep.\n  Whoever does that reads this record first.\n\n## Cited from\n\n- `docs/roadmap.md` M15 — the story order and the unscheduled state.\n- `docs/specs/browser-sdk.md:854` — `M-52` owns the media adapter surfaces.\n- `docs/specs/browser-sdk.md:855` — `A-17` owns the package and lifecycle layer.\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n","status":"proposed","summary":"Write the audio-only RTCPeerConnection adapter here against the sipx spec, not upstream.","title":"The browser media adapter lands in this project"},"entity_type":"aep.architecture-decision-record/v1","locator":"ep://planning/store/architecture-decision-record/media-adapter-lands-here"},"payload":{"actor":"human:timo","at":1788502800613,"causation":"cmd-new-architecture-decision-record-media-adapter-lands-here","change":{"change":"created","status":"proposed"},"correlation":"protocol-artifact-new","event_id":"architecture-decision-record:media-adapter-lands-here@1#0~68acb6128a7ab232","executor":null,"recorded_at":"2026-09-04T06:20:00Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-adapter-lands-here","revision":1,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decides":"story:browser-media-adapter"}]},"args":{"command":"create-relation","kind":"decides","source":"01MEM0000000000000042","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788502800621,"causation":"cmd-architecture-decision-record-media-adapter-lands-here-decides-story-browser-media-adapter","change":{"change":"related","relation":"decides","target":"story:browser-media-adapter"},"correlation":"protocol-artifact-relate","event_id":"architecture-decision-record:media-adapter-lands-here@1#0~5162a07841364765","executor":null,"recorded_at":"2026-09-04T06:20:00Z"}}
{"entity":"decision-blocker","version":1,"id":"media-adapter-owner","revision":2,"type":"aep.entity.update/v1","from_state":"open","to_state":"open","changed":{"body":"# Who owns the browser media adapter\n\n## The question\n\n`story:browser-media-adapter` implements what upstream calls `M-52`. It can live in this project, or\nbe contributed to `codewandler/sipx` and consumed from there.\n\n## Why it is not mine to decide\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so upstream\nhas no date. Writing it here is faster and is reversible for this prototype; writing it upstream is\nslower, needs the sipx contribution gates in `AGENTS.md`, and is the only version that ends with an\ninstallable `@sipx/browser` for every other consumer.\n\nNothing in the repository settles which, and the answer changes where the tests live and which\nlicence applies.\n\n## What clears it\n\nAnswered by the operator on 2026-09-04: the adapter lands in this project.\n`architecture-decision-record:media-adapter-lands-here` holds the decision and its consequences.\n"},"args":{"changes":{"body":"# Who owns the browser media adapter\n\n## The question\n\n`story:browser-media-adapter` implements what upstream calls `M-52`. It can live in this project, or\nbe contributed to `codewandler/sipx` and consumed from there.\n\n## Why it is not mine to decide\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so upstream\nhas no date. Writing it here is faster and is reversible for this prototype; writing it upstream is\nslower, needs the sipx contribution gates in `AGENTS.md`, and is the only version that ends with an\ninstallable `@sipx/browser` for every other consumer.\n\nNothing in the repository settles which, and the answer changes where the tests live and which\nlicence applies.\n\n## What clears it\n\nAnswered by the operator on 2026-09-04: the adapter lands in this project.\n`architecture-decision-record:media-adapter-lands-here` holds the decision and its consequences.\n"},"command":"update-entity","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788502800704,"causation":"cmd-protocol-artifact-body-section-decision-blocker-media-adapter-owner-1788502800704","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"decision-blocker:media-adapter-owner@2#0~fe4f5ab284c330f3","executor":null,"recorded_at":"2026-09-04T06:20:00Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package.\n\nWhere it lands is settled: `architecture-decision-record:media-adapter-lands-here` puts it in this\nproject, under this repository's licence and with its tests here.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n"},"args":{"changes":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package.\n\nWhere it lands is settled: `architecture-decision-record:media-adapter-lands-here` puts it in this\nproject, under this repository's licence and with its tests here.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788502800789,"causation":"cmd-protocol-artifact-body-section-story-browser-media-adapter-1788502800789","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:browser-media-adapter@2#0~7e6f97233ad53e11","executor":null,"recorded_at":"2026-09-04T06:20:00Z"}}
{"entity":"decision-blocker","version":1,"id":"media-adapter-owner","revision":3,"type":"aep.status.move/v1","from_state":"open","to_state":"cleared","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"cleared"},"payload":{"actor":"human:timo","at":1788502800942,"causation":"cmd-move-decision-blocker-media-adapter-owner-1788502800942","change":{"change":"moved","decided_on":{},"from":"open","to":"cleared"},"correlation":"protocol-artifact-move","event_id":"decision-blocker:media-adapter-owner@3#0~1bc928e0351857a8","executor":null,"recorded_at":"2026-09-04T06:20:00Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, and show call state in\na browser page. Signalling and dialog state come from the sipx WebAssembly session kernel\n(`crates/sipx-wasm`, story S-41) driven over the WebSocket binding (`browser/src/`, story T-33), both\npresent at the pinned revision. Media is negotiated by the browser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project:\n`ess/system.yaml`, `ess/components.yaml`, `ess/domains/session.yaml`. Two entities —\n`softphone.session.Registration` and `softphone.session.Call` — ten commands, ten events, three\nviews. `ess validate --path ess` reports `softphone v1 — 3 file(s), valid`.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n"},"args":{"changes":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, and show call state in\na browser page. Signalling and dialog state come from the sipx WebAssembly session kernel\n(`crates/sipx-wasm`, story S-41) driven over the WebSocket binding (`browser/src/`, story T-33), both\npresent at the pinned revision. Media is negotiated by the browser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project:\n`ess/system.yaml`, `ess/components.yaml`, `ess/domains/session.yaml`. Two entities —\n`softphone.session.Registration` and `softphone.session.Call` — ten commands, ten events, three\nviews. `ess validate --path ess` reports `softphone v1 — 3 file(s), valid`.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n"},"command":"update-entity","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788502811526,"causation":"cmd-protocol-artifact-body-section-epic-browser-softphone-1788502811526","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:browser-softphone@2#0~247e12c2cf06ad94","executor":null,"recorded_at":"2026-09-04T06:20:11Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-adapter-lands-here","revision":2,"type":"aep.status.move/v1","from_state":"proposed","to_state":"accepted","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"accepted"},"payload":{"actor":"human:timo","at":1788502814997,"causation":"cmd-move-architecture-decision-record-media-adapter-lands-here-1788502814997","change":{"change":"moved","decided_on":{},"from":"proposed","to":"accepted"},"correlation":"protocol-artifact-move","event_id":"architecture-decision-record:media-adapter-lands-here@2#0~c2833842ca4a242c","executor":null,"recorded_at":"2026-09-04T06:20:14Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, and show call state in\na browser page. Signalling and dialog state come from the sipx WebAssembly session kernel\n(`crates/sipx-wasm`, story S-41) driven over the WebSocket binding (`browser/src/`, story T-33), both\npresent at the pinned revision. Media is negotiated by the browser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project:\n`ess/system.yaml`, `ess/components.yaml`, `ess/domains/session.yaml`. Two entities —\n`softphone.session.Registration` and `softphone.session.Call` — ten commands, ten events, three\nviews. `ess validate --path ess` reports `softphone v1 — 3 file(s), valid`.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n"},"args":{"changes":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, and show call state in\na browser page. Signalling and dialog state come from the sipx WebAssembly session kernel\n(`crates/sipx-wasm`, story S-41) driven over the WebSocket binding (`browser/src/`, story T-33), both\npresent at the pinned revision. Media is negotiated by the browser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project:\n`ess/system.yaml`, `ess/components.yaml`, `ess/domains/session.yaml`. Two entities —\n`softphone.session.Registration` and `softphone.session.Call` — ten commands, ten events, three\nviews. `ess validate --path ess` reports `softphone v1 — 3 file(s), valid`.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n"},"command":"update-entity","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788502973971,"causation":"cmd-protocol-artifact-body-section-epic-browser-softphone-1788502973971","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:browser-softphone@3#0~4800aa0f5cda49f5","executor":null,"recorded_at":"2026-09-04T06:22:53Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.session.Register` through to `Registered` or `Failed`.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\n- `browser/src/transport.mjs`, `platform.mjs`, `records.mjs`, `index.mjs` vendored from the pinned\n  sipx revision — no edits, so the upstream `scripts/check-browser-binding.sh` cases still describe it.\n- The WASM module built from `crates/sipx-wasm` and `wasm/`.\n- The ABI glue that `browser/README.md` says `test/kernel.test.mjs` writes by hand today, because\n  `A-17` has not generated it.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"args":{"changes":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.session.Register` through to `Registered` or `Failed`.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\n- `browser/src/transport.mjs`, `platform.mjs`, `records.mjs`, `index.mjs` vendored from the pinned\n  sipx revision — no edits, so the upstream `scripts/check-browser-binding.sh` cases still describe it.\n- The WASM module built from `crates/sipx-wasm` and `wasm/`.\n- The ABI glue that `browser/README.md` says `test/kernel.test.mjs` writes by hand today, because\n  `A-17` has not generated it.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788502974062,"causation":"cmd-protocol-artifact-body-section-story-signalling-over-wss-1788502974062","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:signalling-over-wss@2#0~6badbc96553fd563","executor":null,"recorded_at":"2026-09-04T06:22:54Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package.\n\nWhere it lands is settled: `architecture-decision-record:media-adapter-lands-here` puts it in this\nproject, under this repository's licence and with its tests here.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"args":{"changes":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package.\n\nWhere it lands is settled: `architecture-decision-record:media-adapter-lands-here` puts it in this\nproject, under this repository's licence and with its tests here.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788502974157,"causation":"cmd-protocol-artifact-body-section-story-browser-media-adapter-1788502974157","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:browser-media-adapter@3#0~9ef2d0880a7e4045","executor":null,"recorded_at":"2026-09-04T06:22:54Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# The softphone page\n\n## Outcome\n\nA page with a registration indicator, a dial field, an incoming-call prompt, and in-call controls for\nhang up and mute.\n\n## Acceptance\n\nWith a registered account, a user can dial a SIP URI and reach an established call, answer an\ninbound call, and end either; the three `softphone.session` views are the only source of what the\npage displays.\n\n## Scope\n\n- Reads `RegistrationById`, `CallById` and `ActiveCalls`; holds no call state of its own.\n- No build step, matching the constraint `browser/README.md` records for the binding it sits on.\n\n## Cited from\n\n- `ess/domains/session.yaml` — the three views.\n- `docs/roadmap.md` M15 story `A-18` — the upstream demo this page is the local equivalent of.\n\n## Browser policy\n\nRegistration may start on load; **media must not.** Placing or answering a call runs from an explicit\nuser gesture in a secure context, because microphone permission and audio autoplay require both\n(`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §3, §7).\n"},"args":{"changes":{"body":"# The softphone page\n\n## Outcome\n\nA page with a registration indicator, a dial field, an incoming-call prompt, and in-call controls for\nhang up and mute.\n\n## Acceptance\n\nWith a registered account, a user can dial a SIP URI and reach an established call, answer an\ninbound call, and end either; the three `softphone.session` views are the only source of what the\npage displays.\n\n## Scope\n\n- Reads `RegistrationById`, `CallById` and `ActiveCalls`; holds no call state of its own.\n- No build step, matching the constraint `browser/README.md` records for the binding it sits on.\n\n## Cited from\n\n- `ess/domains/session.yaml` — the three views.\n- `docs/roadmap.md` M15 story `A-18` — the upstream demo this page is the local equivalent of.\n\n## Browser policy\n\nRegistration may start on load; **media must not.** Placing or answering a call runs from an explicit\nuser gesture in a secure context, because microphone permission and audio autoplay require both\n(`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §3, §7).\n"},"command":"update-entity","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788502974242,"causation":"cmd-protocol-artifact-body-section-story-softphone-page-1788502974242","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:softphone-page@2#0~fc8a6e63ce6a11cf","executor":null,"recorded_at":"2026-09-04T06:22:54Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Model the neutral media session\n\n## Outcome\n\n`softphone.media` states what a media session is without naming a protocol, and `softphone.local`\nexists as a second carrier so that claim has evidence behind it.\n\n## Acceptance\n\n`ess validate --path ess` accepts the domain. No type, entity, command or event in\n`softphone.media` names SIP, RTP, RTVBP, WebRTC, SDP or a WebSocket — grep it and the result is\nempty. Two binding domains own a `MediaSession`, not one.\n\n## What was built\n\n`ess/domains/media.yaml`, modelled construct-for-type from the Rust port that already existed:\n`connectors/crates/domain/src/voice.rs`.\n\n| ESS construct | Read from |\n|---|---|\n| `MediaProfile` | `MediaDescriptor` (`:68`); the single accepted value `pcm_s16le`/8000/1/20 ms/320 B (`:78-95`), so `SampleFormat` has one member |\n| `ChannelSignal` + `ChannelSignalKind` | `ChannelSignal::Dtmf { digits }` (`:114-135`); split in two because an ESS enum carries no fields |\n| `TerminationReason` | `:137-147`, all eight variants unchanged |\n| `Participant`, `ContextTrust` | `ParticipantContext` (`:57`), `ContextTrust::Untrusted` (`:49`) |\n| `SessionRef` | `VoiceRef` (`:33`); opacity modelled, the 128-byte cap left to the host |\n| the six commands | one per `TelephonySession` method that is not the frame path (`:163-229`) |\n\n`softphone.local` is `ess/domains/local.yaml`, cited from `connectors/crates/voice-local-audio` —\nthe sibling of `driver-sip` behind the same port.\n\n## One deliberate difference from the port it was read from\n\nUpstream, establishment happens *before* a neutral session exists: `RuntimeError` is documented as\n\"Failure before a neutral telephony session exists\"\n(`connectors/crates/voice-runtime/src/lib.rs:307-309`). Here it is the `Requested -> Live` move, so\nthe ESS session starts one state earlier than the Rust port's. Written into the domain `summary` so\nthe next reader does not find a contradiction and assume one side is wrong.\n\n## The limit that could not be modelled\n\nA `MediaSession` owns a `SipDialog` and a `LoopbackDevice`, one each, and at runtime exactly one\nexists. ESS cannot state that: `crates/specify/ess-domain/src/entity.rs:821-822` says an invariant\nreads only its own entity's fields, identity and `state`, so a predicate over a related entity's\nexistence is refused. `binding: BindingKind` is the discriminator and the two `creates` outcomes —\n`softphone.sip.OpenDialog` and `softphone.local.AttachLoopback` — are what hold it. No invariant\npretends otherwise.\n","summary":"softphone.media from the existing Rust port, plus softphone.local as the second carrier.","title":"Model the neutral media session"},"args":{"command":"create-entity","data":{"body":"# Model the neutral media session\n\n## Outcome\n\n`softphone.media` states what a media session is without naming a protocol, and `softphone.local`\nexists as a second carrier so that claim has evidence behind it.\n\n## Acceptance\n\n`ess validate --path ess` accepts the domain. No type, entity, command or event in\n`softphone.media` names SIP, RTP, RTVBP, WebRTC, SDP or a WebSocket — grep it and the result is\nempty. Two binding domains own a `MediaSession`, not one.\n\n## What was built\n\n`ess/domains/media.yaml`, modelled construct-for-type from the Rust port that already existed:\n`connectors/crates/domain/src/voice.rs`.\n\n| ESS construct | Read from |\n|---|---|\n| `MediaProfile` | `MediaDescriptor` (`:68`); the single accepted value `pcm_s16le`/8000/1/20 ms/320 B (`:78-95`), so `SampleFormat` has one member |\n| `ChannelSignal` + `ChannelSignalKind` | `ChannelSignal::Dtmf { digits }` (`:114-135`); split in two because an ESS enum carries no fields |\n| `TerminationReason` | `:137-147`, all eight variants unchanged |\n| `Participant`, `ContextTrust` | `ParticipantContext` (`:57`), `ContextTrust::Untrusted` (`:49`) |\n| `SessionRef` | `VoiceRef` (`:33`); opacity modelled, the 128-byte cap left to the host |\n| the six commands | one per `TelephonySession` method that is not the frame path (`:163-229`) |\n\n`softphone.local` is `ess/domains/local.yaml`, cited from `connectors/crates/voice-local-audio` —\nthe sibling of `driver-sip` behind the same port.\n\n## One deliberate difference from the port it was read from\n\nUpstream, establishment happens *before* a neutral session exists: `RuntimeError` is documented as\n\"Failure before a neutral telephony session exists\"\n(`connectors/crates/voice-runtime/src/lib.rs:307-309`). Here it is the `Requested -> Live` move, so\nthe ESS session starts one state earlier than the Rust port's. Written into the domain `summary` so\nthe next reader does not find a contradiction and assume one side is wrong.\n\n## The limit that could not be modelled\n\nA `MediaSession` owns a `SipDialog` and a `LoopbackDevice`, one each, and at runtime exactly one\nexists. ESS cannot state that: `crates/specify/ess-domain/src/entity.rs:821-822` says an invariant\nreads only its own entity's fields, identity and `state`, so a predicate over a related entity's\nexistence is refused. `binding: BindingKind` is the discriminator and the two `creates` outcomes —\n`softphone.sip.OpenDialog` and `softphone.local.AttachLoopback` — are what hold it. No invariant\npretends otherwise.\n","status":"draft","summary":"softphone.media from the existing Rust port, plus softphone.local as the second carrier.","title":"Model the neutral media session"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/media-session-domain"},"payload":{"actor":"human:timo","at":1788513405126,"causation":"cmd-new-story-media-session-domain","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:media-session-domain@1#0~c4ab24529dfd88d2","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000047","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788513405155,"causation":"cmd-story-media-session-domain-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:media-session-domain@1#0~4e39a9e164c7e817","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"call-history","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Record the calls that finished\n\n## Outcome\n\n`softphone.history` holds one durable record per finished call, in the neutral termination\nvocabulary, optionally attributed to a contact.\n\n## Acceptance\n\n`ess validate` accepts the domain. `CallRecord.termination` is\n`softphone.media.TerminationReason` and not a SIP cause. A record survives its call: the relation to\n`softphone.control.Call` is `references`, so nothing in the model lets a call take its record with\nit. There is no bulk-erase command.\n\n## What was built\n\n`ess/domains/history.yaml`. `CallRecord` — `record_id`, an optional `call_id`, an optional\n`contact_id`, `direction`, `remote`, `started_at`, `ended_at`, `termination`. Three commands:\n`RecordCall`, `AttributeRecord`, `DeleteRecord`. Two views: `CallRecordById`, `RecentCalls`.\n\nBridges 6 and 7 are its two `relations:` entries, both `references`/one, both `Optional` — a record\noutlives the call it came from, and a call to a number nobody saved has no contact.\n\n## Why this is a domain and not a view over ended calls\n\nA view would have been cheaper. Three things ruled it out.\n\n- **`owns` forbids the shape a view implies.** A live call cannot own its history entry, because\n  `owns` means the far side cannot survive its owner.\n- **The vocabulary.** Recording `RemoteHangup` rather than `BYE` is what lets the next binding write\n  into the same log unchanged.\n- **Retention is not call control.** Answering it inside `softphone.control` would give the surface\n  an agent drives a second job.\n\n`remote` is copied into the record rather than referenced, so a record of a call to a contact that\nhas since been deleted still says what was dialled.\n\n## Not built\n\n`ClearHistory`. Erasing an unbounded set on one press is a separate decision and does not arrive by\nimplication from *keep a call log*.\n","summary":"softphone.history: one durable record per call, in the neutral termination vocabulary.","title":"Record the calls that finished"},"args":{"command":"create-entity","data":{"body":"# Record the calls that finished\n\n## Outcome\n\n`softphone.history` holds one durable record per finished call, in the neutral termination\nvocabulary, optionally attributed to a contact.\n\n## Acceptance\n\n`ess validate` accepts the domain. `CallRecord.termination` is\n`softphone.media.TerminationReason` and not a SIP cause. A record survives its call: the relation to\n`softphone.control.Call` is `references`, so nothing in the model lets a call take its record with\nit. There is no bulk-erase command.\n\n## What was built\n\n`ess/domains/history.yaml`. `CallRecord` — `record_id`, an optional `call_id`, an optional\n`contact_id`, `direction`, `remote`, `started_at`, `ended_at`, `termination`. Three commands:\n`RecordCall`, `AttributeRecord`, `DeleteRecord`. Two views: `CallRecordById`, `RecentCalls`.\n\nBridges 6 and 7 are its two `relations:` entries, both `references`/one, both `Optional` — a record\noutlives the call it came from, and a call to a number nobody saved has no contact.\n\n## Why this is a domain and not a view over ended calls\n\nA view would have been cheaper. Three things ruled it out.\n\n- **`owns` forbids the shape a view implies.** A live call cannot own its history entry, because\n  `owns` means the far side cannot survive its owner.\n- **The vocabulary.** Recording `RemoteHangup` rather than `BYE` is what lets the next binding write\n  into the same log unchanged.\n- **Retention is not call control.** Answering it inside `softphone.control` would give the surface\n  an agent drives a second job.\n\n`remote` is copied into the record rather than referenced, so a record of a call to a contact that\nhas since been deleted still says what was dialled.\n\n## Not built\n\n`ClearHistory`. Erasing an unbounded set on one press is a separate decision and does not arrive by\nimplication from *keep a call log*.\n","status":"draft","summary":"softphone.history: one durable record per call, in the neutral termination vocabulary.","title":"Record the calls that finished"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/call-history"},"payload":{"actor":"human:timo","at":1788513405349,"causation":"cmd-new-story-call-history","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:call-history@1#0~3a17ebb679731a90","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"call-history","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000052","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788513405366,"causation":"cmd-story-call-history-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:call-history@1#0~9982c924bebcfc31","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Hold a phonebook\n\n## Outcome\n\n`softphone.directory` holds contacts and the addresses they are reachable at, and a live call tile\ncan show a name instead of a number.\n\n## Acceptance\n\n`ess validate` accepts the domain. Deleting a contact cannot orphan an address: the relation is\n`owns`, and the compiler refuses a second owner. A `ContactAddress.address` is exactly the type\n`softphone.control.Dial` takes, so nothing converts between choosing a contact and calling one.\n`softphone.control` names no contact — grep it for `directory` and the result is empty.\n\n## What was built\n\n`ess/domains/directory.yaml`. `Contact` — `contact_id`, `display_name`. `ContactAddress` —\n`address_id`, `contact_id`, `address: softphone.control.RemoteAddress`, `label`. Five commands:\n`AddContact`, `RenameContact`, `AddAddress`, `RemoveAddress`, `DeleteContact`. Three views.\n\nBridge 5 is the `owns`/many entry on `Contact`, carried by `contact_id` on the address — the\ncanonical shape from the relations design document §2.1, and the reason the compiler can refuse a\nsecond owner at all.\n\nBridge 8 is on `softphone.presentation.CallTile`, not here: the tile is the thing that wants a name,\nso the tile is what references a contact. That keeps name resolution out of the control surface.\n\n## One grant that is a statement, not an omission\n\n`softphone.directory` declares `Human` alone. An agent dials the address it was given —\n`softphone.control.Dial` takes a `RemoteAddress`, never a contact — so it needs no grant here.\nWidening this to an agent is a decision somebody should take deliberately, not a gap to fill in\npassing.\n\n## Not built\n\nContact import or export, and any address book the browser did not receive by hand.\n","summary":"softphone.directory: contacts owning their addresses, and a name on a live call tile.","title":"Hold a phonebook"},"args":{"command":"create-entity","data":{"body":"# Hold a phonebook\n\n## Outcome\n\n`softphone.directory` holds contacts and the addresses they are reachable at, and a live call tile\ncan show a name instead of a number.\n\n## Acceptance\n\n`ess validate` accepts the domain. Deleting a contact cannot orphan an address: the relation is\n`owns`, and the compiler refuses a second owner. A `ContactAddress.address` is exactly the type\n`softphone.control.Dial` takes, so nothing converts between choosing a contact and calling one.\n`softphone.control` names no contact — grep it for `directory` and the result is empty.\n\n## What was built\n\n`ess/domains/directory.yaml`. `Contact` — `contact_id`, `display_name`. `ContactAddress` —\n`address_id`, `contact_id`, `address: softphone.control.RemoteAddress`, `label`. Five commands:\n`AddContact`, `RenameContact`, `AddAddress`, `RemoveAddress`, `DeleteContact`. Three views.\n\nBridge 5 is the `owns`/many entry on `Contact`, carried by `contact_id` on the address — the\ncanonical shape from the relations design document §2.1, and the reason the compiler can refuse a\nsecond owner at all.\n\nBridge 8 is on `softphone.presentation.CallTile`, not here: the tile is the thing that wants a name,\nso the tile is what references a contact. That keeps name resolution out of the control surface.\n\n## One grant that is a statement, not an omission\n\n`softphone.directory` declares `Human` alone. An agent dials the address it was given —\n`softphone.control.Dial` takes a `RemoteAddress`, never a contact — so it needs no grant here.\nWidening this to an agent is a decision somebody should take deliberately, not a gap to fill in\npassing.\n\n## Not built\n\nContact import or export, and any address book the browser did not receive by hand.\n","status":"draft","summary":"softphone.directory: contacts owning their addresses, and a name on a live call tile.","title":"Hold a phonebook"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/phonebook"},"payload":{"actor":"human:timo","at":1788513405535,"causation":"cmd-new-story-phonebook","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:phonebook@1#0~1085c4403c322712","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000057","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788513405546,"causation":"cmd-story-phonebook-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:phonebook@1#0~5f092886a7e376c8","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Bridge the layers with checked relations\n\n## Outcome\n\nThe eight links between the seven domains are `relations:` entries, so `ess validate` refuses a\nbroken one. A layering described in prose is a layering nobody can check.\n\n## Acceptance\n\nAll eight appear in the compiled IR under `entities.*.relations`, and each of the five refusal rules\nfires when its defect is introduced. Both were run; the results are below.\n\n## The eight\n\n| # | Source | Kind / card. | Target | via |\n|---|---|---|---|---|\n| 1 | `control.Call` | references / one | `media.MediaSession` | `session_id` |\n| 2 | `media.MediaSession` | owns / one | `sip.SipDialog` | `session_id` |\n| 3 | `media.MediaSession` | owns / one | `local.LoopbackDevice` | `session_id` |\n| 4 | `presentation.CallTile` | references / one | `control.Call` | `call_id` |\n| 5 | `directory.Contact` | owns / many | `directory.ContactAddress` | `contact_id` |\n| 6 | `history.CallRecord` | references / one | `control.Call` | `call_id` |\n| 7 | `history.CallRecord` | references / one | `directory.Contact` | `contact_id` |\n| 8 | `presentation.CallTile` | references / one | `directory.Contact` | `contact_id` |\n\nSeven of the eight cross a domain boundary. That works because `build_registry` indexes every\ndomain's types into one registry and resolves each reference against it\n(`crates/specify/ess-domain/src/system.rs:481-495`); nothing scopes resolution to a domain.\n\n## Evidence that the bridges are checked and not asserted\n\nFive defects, one at a time, each in a throwaway copy of `ess/` under `$TMPDIR` — never the\ncommitted tree. Every run exited 1.\n\n| Defect | Code returned |\n|---|---|\n| misspell a relation `target` | `undeclared_reference` |\n| `via` names a field that does not exist | `missing_declaration` |\n| retype a `via` field to `String` | `type_mismatch` |\n| a second `owns` targeting `SipDialog` | `conflicting_declaration` (with `type_mismatch`) |\n| two relations carried by one field | `duplicate_declaration` (with `type_mismatch`) |\n\nThe five expected codes are the five rules in the relations design document §3. The two secondary\n`type_mismatch` results are consequences of the same edit, not surprises: a stolen `owns` and a\nre-pointed `via` both leave the carrier holding the wrong identity type.\n\n## What no relation can hold\n\nTwo limits, both recorded in the domains rather than worked around.\n\n- **\"Exactly one of these two children exists\" is not expressible.** Bridges 2 and 3 are both\n  `owns`/one. Causation holds the correspondence instead — one `creates` outcome per binding.\n- **A command in one domain causing a command in another is not modelled.** Nothing declares that a\n  keypress in `softphone.presentation` eventually issues `softphone.control.Dial`. The relation is\n  checked; the causation is not, and a component accepting a command another component owns the\n  domain of is refused outright (`crates/specify/ess-domain/src/component.rs:921`).\n","summary":"Eight relations across seven domains, with the five refusal codes as evidence.","title":"Bridge the layers with checked relations"},"args":{"command":"create-entity","data":{"body":"# Bridge the layers with checked relations\n\n## Outcome\n\nThe eight links between the seven domains are `relations:` entries, so `ess validate` refuses a\nbroken one. A layering described in prose is a layering nobody can check.\n\n## Acceptance\n\nAll eight appear in the compiled IR under `entities.*.relations`, and each of the five refusal rules\nfires when its defect is introduced. Both were run; the results are below.\n\n## The eight\n\n| # | Source | Kind / card. | Target | via |\n|---|---|---|---|---|\n| 1 | `control.Call` | references / one | `media.MediaSession` | `session_id` |\n| 2 | `media.MediaSession` | owns / one | `sip.SipDialog` | `session_id` |\n| 3 | `media.MediaSession` | owns / one | `local.LoopbackDevice` | `session_id` |\n| 4 | `presentation.CallTile` | references / one | `control.Call` | `call_id` |\n| 5 | `directory.Contact` | owns / many | `directory.ContactAddress` | `contact_id` |\n| 6 | `history.CallRecord` | references / one | `control.Call` | `call_id` |\n| 7 | `history.CallRecord` | references / one | `directory.Contact` | `contact_id` |\n| 8 | `presentation.CallTile` | references / one | `directory.Contact` | `contact_id` |\n\nSeven of the eight cross a domain boundary. That works because `build_registry` indexes every\ndomain's types into one registry and resolves each reference against it\n(`crates/specify/ess-domain/src/system.rs:481-495`); nothing scopes resolution to a domain.\n\n## Evidence that the bridges are checked and not asserted\n\nFive defects, one at a time, each in a throwaway copy of `ess/` under `$TMPDIR` — never the\ncommitted tree. Every run exited 1.\n\n| Defect | Code returned |\n|---|---|\n| misspell a relation `target` | `undeclared_reference` |\n| `via` names a field that does not exist | `missing_declaration` |\n| retype a `via` field to `String` | `type_mismatch` |\n| a second `owns` targeting `SipDialog` | `conflicting_declaration` (with `type_mismatch`) |\n| two relations carried by one field | `duplicate_declaration` (with `type_mismatch`) |\n\nThe five expected codes are the five rules in the relations design document §3. The two secondary\n`type_mismatch` results are consequences of the same edit, not surprises: a stolen `owns` and a\nre-pointed `via` both leave the carrier holding the wrong identity type.\n\n## What no relation can hold\n\nTwo limits, both recorded in the domains rather than worked around.\n\n- **\"Exactly one of these two children exists\" is not expressible.** Bridges 2 and 3 are both\n  `owns`/one. Causation holds the correspondence instead — one `creates` outcome per binding.\n- **A command in one domain causing a command in another is not modelled.** Nothing declares that a\n  keypress in `softphone.presentation` eventually issues `softphone.control.Dial`. The relation is\n  checked; the causation is not, and a component accepting a command another component owns the\n  domain of is refused outright (`crates/specify/ess-domain/src/component.rs:921`).\n","status":"draft","summary":"Eight relations across seven domains, with the five refusal codes as evidence.","title":"Bridge the layers with checked relations"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/layer-bridges"},"payload":{"actor":"human:timo","at":1788513405773,"causation":"cmd-new-story-layer-bridges","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:layer-bridges@1#0~3cb4cc3fabc8273d","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000062","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788513405784,"causation":"cmd-story-layer-bridges-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:layer-bridges@1#0~526fecc06d46ae1c","executor":null,"recorded_at":"2026-09-04T09:16:45Z"}}
{"entity":"architecture-decision-record","version":1,"id":"layered-softphone-domains","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"proposed","changed":{"body":"# The softphone is specified as seven independent domains in one ESS system\n\n## Status\n\nAccepted 2026-09-04. Supersedes nothing recorded; `ess/domains/session.yaml`, the single conflated\ndomain it replaces, was removed rather than archived because it was never released.\n\n## Context\n\nThe first specification put a media session, a call, a registration and a SIP dialog in one domain,\n`softphone.session`. Three concepts that change for different reasons, at different rates, driven by\ndifferent actors, in one file. Two more arrived afterwards: a call log and a phonebook.\n\n## Decision\n\nSeven domains in one system, `softphone` v1, one per concern:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the protocol-neutral media session |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | what happened |\n| `softphone.directory` | who can be called |\n| `softphone.presentation` | what is on screen |\n| `softphone.sip` | the SIP binding |\n| `softphone.local` | the local-audio binding |\n\n**One system, not seven.** Cross-domain `relations:` are checked by `ess validate` — five refusal\nrules, all five exercised and recorded in `story:layer-bridges`. Three separate ESS systems bridged\nby an `ess-composition/1` document would be more independent and less checkable: `ess validate`\ncannot see across a composition boundary, so a broken link would surface at composition time\ninstead of at validate. Independence that costs the checker is not the independence this was for.\n\n**Two bindings, not one.** With a single binding the neutral layer and its only carrier are\nindistinguishable, and \"protocol-agnostic\" is an assertion. `softphone.local` is cheap on purpose:\nlocal audio, no network, no registration. Its existence is the evidence.\n\n**Browser only.** One deployment unit, every component `reached_by: in_process`, no\n`ess-composition/1` document, no service, no event log.\n\n## Consequences\n\n- 231 capabilities in web synthesis: 168 generated, 52 obligations, 11 refused. Two runs produce\n  byte-identical trees.\n- **Actor grants are not enforced by anything generated.** Synthesis refuses them: \"a grant is\n  checked against a caller identity, which types do not carry, and enforcement belongs to the layer\n  that knows who is calling\". The `Human`/`Agent` parity is a specification fact and a host\n  obligation, not a generated check.\n- **Storage is not specified and not generated.** Every view query is an obligation whose stated\n  reason is \"how the projection is kept current is a storage decision\". The phonebook and the\n  history are therefore host obligations against browser storage. That is one story, not a reason to\n  add a service — adding one would reverse the browser-only decision above.\n- Two things ESS cannot express are recorded in the domains that would have carried them: \"exactly\n  one of these two children exists\", and a command in one domain causing a command in another.\n- Adding an eighth domain for a second real protocol — `softphone.rtvbp` — touches no existing\n  domain. That is the property the split was bought for, and it is the next milestone.\n\n## Cited from\n\n- `docs/design/ess-entity-relations-design-v0.1.md` §2, §3 — the relation shape and the five rules.\n- `crates/specify/ess-domain/src/system.rs:481-495` — one type registry for the whole system.\n- `crates/specify/ess-domain/src/entity.rs:821-822` — an invariant reads only its own entity.\n- `crates/specify/ess-domain/src/component.rs:921` — a component cannot accept another's commands.\n- `connectors/crates/domain/src/voice.rs` — the neutral port `softphone.media` was read from.\n","summary":"One system, one domain per concern, bridged by relations ess validate can refuse.","title":"The softphone is specified as seven independent domains in one ESS system"},"args":{"command":"create-entity","data":{"body":"# The softphone is specified as seven independent domains in one ESS system\n\n## Status\n\nAccepted 2026-09-04. Supersedes nothing recorded; `ess/domains/session.yaml`, the single conflated\ndomain it replaces, was removed rather than archived because it was never released.\n\n## Context\n\nThe first specification put a media session, a call, a registration and a SIP dialog in one domain,\n`softphone.session`. Three concepts that change for different reasons, at different rates, driven by\ndifferent actors, in one file. Two more arrived afterwards: a call log and a phonebook.\n\n## Decision\n\nSeven domains in one system, `softphone` v1, one per concern:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the protocol-neutral media session |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | what happened |\n| `softphone.directory` | who can be called |\n| `softphone.presentation` | what is on screen |\n| `softphone.sip` | the SIP binding |\n| `softphone.local` | the local-audio binding |\n\n**One system, not seven.** Cross-domain `relations:` are checked by `ess validate` — five refusal\nrules, all five exercised and recorded in `story:layer-bridges`. Three separate ESS systems bridged\nby an `ess-composition/1` document would be more independent and less checkable: `ess validate`\ncannot see across a composition boundary, so a broken link would surface at composition time\ninstead of at validate. Independence that costs the checker is not the independence this was for.\n\n**Two bindings, not one.** With a single binding the neutral layer and its only carrier are\nindistinguishable, and \"protocol-agnostic\" is an assertion. `softphone.local` is cheap on purpose:\nlocal audio, no network, no registration. Its existence is the evidence.\n\n**Browser only.** One deployment unit, every component `reached_by: in_process`, no\n`ess-composition/1` document, no service, no event log.\n\n## Consequences\n\n- 231 capabilities in web synthesis: 168 generated, 52 obligations, 11 refused. Two runs produce\n  byte-identical trees.\n- **Actor grants are not enforced by anything generated.** Synthesis refuses them: \"a grant is\n  checked against a caller identity, which types do not carry, and enforcement belongs to the layer\n  that knows who is calling\". The `Human`/`Agent` parity is a specification fact and a host\n  obligation, not a generated check.\n- **Storage is not specified and not generated.** Every view query is an obligation whose stated\n  reason is \"how the projection is kept current is a storage decision\". The phonebook and the\n  history are therefore host obligations against browser storage. That is one story, not a reason to\n  add a service — adding one would reverse the browser-only decision above.\n- Two things ESS cannot express are recorded in the domains that would have carried them: \"exactly\n  one of these two children exists\", and a command in one domain causing a command in another.\n- Adding an eighth domain for a second real protocol — `softphone.rtvbp` — touches no existing\n  domain. That is the property the split was bought for, and it is the next milestone.\n\n## Cited from\n\n- `docs/design/ess-entity-relations-design-v0.1.md` §2, §3 — the relation shape and the five rules.\n- `crates/specify/ess-domain/src/system.rs:481-495` — one type registry for the whole system.\n- `crates/specify/ess-domain/src/entity.rs:821-822` — an invariant reads only its own entity.\n- `crates/specify/ess-domain/src/component.rs:921` — a component cannot accept another's commands.\n- `connectors/crates/domain/src/voice.rs` — the neutral port `softphone.media` was read from.\n","status":"proposed","summary":"One system, one domain per concern, bridged by relations ess validate can refuse.","title":"The softphone is specified as seven independent domains in one ESS system"},"entity_type":"aep.architecture-decision-record/v1","locator":"ep://planning/store/architecture-decision-record/layered-softphone-domains"},"payload":{"actor":"human:timo","at":1788513427637,"causation":"cmd-new-architecture-decision-record-layered-softphone-domains","change":{"change":"created","status":"proposed"},"correlation":"protocol-artifact-new","event_id":"architecture-decision-record:layered-softphone-domains@1#0~976bb58ac5c375ef","executor":null,"recorded_at":"2026-09-04T09:17:07Z"}}
{"entity":"architecture-decision-record","version":1,"id":"layered-softphone-domains","revision":1,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decides":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decides","source":"01MEM0000000000000067","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788513427652,"causation":"cmd-architecture-decision-record-layered-softphone-domains-decides-epic-browser-softphone","change":{"change":"related","relation":"decides","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"architecture-decision-record:layered-softphone-domains@1#0~314629845700448e","executor":null,"recorded_at":"2026-09-04T09:17:07Z"}}
{"entity":"architecture-decision-record","version":1,"id":"layered-softphone-domains","revision":2,"type":"aep.status.move/v1","from_state":"proposed","to_state":"accepted","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"accepted"},"payload":{"actor":"human:timo","at":1788513428265,"causation":"cmd-move-architecture-decision-record-layered-softphone-domains-1788513428265","change":{"change":"moved","decided_on":{},"from":"proposed","to":"accepted"},"correlation":"protocol-artifact-move","event_id":"architecture-decision-record:layered-softphone-domains@2#0~c2833842ca4a242c","executor":null,"recorded_at":"2026-09-04T09:17:08Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.session.Register` through to `Registered` or `Failed`.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\nThis story is now scoped to `softphone.sip` — `ess/domains/sip.yaml` — and nothing else. Registration\nand the dialog live there; the call lifecycle moved to `softphone.control` and the media session to\n`softphone.media`.\n\n- `Registration` — `aor`, `transport`, four states, four commands. `SignallingTransport` has one\n  member, `SecureWebSocket`, because a browser can open no other and the page is served over HTTPS.\n- `SipDialog` — `session_id` typed `softphone.media.MediaSessionId`, which is the carrier of bridge 2.\n  `OpenDialog` is the only outcome that creates one, and that is what holds the binding\n  discriminator on `MediaSession` honest, since no invariant can.\n- `MediaSecurity` has one member, `DtlsSrtp`: the browser negotiates media and that is the only\n  keying `RTCPeerConnection` offers.\n- `SipCause` stays text. Inventing a closed set of SIP causes here would be inventing the protocol's\n  vocabulary; the call log records `softphone.media.TerminationReason` instead.\n\nImplementation, unchanged from before: the vendored `browser/src/` binding from the pinned sipx\nrevision, the WASM module from `crates/sipx-wasm` and `wasm/`, and the hand-written ABI glue that\n`A-17` has not generated.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"args":{"changes":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.session.Register` through to `Registered` or `Failed`.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\nThis story is now scoped to `softphone.sip` — `ess/domains/sip.yaml` — and nothing else. Registration\nand the dialog live there; the call lifecycle moved to `softphone.control` and the media session to\n`softphone.media`.\n\n- `Registration` — `aor`, `transport`, four states, four commands. `SignallingTransport` has one\n  member, `SecureWebSocket`, because a browser can open no other and the page is served over HTTPS.\n- `SipDialog` — `session_id` typed `softphone.media.MediaSessionId`, which is the carrier of bridge 2.\n  `OpenDialog` is the only outcome that creates one, and that is what holds the binding\n  discriminator on `MediaSession` honest, since no invariant can.\n- `MediaSecurity` has one member, `DtlsSrtp`: the browser negotiates media and that is the only\n  keying `RTCPeerConnection` offers.\n- `SipCause` stays text. Inventing a closed set of SIP causes here would be inventing the protocol's\n  vocabulary; the call log records `softphone.media.TerminationReason` instead.\n\nImplementation, unchanged from before: the vendored `browser/src/` binding from the pinned sipx\nrevision, the WASM module from `crates/sipx-wasm` and `wasm/`, and the hand-written ABI glue that\n`A-17` has not generated.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788513465581,"causation":"cmd-protocol-artifact-body-section-story-signalling-over-wss-1788513465581","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:signalling-over-wss@3#0~3d53a51735c0628a","executor":null,"recorded_at":"2026-09-04T09:17:45Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nWhat changed with the layering: the adapter no longer implements a domain. It **fills obligations**.\n`softphone.media` states the session and its signals; `softphone.sip` states the dialog; web\nsynthesis emits both as unfilled command behaviours answering with a typed refusal naming what is\nowed. This story fills the SIP binding's share of them with a real `RTCPeerConnection`.\n\nThe neutral port is what the adapter satisfies, not a shape of its own: `MediaProfile` is\n`pcm_s16le`/8000/1/20 ms/320 B, signals are DTMF only, and a session ends with one of the eight\n`softphone.media.TerminationReason` variants — so a media failure arrives as `MediaFailed` and never\nas a SIP cause.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"args":{"changes":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.session.CallFailed` with a media\ncause rather than a SIP failure.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nWhat changed with the layering: the adapter no longer implements a domain. It **fills obligations**.\n`softphone.media` states the session and its signals; `softphone.sip` states the dialog; web\nsynthesis emits both as unfilled command behaviours answering with a typed refusal naming what is\nowed. This story fills the SIP binding's share of them with a real `RTCPeerConnection`.\n\nThe neutral port is what the adapter satisfies, not a shape of its own: `MediaProfile` is\n`pcm_s16le`/8000/1/20 ms/320 B, signals are DTMF only, and a session ends with one of the eight\n`softphone.media.TerminationReason` variants — so a media failure arrives as `MediaFailed` and never\nas a SIP cause.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788513466196,"causation":"cmd-protocol-artifact-body-section-story-browser-media-adapter-1788513466196","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:browser-media-adapter@4#0~eac2676f3111efde","executor":null,"recorded_at":"2026-09-04T09:17:46Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Place, answer and end calls\n\n## Outcome\n\n`softphone.control` is the phone's whole surface, and a human and an agent drive it with the same\ncommands. No protocol type and no contact appears in it.\n\n## Acceptance\n\n`ess validate` accepts the domain. `Human`'s and `Agent`'s `may:` lists are identical — read from\nthe compiled IR, not the YAML — and the agent is granted nothing in `softphone.presentation`. A\ncommand issued from a state its transition does not allow returns\n`softphone.control.CallStateConflict` and appends nothing. Grep the domain for `sip`, `rtvbp`,\n`webrtc` and `directory`: all empty.\n\n## What was built\n\n`ess/domains/control.yaml`. `PhoneEndpoint` — the phone itself, with a `default_binding` typed\n`softphone.media.BindingKind`. `Call` — `endpoint_id`, `direction`, `remote`, an optional\n`session_id`, `muted`; states `Requested → Ringing → Active → Ended`.\n\nTen commands, and three actors:\n\n| Actor | Grants |\n|---|---|\n| `Human` | `ConfigureEndpoint`, `Dial`, `Answer`, `Reject`, `HangUp`, `SendDigits`, `SetMuted` |\n| `Agent` | the same seven, byte-identical |\n| `Kernel` | `OfferCall`, `RingCall`, `ConfirmAnswer` |\n\n`Answer` and `ConfirmAnswer` take the same `activate` transition and are different facts: the first\nis the local decision on an inbound call, the second is the far end answering an outbound one. Only\n`Kernel` may assert the second, so a page or an agent cannot claim the far end picked up. `Reject`\nand `HangUp` stand in the same relation over `end`.\n\n`Call.session_id` is bridge 1 — `references`, not `owns`, because a media session is a thing in its\nown right and a call that ends does not take it with it.\n\n## The parity is the specification, and nothing enforces it\n\nWeb synthesis refuses actor grants: \"a grant is checked against a caller identity, which types do\nnot carry, and enforcement belongs to the layer that knows who is calling\". So `Human == Agent` is a\nfact a reader and a checker of the IR can read, and a host obligation. It is not a generated check.\n\nVerified from the IR rather than the source: an earlier check compared two absent keys and reported\na pass. Seven commands each, identical.\n"},"args":{"changes":{"body":"# Place, answer and end calls\n\n## Outcome\n\n`softphone.control` is the phone's whole surface, and a human and an agent drive it with the same\ncommands. No protocol type and no contact appears in it.\n\n## Acceptance\n\n`ess validate` accepts the domain. `Human`'s and `Agent`'s `may:` lists are identical — read from\nthe compiled IR, not the YAML — and the agent is granted nothing in `softphone.presentation`. A\ncommand issued from a state its transition does not allow returns\n`softphone.control.CallStateConflict` and appends nothing. Grep the domain for `sip`, `rtvbp`,\n`webrtc` and `directory`: all empty.\n\n## What was built\n\n`ess/domains/control.yaml`. `PhoneEndpoint` — the phone itself, with a `default_binding` typed\n`softphone.media.BindingKind`. `Call` — `endpoint_id`, `direction`, `remote`, an optional\n`session_id`, `muted`; states `Requested → Ringing → Active → Ended`.\n\nTen commands, and three actors:\n\n| Actor | Grants |\n|---|---|\n| `Human` | `ConfigureEndpoint`, `Dial`, `Answer`, `Reject`, `HangUp`, `SendDigits`, `SetMuted` |\n| `Agent` | the same seven, byte-identical |\n| `Kernel` | `OfferCall`, `RingCall`, `ConfirmAnswer` |\n\n`Answer` and `ConfirmAnswer` take the same `activate` transition and are different facts: the first\nis the local decision on an inbound call, the second is the far end answering an outbound one. Only\n`Kernel` may assert the second, so a page or an agent cannot claim the far end picked up. `Reject`\nand `HangUp` stand in the same relation over `end`.\n\n`Call.session_id` is bridge 1 — `references`, not `owns`, because a media session is a thing in its\nown right and a call that ends does not take it with it.\n\n## The parity is the specification, and nothing enforces it\n\nWeb synthesis refuses actor grants: \"a grant is checked against a caller identity, which types do\nnot carry, and enforcement belongs to the layer that knows who is calling\". So `Human == Agent` is a\nfact a reader and a checker of the IR can read, and a host obligation. It is not a generated check.\n\nVerified from the IR rather than the source: an earlier check compared two absent keys and reported\na pass. Seven commands each, identical.\n"},"command":"update-entity","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788513466438,"causation":"cmd-protocol-artifact-body-story-call-lifecycle-1788513466438","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:call-lifecycle@2#0~5454c1e0bb87c6f1","executor":null,"recorded_at":"2026-09-04T09:17:46Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# The softphone page\n\n## Outcome\n\nA surface a person can use: a keypad, a tile per call carrying a contact's name, and controls to\nanswer, hang up and mute.\n\n## Acceptance\n\nThe generated web surface drives a call before any page is hand-written.\n`ess generate synthesize --path ess --target web --out <dir>` emits it — 14 artifacts including\n`index.html`, `bridge.js`, `catalog.json` and the `softphone-web` WASM crate — and its command list,\nforms, event log and state panel are built from the model. Driving a command with nothing installed\nreturns the typed refusal naming what is owed; that is the correct empty state, not a failure.\n\n## What was built\n\n`ess/domains/presentation.yaml`. Two entities and nothing else:\n\n- `Keypad` — a partially typed address. Not a `softphone.control.RemoteAddress`: it becomes one at\n  the moment somebody dials, and until then it is text on a screen.\n- `CallTile` — `call_id` (bridge 4, not optional: a tile with no call is not a tile) and an optional\n  `contact_id` (bridge 8). The tile is the thing that wants a name, which is why name resolution is\n  here and not in the control surface.\n\n`DismissCall` dismisses the tile. It does not end the call — that is `softphone.control.HangUp`, and\nkeeping them apart is what stops a screen change from dropping a conversation. `PressKey` is not how\ndigits reach a live call either; that is `softphone.control.SendDigits`, a different surface and a\ndifferent grant.\n\n`Human` is the only actor. That is the other half of the actor-neutrality statement:\n`softphone.control` grants `Human` and `Agent` the same commands, and this domain grants an agent\nnothing. An agent drives the phone; it does not drive a screen.\n\n## Browser policy\n\nRegistration may start on load; **media must not.** Placing or answering a call runs from an explicit\nuser gesture in a secure context, because microphone permission and audio autoplay require both\n(`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §3, §7).\n\n## Where the state lives, and what is not yet known\n\nEvery view query in synthesis is an obligation, and the reason it gives is \"how the projection is\nkept current is a storage decision\". So the specification chooses no storage and the generated page\nimplements none. Whether the emitted page keeps anything across a reload has not been observed —\nloading it needs a browser. If it does not, the keypad, the tiles, the phonebook and the history are\nhost obligations against browser storage.\n"},"args":{"changes":{"body":"# The softphone page\n\n## Outcome\n\nA surface a person can use: a keypad, a tile per call carrying a contact's name, and controls to\nanswer, hang up and mute.\n\n## Acceptance\n\nThe generated web surface drives a call before any page is hand-written.\n`ess generate synthesize --path ess --target web --out <dir>` emits it — 14 artifacts including\n`index.html`, `bridge.js`, `catalog.json` and the `softphone-web` WASM crate — and its command list,\nforms, event log and state panel are built from the model. Driving a command with nothing installed\nreturns the typed refusal naming what is owed; that is the correct empty state, not a failure.\n\n## What was built\n\n`ess/domains/presentation.yaml`. Two entities and nothing else:\n\n- `Keypad` — a partially typed address. Not a `softphone.control.RemoteAddress`: it becomes one at\n  the moment somebody dials, and until then it is text on a screen.\n- `CallTile` — `call_id` (bridge 4, not optional: a tile with no call is not a tile) and an optional\n  `contact_id` (bridge 8). The tile is the thing that wants a name, which is why name resolution is\n  here and not in the control surface.\n\n`DismissCall` dismisses the tile. It does not end the call — that is `softphone.control.HangUp`, and\nkeeping them apart is what stops a screen change from dropping a conversation. `PressKey` is not how\ndigits reach a live call either; that is `softphone.control.SendDigits`, a different surface and a\ndifferent grant.\n\n`Human` is the only actor. That is the other half of the actor-neutrality statement:\n`softphone.control` grants `Human` and `Agent` the same commands, and this domain grants an agent\nnothing. An agent drives the phone; it does not drive a screen.\n\n## Browser policy\n\nRegistration may start on load; **media must not.** Placing or answering a call runs from an explicit\nuser gesture in a secure context, because microphone permission and audio autoplay require both\n(`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §3, §7).\n\n## Where the state lives, and what is not yet known\n\nEvery view query in synthesis is an obligation, and the reason it gives is \"how the projection is\nkept current is a storage decision\". So the specification chooses no storage and the generated page\nimplements none. Whether the emitted page keeps anything across a reload has not been observed —\nloading it needs a browser. If it does not, the keypad, the tiles, the phonebook and the history are\nhost obligations against browser storage.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788513466548,"causation":"cmd-protocol-artifact-body-story-softphone-page-1788513466548","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:softphone-page@3#0~15680c4eb91923b3","executor":null,"recorded_at":"2026-09-04T09:17:46Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, keep a log of them and\na phonebook, and show it all in a browser page. Signalling and dialog state come from the sipx\nWebAssembly session kernel (`crates/sipx-wasm`, story S-41) driven over the WebSocket binding\n(`browser/src/`, story T-33), both present at the pinned revision. Media is negotiated by the\nbrowser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project — **seven domains, one per\nconcern**, in system `softphone` v1:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the protocol-neutral media session; names no protocol at all |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | one durable record per finished call |\n| `softphone.directory` | contacts and the addresses they are reachable at |\n| `softphone.presentation` | the keypad and the call tiles |\n| `softphone.sip` | the SIP binding |\n| `softphone.local` | the local-audio binding, whose existence is what makes the media layer's neutrality checkable |\n\nThey are bridged by **eight `relations:` entries**, seven of them crossing a domain boundary. That\nis the point of one system rather than seven: `ess validate` refuses a broken bridge — five rules,\nall five exercised, recorded in `story:layer-bridges`. A composition boundary would not be checked\nthere.\n\n`ess validate --path ess` reports `softphone v1 — 9 file(s), valid`.\n`ess generate synthesize --target web` emits 231 capabilities: 168 generated, 52 obligations, 11\nrefused, byte-identical across two runs.\n\n`architecture-decision-record:layered-softphone-domains` holds why this shape and not three systems,\nand what ESS could not be made to say.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n"},"args":{"changes":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, keep a log of them and\na phonebook, and show it all in a browser page. Signalling and dialog state come from the sipx\nWebAssembly session kernel (`crates/sipx-wasm`, story S-41) driven over the WebSocket binding\n(`browser/src/`, story T-33), both present at the pinned revision. Media is negotiated by the\nbrowser's own `RTCPeerConnection`.\n\nThe domain is modelled before decomposition, at `ess/` in this project — **seven domains, one per\nconcern**, in system `softphone` v1:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the protocol-neutral media session; names no protocol at all |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | one durable record per finished call |\n| `softphone.directory` | contacts and the addresses they are reachable at |\n| `softphone.presentation` | the keypad and the call tiles |\n| `softphone.sip` | the SIP binding |\n| `softphone.local` | the local-audio binding, whose existence is what makes the media layer's neutrality checkable |\n\nThey are bridged by **eight `relations:` entries**, seven of them crossing a domain boundary. That\nis the point of one system rather than seven: `ess validate` refuses a broken bridge — five rules,\nall five exercised, recorded in `story:layer-bridges`. A composition boundary would not be checked\nthere.\n\n`ess validate --path ess` reports `softphone v1 — 9 file(s), valid`.\n`ess generate synthesize --target web` emits 231 capabilities: 168 generated, 52 obligations, 11\nrefused, byte-identical across two runs.\n\n`architecture-decision-record:layered-softphone-domains` holds why this shape and not three systems,\nand what ESS could not be made to say.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n"},"command":"update-entity","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788513482089,"causation":"cmd-protocol-artifact-body-section-epic-browser-softphone-1788513482089","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:browser-softphone@4#0~1edfffd391eb865f","executor":null,"recorded_at":"2026-09-04T09:18:02Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":3,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000034","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788513490625,"causation":"cmd-story-signalling-over-wss-depends_on-story-media-session-domain","change":{"change":"related","relation":"depends_on","target":"story:media-session-domain"},"correlation":"protocol-artifact-relate","event_id":"story:signalling-over-wss@3#0~65bab2fe803d4995","executor":null,"recorded_at":"2026-09-04T09:18:10Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":2,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:signalling-over-wss"},{"depends_on":"story:media-session-domain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000022","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788513503570,"causation":"cmd-story-call-lifecycle-depends_on-story-media-session-domain","change":{"change":"related","relation":"depends_on","target":"story:media-session-domain"},"correlation":"protocol-artifact-relate","event_id":"story:call-lifecycle@2#0~b87aa4f524fd95c8","executor":null,"recorded_at":"2026-09-04T09:18:23Z"}}
{"entity":"story","version":1,"id":"call-history","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000019","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788513503687,"causation":"cmd-story-call-history-depends_on-story-media-session-domain","change":{"change":"related","relation":"depends_on","target":"story:media-session-domain"},"correlation":"protocol-artifact-relate","event_id":"story:call-history@1#0~a557f77abe4936da","executor":null,"recorded_at":"2026-09-04T09:18:23Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000031","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788513503777,"causation":"cmd-story-phonebook-depends_on-story-media-session-domain","change":{"change":"related","relation":"depends_on","target":"story:media-session-domain"},"correlation":"protocol-artifact-relate","event_id":"story:phonebook@1#0~e5189dba56a99f94","executor":null,"recorded_at":"2026-09-04T09:18:23Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000025","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788513503863,"causation":"cmd-story-layer-bridges-depends_on-story-media-session-domain","change":{"change":"related","relation":"depends_on","target":"story:media-session-domain"},"correlation":"protocol-artifact-relate","event_id":"story:layer-bridges@1#0~307c33982087224f","executor":null,"recorded_at":"2026-09-04T09:18:23Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"},{"depends_on":"story:signalling-over-wss"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000025","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788513503950,"causation":"cmd-story-layer-bridges-depends_on-story-signalling-over-wss","change":{"change":"related","relation":"depends_on","target":"story:signalling-over-wss"},"correlation":"protocol-artifact-relate","event_id":"story:layer-bridges@1#0~c30009a1001c3bd6","executor":null,"recorded_at":"2026-09-04T09:18:23Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"},{"depends_on":"story:signalling-over-wss"},{"depends_on":"story:call-lifecycle"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000025","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788513504040,"causation":"cmd-story-layer-bridges-depends_on-story-call-lifecycle","change":{"change":"related","relation":"depends_on","target":"story:call-lifecycle"},"correlation":"protocol-artifact-relate","event_id":"story:layer-bridges@1#0~d9ea9d97ef821925","executor":null,"recorded_at":"2026-09-04T09:18:24Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"},{"depends_on":"story:signalling-over-wss"},{"depends_on":"story:call-lifecycle"},{"depends_on":"story:call-history"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000025","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788513504152,"causation":"cmd-story-layer-bridges-depends_on-story-call-history","change":{"change":"related","relation":"depends_on","target":"story:call-history"},"correlation":"protocol-artifact-relate","event_id":"story:layer-bridges@1#0~72a8948f286968eb","executor":null,"recorded_at":"2026-09-04T09:18:24Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"},{"depends_on":"story:signalling-over-wss"},{"depends_on":"story:call-lifecycle"},{"depends_on":"story:call-history"},{"depends_on":"story:phonebook"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000025","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788513504290,"causation":"cmd-story-layer-bridges-depends_on-story-phonebook","change":{"change":"related","relation":"depends_on","target":"story:phonebook"},"correlation":"protocol-artifact-relate","event_id":"story:layer-bridges@1#0~97be3ca0e79fd289","executor":null,"recorded_at":"2026-09-04T09:18:24Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":3,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:call-lifecycle"},{"depends_on":"story:browser-media-adapter"},{"depends_on":"story:layer-bridges"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000037","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788513504379,"causation":"cmd-story-softphone-page-depends_on-story-layer-bridges","change":{"change":"related","relation":"depends_on","target":"story:layer-bridges"},"correlation":"protocol-artifact-relate","event_id":"story:softphone-page@3#0~fbc5a5a30a6380d9","executor":null,"recorded_at":"2026-09-04T09:18:24Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-web-target-redeliver","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# The generated web target does not compile at ESS 0.13.1\n\n## What was observed\n\n`ess generate synthesize --path ess --target web` emits a tree that fails to build:\n\n```\ncrates/softphone-web/src/lib.rs:377:14\n    self.redeliver(&event)?;\n         ^^^^^^^^^ method not found in `&mut System<CallHistoryBehaviors, ..., ...>`\nerror: could not compile `softphone-web` (lib) due to 1 previous error\n```\n\n`redeliver` appears **nowhere** in the rust target the web crate depends on — `grep -rn redeliver`\nover all 30 emitted Rust artifacts returns nothing. The web emitter emits a call to a method the\nrust emitter does not generate.\n\n## It is not this specification\n\nThe rust target from the same specification builds clean: `cargo build --release` →\n`Finished release profile [optimized] target(s) in 1.02s`, all nine crates. Both targets report the\nsame 231 capabilities — 168 generated, 52 obligations, 11 refused — and web synthesis is\nbyte-identical across two runs. The specification validates: `softphone v1 — 9 file(s), valid`.\n\nSo the defect is in `ess generate synthesize --target web` at `0.13.1`\n(`git+https://github.com/beyond10x/ess?tag=0.13.1#d1a66772a91b5411d942d7a45bbf08dfc5de4651`), in the\n`replay` function of the emitted bridge.\n\n## What it blocks\n\n`story:softphone-page`'s acceptance is *the generated web surface drives a call before any page is\nhand-written*. That surface cannot be loaded, so no browser observation of this specification can be\nproduced at all — which also leaves the open question of whether the page persists anything across\na reload unanswerable.\n\n## What would clear it\n\nA fix upstream in `beyond10x/ess`: either the web emitter stops calling `redeliver`, or the rust\nemitter generates it. One line of the emitted bridge names the seam.\n\nFiled as an `upstream-blocker` rather than a `decision-blocker` because there is nothing here to\ndecide. `aep artifact kinds` does not list this kind by name — the `<type>-blocker` family is open\nand `aep artifact lifecycle upstream-blocker` answers `open -> cleared`.\n\n## What is not blocked\n\nEverything specification-side. All eight bridges are in the compiled IR, the five refusal codes were\nobserved, and the rust target builds — so the layering is verified without the page.\n","title":"ESS 0.13.1 web synthesis emits a call to a method its rust target does not generate","withholds":"test_result"},"args":{"command":"create-entity","data":{"body":"# The generated web target does not compile at ESS 0.13.1\n\n## What was observed\n\n`ess generate synthesize --path ess --target web` emits a tree that fails to build:\n\n```\ncrates/softphone-web/src/lib.rs:377:14\n    self.redeliver(&event)?;\n         ^^^^^^^^^ method not found in `&mut System<CallHistoryBehaviors, ..., ...>`\nerror: could not compile `softphone-web` (lib) due to 1 previous error\n```\n\n`redeliver` appears **nowhere** in the rust target the web crate depends on — `grep -rn redeliver`\nover all 30 emitted Rust artifacts returns nothing. The web emitter emits a call to a method the\nrust emitter does not generate.\n\n## It is not this specification\n\nThe rust target from the same specification builds clean: `cargo build --release` →\n`Finished release profile [optimized] target(s) in 1.02s`, all nine crates. Both targets report the\nsame 231 capabilities — 168 generated, 52 obligations, 11 refused — and web synthesis is\nbyte-identical across two runs. The specification validates: `softphone v1 — 9 file(s), valid`.\n\nSo the defect is in `ess generate synthesize --target web` at `0.13.1`\n(`git+https://github.com/beyond10x/ess?tag=0.13.1#d1a66772a91b5411d942d7a45bbf08dfc5de4651`), in the\n`replay` function of the emitted bridge.\n\n## What it blocks\n\n`story:softphone-page`'s acceptance is *the generated web surface drives a call before any page is\nhand-written*. That surface cannot be loaded, so no browser observation of this specification can be\nproduced at all — which also leaves the open question of whether the page persists anything across\na reload unanswerable.\n\n## What would clear it\n\nA fix upstream in `beyond10x/ess`: either the web emitter stops calling `redeliver`, or the rust\nemitter generates it. One line of the emitted bridge names the seam.\n\nFiled as an `upstream-blocker` rather than a `decision-blocker` because there is nothing here to\ndecide. `aep artifact kinds` does not list this kind by name — the `<type>-blocker` family is open\nand `aep artifact lifecycle upstream-blocker` answers `open -> cleared`.\n\n## What is not blocked\n\nEverything specification-side. All eight bridges are in the compiled IR, the five refusal codes were\nobserved, and the rust target builds — so the layering is verified without the page.\n","status":"open","title":"ESS 0.13.1 web synthesis emits a call to a method its rust target does not generate","withholds":"test_result"},"entity_type":"aep.upstream-blocker/v1","locator":"ep://planning/store/upstream-blocker/ess-web-target-redeliver"},"payload":{"actor":"human:timo","at":1788513619437,"causation":"cmd-new-upstream-blocker-ess-web-target-redeliver","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"upstream-blocker:ess-web-target-redeliver@1#0~45cb42ad8e7c8e8e","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-web-target-redeliver","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:softphone-page"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000092","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788513619530,"causation":"cmd-upstream-blocker-ess-web-target-redeliver-blocks-story-softphone-page","change":{"change":"related","relation":"blocks","target":"story:softphone-page"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-web-target-redeliver@1#0~6026ec149fd303b6","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788513619000,"causation":"cmd-evidence-story-media-session-domain-test_result-1788513619629","change":{"change":"evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid"},"correlation":"protocol-artifact-evidence","event_id":"story:media-session-domain@1#0~30300c006bd963cd","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"call-history","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788513619000,"causation":"cmd-evidence-story-call-history-test_result-1788513619723","change":{"change":"evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid"},"correlation":"protocol-artifact-evidence","event_id":"story:call-history@1#0~67af35187e1252d5","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788513619000,"causation":"cmd-evidence-story-phonebook-test_result-1788513619809","change":{"change":"evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid"},"correlation":"protocol-artifact-evidence","event_id":"story:phonebook@1#0~97570d0932db3eb7","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788513619000,"causation":"cmd-evidence-story-layer-bridges-test_result-1788513619904","change":{"change":"evidence","kind":"test_result","reference":"file:///home/timo/beyond10x/widgets/widgets/phone/ess","source":"ess validate --path ess -> softphone v1 - 9 file(s), valid"},"correlation":"protocol-artifact-evidence","event_id":"story:layer-bridges@1#0~d0004900351c750a","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"five relation defects in throwaway copies -> undeclared_reference, missing_declaration, type_mismatch, conflicting_declaration, duplicate_declaration; all exit 1","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788513619000,"causation":"cmd-evidence-story-layer-bridges-test_result-1788513619988","change":{"change":"evidence","kind":"test_result","source":"five relation defects in throwaway copies -> undeclared_reference, missing_declaration, type_mismatch, conflicting_declaration, duplicate_declaration; all exit 1"},"correlation":"protocol-artifact-evidence","event_id":"story:layer-bridges@1#0~975fcca254834282","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":2,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"compiled IR: softphone.control.Human and .Agent may-lists identical, 7 commands each; Agent granted nothing in softphone.presentation","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788513619000,"causation":"cmd-evidence-story-call-lifecycle-test_result-1788513620078","change":{"change":"evidence","kind":"test_result","source":"compiled IR: softphone.control.Human and .Agent may-lists identical, 7 commands each; Agent granted nothing in softphone.presentation"},"correlation":"protocol-artifact-evidence","event_id":"story:call-lifecycle@2#0~18098891990ff33e","executor":null,"recorded_at":"2026-09-04T09:20:19Z"}}
{"entity":"story","version":1,"id":"softphone-page","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# The softphone page\n\n## Outcome\n\nA surface a person can use: a keypad, a tile per call carrying a contact's name, and controls to\nanswer, hang up and mute.\n\n## Acceptance\n\nThe generated web surface drives a call before any page is hand-written.\n`ess generate synthesize --path ess --target web --out <dir>` emits it — 14 artifacts including\n`index.html`, `bridge.js`, `catalog.json` and the `softphone-web` WASM crate — and its command list,\nforms, event log and state panel are built from the model. Driving a command with nothing installed\nreturns the typed refusal naming what is owed; that is the correct empty state, not a failure.\n\n## What was built\n\n`ess/domains/presentation.yaml`. Two entities and nothing else:\n\n- `Keypad` — a partially typed address. Not a `softphone.control.RemoteAddress`: it becomes one at\n  the moment somebody dials, and until then it is text on a screen.\n- `CallTile` — `call_id` (bridge 4, not optional: a tile with no call is not a tile) and an optional\n  `contact_id` (bridge 8). The tile is the thing that wants a name, which is why name resolution is\n  here and not in the control surface.\n\n`DismissCall` dismisses the tile. It does not end the call — that is `softphone.control.HangUp`, and\nkeeping them apart is what stops a screen change from dropping a conversation. `PressKey` is not how\ndigits reach a live call either; that is `softphone.control.SendDigits`, a different surface and a\ndifferent grant.\n\n`Human` is the only actor. That is the other half of the actor-neutrality statement:\n`softphone.control` grants `Human` and `Agent` the same commands, and this domain grants an agent\nnothing. An agent drives the phone; it does not drive a screen.\n\n## Browser policy\n\nRegistration may start on load; **media must not.** Placing or answering a call runs from an explicit\nuser gesture in a secure context, because microphone permission and audio autoplay require both\n(`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §3, §7).\n\n## Where the state lives, and what is not yet known\n\nEvery view query in synthesis is an obligation, and the reason it gives is \"how the projection is\nkept current is a storage decision\". So the specification chooses no storage and the generated page\nimplements none.\n\nWhether the page keeps anything across a reload is **still unanswered, and currently unanswerable**:\nthe emitted web tree does not compile at ESS 0.13.1. See\n`upstream-blocker:ess-web-target-redeliver` — `crates/softphone-web/src/lib.rs:377` calls\n`self.redeliver(&event)`, which the rust target it depends on does not generate. The rust target\nfrom the same specification builds clean, so this is the emitter and not the model.\n\nUntil that clears, this story has no way to produce a browser observation of anything.\n"},"args":{"changes":{"body":"# The softphone page\n\n## Outcome\n\nA surface a person can use: a keypad, a tile per call carrying a contact's name, and controls to\nanswer, hang up and mute.\n\n## Acceptance\n\nThe generated web surface drives a call before any page is hand-written.\n`ess generate synthesize --path ess --target web --out <dir>` emits it — 14 artifacts including\n`index.html`, `bridge.js`, `catalog.json` and the `softphone-web` WASM crate — and its command list,\nforms, event log and state panel are built from the model. Driving a command with nothing installed\nreturns the typed refusal naming what is owed; that is the correct empty state, not a failure.\n\n## What was built\n\n`ess/domains/presentation.yaml`. Two entities and nothing else:\n\n- `Keypad` — a partially typed address. Not a `softphone.control.RemoteAddress`: it becomes one at\n  the moment somebody dials, and until then it is text on a screen.\n- `CallTile` — `call_id` (bridge 4, not optional: a tile with no call is not a tile) and an optional\n  `contact_id` (bridge 8). The tile is the thing that wants a name, which is why name resolution is\n  here and not in the control surface.\n\n`DismissCall` dismisses the tile. It does not end the call — that is `softphone.control.HangUp`, and\nkeeping them apart is what stops a screen change from dropping a conversation. `PressKey` is not how\ndigits reach a live call either; that is `softphone.control.SendDigits`, a different surface and a\ndifferent grant.\n\n`Human` is the only actor. That is the other half of the actor-neutrality statement:\n`softphone.control` grants `Human` and `Agent` the same commands, and this domain grants an agent\nnothing. An agent drives the phone; it does not drive a screen.\n\n## Browser policy\n\nRegistration may start on load; **media must not.** Placing or answering a call runs from an explicit\nuser gesture in a secure context, because microphone permission and audio autoplay require both\n(`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §3, §7).\n\n## Where the state lives, and what is not yet known\n\nEvery view query in synthesis is an obligation, and the reason it gives is \"how the projection is\nkept current is a storage decision\". So the specification chooses no storage and the generated page\nimplements none.\n\nWhether the page keeps anything across a reload is **still unanswered, and currently unanswerable**:\nthe emitted web tree does not compile at ESS 0.13.1. See\n`upstream-blocker:ess-web-target-redeliver` — `crates/softphone-web/src/lib.rs:377` calls\n`self.redeliver(&event)`, which the rust target it depends on does not generate. The rust target\nfrom the same specification builds clean, so this is the emitter and not the model.\n\nUntil that clears, this story has no way to produce a browser observation of anything.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788513629323,"causation":"cmd-protocol-artifact-body-section-story-softphone-page-1788513629323","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:softphone-page@4#0~512ca02eeaf24da6","executor":null,"recorded_at":"2026-09-04T09:20:29Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000028","to":"proposed"},"payload":{"actor":"human:timo","at":1788514425078,"causation":"cmd-move-story-media-session-domain-1788514425078","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:media-session-domain@2#0~aa765dc73b434805","executor":null,"recorded_at":"2026-09-04T09:33:45Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000028","to":"active"},"payload":{"actor":"human:timo","at":1788514425238,"causation":"cmd-move-story-media-session-domain-1788514425238","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:media-session-domain@3#0~a1a5fe958050d6e5","executor":null,"recorded_at":"2026-09-04T09:33:45Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000028","to":"implemented"},"payload":{"actor":"human:timo","at":1788514425396,"causation":"cmd-move-story-media-session-domain-1788514425396","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:media-session-domain@4#0~05e596730f9aa03f","executor":null,"recorded_at":"2026-09-04T09:33:45Z"}}
{"entity":"story","version":1,"id":"call-history","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000019","to":"proposed"},"payload":{"actor":"human:timo","at":1788514425552,"causation":"cmd-move-story-call-history-1788514425552","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:call-history@2#0~f9a58fef9456570d","executor":null,"recorded_at":"2026-09-04T09:33:45Z"}}
{"entity":"story","version":1,"id":"call-history","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000019","to":"active"},"payload":{"actor":"human:timo","at":1788514425714,"causation":"cmd-move-story-call-history-1788514425714","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:call-history@3#0~2fcd169a7c16343d","executor":null,"recorded_at":"2026-09-04T09:33:45Z"}}
{"entity":"story","version":1,"id":"call-history","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000019","to":"implemented"},"payload":{"actor":"human:timo","at":1788514425871,"causation":"cmd-move-story-call-history-1788514425871","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:call-history@4#0~1b19f78d5effdf07","executor":null,"recorded_at":"2026-09-04T09:33:45Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000031","to":"proposed"},"payload":{"actor":"human:timo","at":1788514426027,"causation":"cmd-move-story-phonebook-1788514426027","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:phonebook@2#0~803970d9d588e9bb","executor":null,"recorded_at":"2026-09-04T09:33:46Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000031","to":"active"},"payload":{"actor":"human:timo","at":1788514426353,"causation":"cmd-move-story-phonebook-1788514426353","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:phonebook@3#0~76a71e9fd10ee953","executor":null,"recorded_at":"2026-09-04T09:33:46Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000031","to":"implemented"},"payload":{"actor":"human:timo","at":1788514426512,"causation":"cmd-move-story-phonebook-1788514426512","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:phonebook@4#0~23ef45bb74d2359d","executor":null,"recorded_at":"2026-09-04T09:33:46Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000025","to":"proposed"},"payload":{"actor":"human:timo","at":1788514426670,"causation":"cmd-move-story-layer-bridges-1788514426670","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:layer-bridges@2#0~89ec998c41945ae9","executor":null,"recorded_at":"2026-09-04T09:33:46Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000025","to":"active"},"payload":{"actor":"human:timo","at":1788514426827,"causation":"cmd-move-story-layer-bridges-1788514426827","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:layer-bridges@3#0~65306e9bbf6892e1","executor":null,"recorded_at":"2026-09-04T09:33:46Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000025","to":"implemented"},"payload":{"actor":"human:timo","at":1788514426984,"causation":"cmd-move-story-layer-bridges-1788514426984","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:layer-bridges@4#0~031dff866baffda3","executor":null,"recorded_at":"2026-09-04T09:33:46Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-system-level-type-breaks-synthesis","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# A system-level type validates and breaks every synthesis target\n\n## What was observed\n\n`system.yaml` may declare a `types:` block of its own — a type belonging to no domain, which\n`crates/specify/ess-domain/src/system.rs` checks only for being inside the system's namespace.\n`ess validate` and `ess compile` both accept one. All three synthesis targets then fail:\n\n```\n$ ess generate synthesize --path ess --target rust --out <dir>\n`softphone.BindingKind` is not a declaration this layout knows: it was derived from a different IR\nnote: run with `RUST_BACKTRACE=1` environment variable to display a backtrace\n```\n\n`--target go` and `--target web` fail identically. The four documentation and contract kinds are\nunaffected: `--kind docs` (11 artifacts), `--kind schema` (146), `--kind openapi` (7),\n`--kind asyncapi` (7) all succeed with the same document.\n\n## Isolated\n\nA copy of the specification with the one type moved back into a domain — nothing else changed —\nsynthesises: `231 capabilities: 168 generated, 52 obligation(s), 11 refused`, 30 artifacts. The\nsystem-level declaration is the sole cause.\n\nObserved at `ess 0.14.0` (`git+https://github.com/beyond10x/ess?tag=0.14.0#a25090533d2dcaf8e3de6736e7cced236571b563`).\n\n## What it cost here\n\n`softphone.media.BindingKind` enumerates the two bindings and lives in the one domain whose whole\nclaim is that it names none. System level is the right home for it and the reason the construct\nexists. It is declared in `domains/media.yaml` instead, with the refusal quoted beside it.\n\n## What would clear it\n\nThe synthesis layout resolving a type that belongs to no domain. Nothing in this project can.\n","title":"A system-level type validates and then breaks all three ESS synthesis targets"},"args":{"command":"create-entity","data":{"body":"# A system-level type validates and breaks every synthesis target\n\n## What was observed\n\n`system.yaml` may declare a `types:` block of its own — a type belonging to no domain, which\n`crates/specify/ess-domain/src/system.rs` checks only for being inside the system's namespace.\n`ess validate` and `ess compile` both accept one. All three synthesis targets then fail:\n\n```\n$ ess generate synthesize --path ess --target rust --out <dir>\n`softphone.BindingKind` is not a declaration this layout knows: it was derived from a different IR\nnote: run with `RUST_BACKTRACE=1` environment variable to display a backtrace\n```\n\n`--target go` and `--target web` fail identically. The four documentation and contract kinds are\nunaffected: `--kind docs` (11 artifacts), `--kind schema` (146), `--kind openapi` (7),\n`--kind asyncapi` (7) all succeed with the same document.\n\n## Isolated\n\nA copy of the specification with the one type moved back into a domain — nothing else changed —\nsynthesises: `231 capabilities: 168 generated, 52 obligation(s), 11 refused`, 30 artifacts. The\nsystem-level declaration is the sole cause.\n\nObserved at `ess 0.14.0` (`git+https://github.com/beyond10x/ess?tag=0.14.0#a25090533d2dcaf8e3de6736e7cced236571b563`).\n\n## What it cost here\n\n`softphone.media.BindingKind` enumerates the two bindings and lives in the one domain whose whole\nclaim is that it names none. System level is the right home for it and the reason the construct\nexists. It is declared in `domains/media.yaml` instead, with the refusal quoted beside it.\n\n## What would clear it\n\nThe synthesis layout resolving a type that belongs to no domain. Nothing in this project can.\n","status":"open","title":"A system-level type validates and then breaks all three ESS synthesis targets"},"entity_type":"aep.upstream-blocker/v1","locator":"ep://planning/store/upstream-blocker/ess-system-level-type-breaks-synthesis"},"payload":{"actor":"human:timo","at":1788518551676,"causation":"cmd-new-upstream-blocker-ess-system-level-type-breaks-synthesis","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"upstream-blocker:ess-system-level-type-breaks-synthesis@1#0~a4e48254fdc10686","executor":null,"recorded_at":"2026-09-04T10:42:31Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-system-level-type-breaks-synthesis","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:media-session-domain"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000097","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788518551683,"causation":"cmd-upstream-blocker-ess-system-level-type-breaks-synthesis-blocks-story-media-session-domain","change":{"change":"related","relation":"blocks","target":"story:media-session-domain"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-system-level-type-breaks-synthesis@1#0~e19ed081c4053fdf","executor":null,"recorded_at":"2026-09-04T10:42:31Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-mapping-does-not-widen-optional","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# A binding mapping widens into `Optional` at validate and not in the emitted code\n\n## What was observed\n\nA binding mapped an event field of type `T` into a command input of type `Optional<T>`:\n\n```yaml\n    when:  { event: softphone.control.CallHungUp }     # call_id: softphone.control.CallId\n    invoke: { command: softphone.history.RecordCall }  # call_id: Optional<softphone.control.CallId>\n    mapping:\n      call_id: event.call_id\n```\n\n`ess validate` accepts it. `ess generate synthesize --target rust` emits code that does not compile:\n\n```\nerror[E0308]: mismatched types\n   --> crates/softphone-system/src/lib.rs:268:18\n268 |         call_id: event.call_id.clone(),\n    |                  ^^^^^^^^^^^^^^^^^^^^^ expected `Option<CallId>`, found `CallId`\nhelp: try wrapping the expression in `Some`\n```\n\nThree occurrences, one per binding with that shape.\n\n## Which half is wrong is not established here\n\nEither the mapping is well-typed and the emitter should wrap it, or it is ill-typed and `validate`\nshould refuse it. Both are defensible and this project cannot tell which was intended. What is\ncertain is that the two disagree: a document passes every gate and produces a system that does not\nbuild.\n\nObserved at `ess 0.14.0`, commit `a25090533d2dcaf8e3de6736e7cced236571b563`.\n\n## Worked around\n\n`softphone.history.CallRecord.call_id` is now bare rather than `Optional`. That is a better model\nanyway — every record was made from a call — so the workaround cost nothing here. It would cost\nsomething in a model where the absence was real.\n","title":"A binding mapping widens into Optional at validate but not in the emitted Rust"},"args":{"command":"create-entity","data":{"body":"# A binding mapping widens into `Optional` at validate and not in the emitted code\n\n## What was observed\n\nA binding mapped an event field of type `T` into a command input of type `Optional<T>`:\n\n```yaml\n    when:  { event: softphone.control.CallHungUp }     # call_id: softphone.control.CallId\n    invoke: { command: softphone.history.RecordCall }  # call_id: Optional<softphone.control.CallId>\n    mapping:\n      call_id: event.call_id\n```\n\n`ess validate` accepts it. `ess generate synthesize --target rust` emits code that does not compile:\n\n```\nerror[E0308]: mismatched types\n   --> crates/softphone-system/src/lib.rs:268:18\n268 |         call_id: event.call_id.clone(),\n    |                  ^^^^^^^^^^^^^^^^^^^^^ expected `Option<CallId>`, found `CallId`\nhelp: try wrapping the expression in `Some`\n```\n\nThree occurrences, one per binding with that shape.\n\n## Which half is wrong is not established here\n\nEither the mapping is well-typed and the emitter should wrap it, or it is ill-typed and `validate`\nshould refuse it. Both are defensible and this project cannot tell which was intended. What is\ncertain is that the two disagree: a document passes every gate and produces a system that does not\nbuild.\n\nObserved at `ess 0.14.0`, commit `a25090533d2dcaf8e3de6736e7cced236571b563`.\n\n## Worked around\n\n`softphone.history.CallRecord.call_id` is now bare rather than `Optional`. That is a better model\nanyway — every record was made from a call — so the workaround cost nothing here. It would cost\nsomething in a model where the absence was real.\n","status":"open","title":"A binding mapping widens into Optional at validate but not in the emitted Rust"},"entity_type":"aep.upstream-blocker/v1","locator":"ep://planning/store/upstream-blocker/ess-mapping-does-not-widen-optional"},"payload":{"actor":"human:timo","at":1788518551841,"causation":"cmd-new-upstream-blocker-ess-mapping-does-not-widen-optional","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"upstream-blocker:ess-mapping-does-not-widen-optional@1#0~c5f8e57adac60471","executor":null,"recorded_at":"2026-09-04T10:42:31Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-mapping-does-not-widen-optional","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:call-history"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000102","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788518551850,"causation":"cmd-upstream-blocker-ess-mapping-does-not-widen-optional-blocks-story-call-history","change":{"change":"related","relation":"blocks","target":"story:call-history"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-mapping-does-not-widen-optional@1#0~951dc7862fb00e5c","executor":null,"recorded_at":"2026-09-04T10:42:31Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-web-target-redeliver","revision":2,"type":"aep.entity.update/v1","from_state":"open","to_state":"open","changed":{"body":"# The generated web target does not compile at ESS 0.13.1\n\n## What was observed\n\n`ess generate synthesize --path ess --target web` emits a tree that fails to build:\n\n```\ncrates/softphone-web/src/lib.rs:377:14\n    self.redeliver(&event)?;\n         ^^^^^^^^^ method not found in `&mut System<CallHistoryBehaviors, ..., ...>`\nerror: could not compile `softphone-web` (lib) due to 1 previous error\n```\n\n`redeliver` appears **nowhere** in the rust target the web crate depends on — `grep -rn redeliver`\nover all 30 emitted Rust artifacts returns nothing. The web emitter emits a call to a method the\nrust emitter does not generate.\n\n## It is not this specification\n\nThe rust target from the same specification builds clean: `cargo build --release` →\n`Finished release profile [optimized] target(s) in 1.02s`, all nine crates. Both targets report the\nsame 231 capabilities — 168 generated, 52 obligations, 11 refused — and web synthesis is\nbyte-identical across two runs. The specification validates: `softphone v1 — 9 file(s), valid`.\n\nSo the defect is in `ess generate synthesize --target web` at `0.13.1`\n(`git+https://github.com/beyond10x/ess?tag=0.13.1#d1a66772a91b5411d942d7a45bbf08dfc5de4651`), in the\n`replay` function of the emitted bridge.\n\n## What it blocks\n\n`story:softphone-page`'s acceptance is *the generated web surface drives a call before any page is\nhand-written*. That surface cannot be loaded, so no browser observation of this specification can be\nproduced at all — which also leaves the open question of whether the page persists anything across\na reload unanswerable.\n\n## What would clear it\n\nA fix upstream in `beyond10x/ess`: either the web emitter stops calling `redeliver`, or the rust\nemitter generates it. One line of the emitted bridge names the seam.\n\nFiled as an `upstream-blocker` rather than a `decision-blocker` because there is nothing here to\ndecide. `aep artifact kinds` does not list this kind by name — the `<type>-blocker` family is open\nand `aep artifact lifecycle upstream-blocker` answers `open -> cleared`.\n\n## What is not blocked\n\nEverything specification-side. All eight bridges are in the compiled IR, the five refusal codes were\nobserved, and the rust target builds — so the layering is verified without the page.\n\n## Version, corrected\n\nThe version this record was filed against is wrong twice over, and the defect outlived both.\n\nFiled as ESS `0.13.1`. The installed binary was `0.13.5` at the time of the reviews, and was\nreinstalled to **`0.14.0`** (`git+https://github.com/beyond10x/ess?tag=0.14.0#a25090533d2dcaf8e3de6736e7cced236571b563`)\npartway through the work. The `redeliver` defect is present at 0.14.0: `redeliver` appears 12 times\nin the web output and 0 times in the rust output from the same specification, and the rust target\nbuilds clean.\n\nThe model source is byte-identical across 0.13.1 and 0.13.5 (`diff -rq` over\n`crates/specify/ess-domain/src` and `docs/`), so nothing read from either tree is invalidated —\nonly the version number in this record was.\n"},"args":{"changes":{"body":"# The generated web target does not compile at ESS 0.13.1\n\n## What was observed\n\n`ess generate synthesize --path ess --target web` emits a tree that fails to build:\n\n```\ncrates/softphone-web/src/lib.rs:377:14\n    self.redeliver(&event)?;\n         ^^^^^^^^^ method not found in `&mut System<CallHistoryBehaviors, ..., ...>`\nerror: could not compile `softphone-web` (lib) due to 1 previous error\n```\n\n`redeliver` appears **nowhere** in the rust target the web crate depends on — `grep -rn redeliver`\nover all 30 emitted Rust artifacts returns nothing. The web emitter emits a call to a method the\nrust emitter does not generate.\n\n## It is not this specification\n\nThe rust target from the same specification builds clean: `cargo build --release` →\n`Finished release profile [optimized] target(s) in 1.02s`, all nine crates. Both targets report the\nsame 231 capabilities — 168 generated, 52 obligations, 11 refused — and web synthesis is\nbyte-identical across two runs. The specification validates: `softphone v1 — 9 file(s), valid`.\n\nSo the defect is in `ess generate synthesize --target web` at `0.13.1`\n(`git+https://github.com/beyond10x/ess?tag=0.13.1#d1a66772a91b5411d942d7a45bbf08dfc5de4651`), in the\n`replay` function of the emitted bridge.\n\n## What it blocks\n\n`story:softphone-page`'s acceptance is *the generated web surface drives a call before any page is\nhand-written*. That surface cannot be loaded, so no browser observation of this specification can be\nproduced at all — which also leaves the open question of whether the page persists anything across\na reload unanswerable.\n\n## What would clear it\n\nA fix upstream in `beyond10x/ess`: either the web emitter stops calling `redeliver`, or the rust\nemitter generates it. One line of the emitted bridge names the seam.\n\nFiled as an `upstream-blocker` rather than a `decision-blocker` because there is nothing here to\ndecide. `aep artifact kinds` does not list this kind by name — the `<type>-blocker` family is open\nand `aep artifact lifecycle upstream-blocker` answers `open -> cleared`.\n\n## What is not blocked\n\nEverything specification-side. All eight bridges are in the compiled IR, the five refusal codes were\nobserved, and the rust target builds — so the layering is verified without the page.\n\n## Version, corrected\n\nThe version this record was filed against is wrong twice over, and the defect outlived both.\n\nFiled as ESS `0.13.1`. The installed binary was `0.13.5` at the time of the reviews, and was\nreinstalled to **`0.14.0`** (`git+https://github.com/beyond10x/ess?tag=0.14.0#a25090533d2dcaf8e3de6736e7cced236571b563`)\npartway through the work. The `redeliver` defect is present at 0.14.0: `redeliver` appears 12 times\nin the web output and 0 times in the rust output from the same specification, and the rust target\nbuilds clean.\n\nThe model source is byte-identical across 0.13.1 and 0.13.5 (`diff -rq` over\n`crates/specify/ess-domain/src` and `docs/`), so nothing read from either tree is invalidated —\nonly the version number in this record was.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788518557713,"causation":"cmd-protocol-artifact-body-append-upstream-blocker-ess-web-target-redeliver-1788518557713","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"upstream-blocker:ess-web-target-redeliver@2#0~67817c3671cb8384","executor":null,"recorded_at":"2026-09-04T10:42:37Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-session-owns-nothing","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"proposed","changed":{"body":"# The media session owns nothing\n\n## Status\n\nAccepted 2026-09-04. Amends `architecture-decision-record:layered-softphone-domains`, which recorded\nbridges 2 and 3 as `owns` relations on `softphone.media.MediaSession`.\n\n## Context\n\n`softphone.media` claimed, in its own header comment and in the acceptance statement of\n`story:media-session-domain`, that nothing in it names a binding. It named two:\n`BindingKind`'s variants are `Sip` and `Loopback`, and the two `owns` relations targeted\n`softphone.sip.SipDialog` and `softphone.local.LoopbackDevice`. An independent review found the\ngenerated documentation contradicting itself inside one file — `domains/softphone-media.md` printed\n\"no binding type appears here\" and, eighty lines later, \"It owns at most one\n`softphone.sip.SipDialog`\".\n\n## Decision\n\nThe two relations move to the binding side and become `references`:\n\n| | before | after |\n|---|---|---|\n| bridge 2 | `media.MediaSession owns sip.SipDialog` | `sip.SipDialog references media.MediaSession` |\n| bridge 3 | `media.MediaSession owns local.LoopbackDevice` | `local.LoopbackDevice references media.MediaSession` |\n\nThe carrier field was already on the far side with the correct type, so this is a one-way edit and\n`ess validate` accepts both spellings.\n\n`BindingKind` stays in `softphone.media`. System level is its right home and the reason a\nsystem-level `types:` block exists, and it is not usable:\n`upstream-blocker:ess-system-level-type-breaks-synthesis`.\n\n## What this costs\n\n`owns` says the far side cannot outlive its owner. That is the whole content of the kind, per the\nrelations design §5, and `references` does not say it. So the guarantee that a SIP dialog cannot\nsurvive the session it carries is no longer a rule.\n\n**Two bindings hold it instead**, and being bindings they are visible in the interaction graph rather\nthan implied by a relation kind:\n\n- `end-session-with-dialog` — `sip.SipDialogClosed` causes `media.TerminateSession`\n- `end-session-with-loopback` — `local.LoopbackDetached` causes `media.TerminateSession`\n\nThat is weaker in one specific way worth stating: a binding is `at_least_once` with `on_failure:\nretry`, so it can be late, where a rule could not be. Nothing implemented a cascade under `owns`\neither, so no behaviour was lost — but a reader who trusted the word `owns` was trusting something\nthe model was not going to enforce.\n\n## Consequence\n\nThe claim is now mechanical rather than asserted: `ess compile --format json`, then search\n`domains.\"softphone.media\"` for `softphone.sip.*` or `softphone.local.*`, returns nothing. That check\nis the acceptance statement of `story:media-session-domain`, replacing a `grep` over the source that\nreturned five comment lines.\n","summary":"Bridges 2 and 3 move to the binding side as references; two bindings hold what owns claimed.","title":"The media session owns nothing"},"args":{"command":"create-entity","data":{"body":"# The media session owns nothing\n\n## Status\n\nAccepted 2026-09-04. Amends `architecture-decision-record:layered-softphone-domains`, which recorded\nbridges 2 and 3 as `owns` relations on `softphone.media.MediaSession`.\n\n## Context\n\n`softphone.media` claimed, in its own header comment and in the acceptance statement of\n`story:media-session-domain`, that nothing in it names a binding. It named two:\n`BindingKind`'s variants are `Sip` and `Loopback`, and the two `owns` relations targeted\n`softphone.sip.SipDialog` and `softphone.local.LoopbackDevice`. An independent review found the\ngenerated documentation contradicting itself inside one file — `domains/softphone-media.md` printed\n\"no binding type appears here\" and, eighty lines later, \"It owns at most one\n`softphone.sip.SipDialog`\".\n\n## Decision\n\nThe two relations move to the binding side and become `references`:\n\n| | before | after |\n|---|---|---|\n| bridge 2 | `media.MediaSession owns sip.SipDialog` | `sip.SipDialog references media.MediaSession` |\n| bridge 3 | `media.MediaSession owns local.LoopbackDevice` | `local.LoopbackDevice references media.MediaSession` |\n\nThe carrier field was already on the far side with the correct type, so this is a one-way edit and\n`ess validate` accepts both spellings.\n\n`BindingKind` stays in `softphone.media`. System level is its right home and the reason a\nsystem-level `types:` block exists, and it is not usable:\n`upstream-blocker:ess-system-level-type-breaks-synthesis`.\n\n## What this costs\n\n`owns` says the far side cannot outlive its owner. That is the whole content of the kind, per the\nrelations design §5, and `references` does not say it. So the guarantee that a SIP dialog cannot\nsurvive the session it carries is no longer a rule.\n\n**Two bindings hold it instead**, and being bindings they are visible in the interaction graph rather\nthan implied by a relation kind:\n\n- `end-session-with-dialog` — `sip.SipDialogClosed` causes `media.TerminateSession`\n- `end-session-with-loopback` — `local.LoopbackDetached` causes `media.TerminateSession`\n\nThat is weaker in one specific way worth stating: a binding is `at_least_once` with `on_failure:\nretry`, so it can be late, where a rule could not be. Nothing implemented a cascade under `owns`\neither, so no behaviour was lost — but a reader who trusted the word `owns` was trusting something\nthe model was not going to enforce.\n\n## Consequence\n\nThe claim is now mechanical rather than asserted: `ess compile --format json`, then search\n`domains.\"softphone.media\"` for `softphone.sip.*` or `softphone.local.*`, returns nothing. That check\nis the acceptance statement of `story:media-session-domain`, replacing a `grep` over the source that\nreturned five comment lines.\n","status":"proposed","summary":"Bridges 2 and 3 move to the binding side as references; two bindings hold what owns claimed.","title":"The media session owns nothing"},"entity_type":"aep.architecture-decision-record/v1","locator":"ep://planning/store/architecture-decision-record/media-session-owns-nothing"},"payload":{"actor":"human:timo","at":1788518578383,"causation":"cmd-new-architecture-decision-record-media-session-owns-nothing","change":{"change":"created","status":"proposed"},"correlation":"protocol-artifact-new","event_id":"architecture-decision-record:media-session-owns-nothing@1#0~d961adf2950965e5","executor":null,"recorded_at":"2026-09-04T10:42:58Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-session-owns-nothing","revision":1,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decides":"story:layer-bridges"}]},"args":{"command":"create-relation","kind":"decides","source":"01MEM0000000000000107","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788518578400,"causation":"cmd-architecture-decision-record-media-session-owns-nothing-decides-story-layer-bridges","change":{"change":"related","relation":"decides","target":"story:layer-bridges"},"correlation":"protocol-artifact-relate","event_id":"architecture-decision-record:media-session-owns-nothing@1#0~ba5cbcbcf70c20b9","executor":null,"recorded_at":"2026-09-04T10:42:58Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-session-owns-nothing","revision":1,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decides":"story:layer-bridges"},{"supersedes":"architecture-decision-record:layered-softphone-domains"}]},"args":{"command":"create-relation","kind":"supersedes","source":"01MEM0000000000000107","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788518578408,"causation":"cmd-architecture-decision-record-media-session-owns-nothing-supersedes-architecture-decision-record-layered-softphone-domains","change":{"change":"related","relation":"supersedes","target":"architecture-decision-record:layered-softphone-domains"},"correlation":"protocol-artifact-relate","event_id":"architecture-decision-record:media-session-owns-nothing@1#0~1cac74a7c696714b","executor":null,"recorded_at":"2026-09-04T10:42:58Z"}}
{"entity":"architecture-decision-record","version":1,"id":"media-session-owns-nothing","revision":2,"type":"aep.status.move/v1","from_state":"proposed","to_state":"accepted","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000007","to":"accepted"},"payload":{"actor":"human:timo","at":1788518578567,"causation":"cmd-move-architecture-decision-record-media-session-owns-nothing-1788518578567","change":{"change":"moved","decided_on":{},"from":"proposed","to":"accepted"},"correlation":"protocol-artifact-move","event_id":"architecture-decision-record:media-session-owns-nothing@2#0~3186f0c4a10735ce","executor":null,"recorded_at":"2026-09-04T10:42:58Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.sip.Register` through to `Registered` or `Failed` — and back out of `Failed`, which\nit could not do before: that state was terminal, so one transient WSS drop killed the registration\npermanently.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\nThis story is now scoped to `softphone.sip` — `ess/domains/sip.yaml` — and nothing else. Registration\nand the dialog live there; the call lifecycle moved to `softphone.control` and the media session to\n`softphone.media`.\n\n- `Registration` — `aor`, `transport`, four states, four commands. `SignallingTransport` has one\n  member, `SecureWebSocket`, because a browser can open no other and the page is served over HTTPS.\n- `SipDialog` — `session_id` typed `softphone.media.MediaSessionId`, which is the carrier of bridge 2.\n  `OpenDialog` is the only outcome that creates one, and that is what holds the binding\n  discriminator on `MediaSession` honest, since no invariant can.\n- `MediaSecurity` has one member, `DtlsSrtp`: the browser negotiates media and that is the only\n  keying `RTCPeerConnection` offers.\n- `SipCause` stays text. Inventing a closed set of SIP causes here would be inventing the protocol's\n  vocabulary; the call log records `softphone.media.TerminationReason` instead.\n\nImplementation, unchanged from before: the vendored `browser/src/` binding from the pinned sipx\nrevision, the WASM module from `crates/sipx-wasm` and `wasm/`, and the hand-written ABI glue that\n`A-17` has not generated.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"args":{"changes":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.sip.Register` through to `Registered` or `Failed` — and back out of `Failed`, which\nit could not do before: that state was terminal, so one transient WSS drop killed the registration\npermanently.\n\n## Acceptance\n\nLoading the page with a configured address-of-record and WSS URL produces a\n`softphone.session.RegistrationConfirmed` fact, and the `softphone.session.RegistrationById` view\nreports `Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop.\n\n## Scope\n\nThis story is now scoped to `softphone.sip` — `ess/domains/sip.yaml` — and nothing else. Registration\nand the dialog live there; the call lifecycle moved to `softphone.control` and the media session to\n`softphone.media`.\n\n- `Registration` — `aor`, `transport`, four states, four commands. `SignallingTransport` has one\n  member, `SecureWebSocket`, because a browser can open no other and the page is served over HTTPS.\n- `SipDialog` — `session_id` typed `softphone.media.MediaSessionId`, which is the carrier of bridge 2.\n  `OpenDialog` is the only outcome that creates one, and that is what holds the binding\n  discriminator on `MediaSession` honest, since no invariant can.\n- `MediaSecurity` has one member, `DtlsSrtp`: the browser negotiates media and that is the only\n  keying `RTCPeerConnection` offers.\n- `SipCause` stays text. Inventing a closed set of SIP causes here would be inventing the protocol's\n  vocabulary; the call log records `softphone.media.TerminationReason` instead.\n\nImplementation, unchanged from before: the vendored `browser/src/` binding from the pinned sipx\nrevision, the WASM module from `crates/sipx-wasm` and `wasm/`, and the hand-written ABI glue that\n`A-17` has not generated.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788518621917,"causation":"cmd-protocol-artifact-body-section-story-signalling-over-wss-1788518621917","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:signalling-over-wss@4#0~3e7c2c28c29f83f8","executor":null,"recorded_at":"2026-09-04T10:43:41Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.sip.Register` through to `Registered` or `Failed` — and back out of `Failed`, which\nit could not do before: that state was terminal, so one transient WSS drop killed the registration\npermanently.\n\n## Acceptance\n\nLoading the page with a configured address-of-record, a WSS URL and an expiry produces a\n`softphone.sip.RegistrationConfirmed` fact, and `softphone.sip.RegistrationById` reports\n`Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop —\n`RetryRegistration` is a command somebody issues, not a loop the model implies. `RefreshRegistration`\nextends the lifetime without a state change.\n\n## Scope\n\nThis story is now scoped to `softphone.sip` — `ess/domains/sip.yaml` — and nothing else. Registration\nand the dialog live there; the call lifecycle moved to `softphone.control` and the media session to\n`softphone.media`.\n\n- `Registration` — `aor`, `transport`, four states, four commands. `SignallingTransport` has one\n  member, `SecureWebSocket`, because a browser can open no other and the page is served over HTTPS.\n- `SipDialog` — `session_id` typed `softphone.media.MediaSessionId`, which is the carrier of bridge 2.\n  `OpenDialog` is the only outcome that creates one, and that is what holds the binding\n  discriminator on `MediaSession` honest, since no invariant can.\n- `MediaSecurity` has one member, `DtlsSrtp`: the browser negotiates media and that is the only\n  keying `RTCPeerConnection` offers.\n- `SipCause` stays text. Inventing a closed set of SIP causes here would be inventing the protocol's\n  vocabulary; the call log records `softphone.media.TerminationReason` instead.\n\nImplementation, unchanged from before: the vendored `browser/src/` binding from the pinned sipx\nrevision, the WASM module from `crates/sipx-wasm` and `wasm/`, and the hand-written ABI glue that\n`A-17` has not generated.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"args":{"changes":{"body":"# Register over secure WebSocket\n\n## Outcome\n\nThe page loads the sipx WebAssembly session kernel, opens a WSS connection to the SIP provider, and\ndrives `softphone.sip.Register` through to `Registered` or `Failed` — and back out of `Failed`, which\nit could not do before: that state was terminal, so one transient WSS drop killed the registration\npermanently.\n\n## Acceptance\n\nLoading the page with a configured address-of-record, a WSS URL and an expiry produces a\n`softphone.sip.RegistrationConfirmed` fact, and `softphone.sip.RegistrationById` reports\n`Registered`. A rejected REGISTER produces `RegistrationFailed` and no retry loop —\n`RetryRegistration` is a command somebody issues, not a loop the model implies. `RefreshRegistration`\nextends the lifetime without a state change.\n\n## Scope\n\nThis story is now scoped to `softphone.sip` — `ess/domains/sip.yaml` — and nothing else. Registration\nand the dialog live there; the call lifecycle moved to `softphone.control` and the media session to\n`softphone.media`.\n\n- `Registration` — `aor`, `transport`, four states, four commands. `SignallingTransport` has one\n  member, `SecureWebSocket`, because a browser can open no other and the page is served over HTTPS.\n- `SipDialog` — `session_id` typed `softphone.media.MediaSessionId`, which is the carrier of bridge 2.\n  `OpenDialog` is the only outcome that creates one, and that is what holds the binding\n  discriminator on `MediaSession` honest, since no invariant can.\n- `MediaSecurity` has one member, `DtlsSrtp`: the browser negotiates media and that is the only\n  keying `RTCPeerConnection` offers.\n- `SipCause` stays text. Inventing a closed set of SIP causes here would be inventing the protocol's\n  vocabulary; the call log records `softphone.media.TerminationReason` instead.\n\nImplementation, unchanged from before: the vendored `browser/src/` binding from the pinned sipx\nrevision, the WASM module from `crates/sipx-wasm` and `wasm/`, and the hand-written ABI glue that\n`A-17` has not generated.\n\n## Cited from\n\n- `browser/README.md` — the binding's four platform facilities and its constructor-injected socket,\n  clock, entropy and connectivity monitor.\n- `docs/specs/browser-signalling.md` — the contract the binding implements.\n- `docs/specs/browser-sdk.md` §4 — the kernel ABI.\n\n## Authentication\n\nA browser cannot set an `Authorization` header on a WebSocket upgrade. The access token rides the\nnegotiated WebSocket subprotocol list, the way\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §4 records for the console's\nRTVBP phone. Whether the SIP provider's WSS endpoint accepts a token that way, or wants SIP digest\nauthentication in the REGISTER instead, is not established here and is the first thing to check\nagainst the chosen provider.\n\nOrigin allowlisting and a TLS endpoint reachable from the browser are the deployment's, not this\nstory's.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788518622013,"causation":"cmd-protocol-artifact-body-section-story-signalling-over-wss-1788518622013","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:signalling-over-wss@5#0~24a53a1b94ca55d6","executor":null,"recorded_at":"2026-09-04T10:43:42Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.control.FailCall` with\n`EndCause::Media` rather than into a SIP cause.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nWhat changed with the layering: the adapter no longer implements a domain. It **fills obligations**.\n`softphone.media` states the session and its signals; `softphone.sip` states the dialog; web\nsynthesis emits both as unfilled command behaviours answering with a typed refusal naming what is\nowed. This story fills the SIP binding's share of them with a real `RTCPeerConnection`.\n\nThe neutral port is what the adapter satisfies, not a shape of its own: `MediaProfile` is\n`pcm_s16le`/8000/1/20 ms/320 B, signals are DTMF only, and a session ends with one of the eight\n`softphone.media.TerminationReason` variants — so a media failure arrives as `MediaFailed` and never\nas a SIP cause.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"args":{"changes":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.control.FailCall` with\n`EndCause::Media` rather than into a SIP cause.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call as `MediaFailed` and never presents it as connected.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nWhat changed with the layering: the adapter no longer implements a domain. It **fills obligations**.\n`softphone.media` states the session and its signals; `softphone.sip` states the dialog; web\nsynthesis emits both as unfilled command behaviours answering with a typed refusal naming what is\nowed. This story fills the SIP binding's share of them with a real `RTCPeerConnection`.\n\nThe neutral port is what the adapter satisfies, not a shape of its own: `MediaProfile` is\n`pcm_s16le`/8000/1/20 ms/320 B, signals are DTMF only, and a session ends with one of the eight\n`softphone.media.TerminationReason` variants — so a media failure arrives as `MediaFailed` and never\nas a SIP cause.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788518622102,"causation":"cmd-protocol-artifact-body-section-story-browser-media-adapter-1788518622102","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:browser-media-adapter@5#0~a9a8898ecd6203d8","executor":null,"recorded_at":"2026-09-04T10:43:42Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.control.FailCall` with\n`EndCause::Media` rather than into a SIP cause.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call with `softphone.control.EndCause::Media` and never presents it as\nconnected — and the model now holds that rather than hoping for it: a call reaches `Active` only\nthrough `MediaConnected`, which only `Kernel` may issue, so `Answering` is where a call whose media\nfailed stops.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nWhat changed with the layering: the adapter no longer implements a domain. It **fills obligations**.\n`softphone.media` states the session and its signals; `softphone.sip` states the dialog; web\nsynthesis emits both as unfilled command behaviours answering with a typed refusal naming what is\nowed. This story fills the SIP binding's share of them with a real `RTCPeerConnection`.\n\nThe neutral port is what the adapter satisfies, not a shape of its own: `MediaProfile` is\n`pcm_s16le`/8000/1/20 ms/320 B, signals are DTMF only, and a session ends with one of the eight\n`softphone.media.TerminationReason` variants — so a media failure arrives as `MediaFailed` and never\nas a SIP cause.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"args":{"changes":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.control.FailCall` with\n`EndCause::Media` rather than into a SIP cause.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call with `softphone.control.EndCause::Media` and never presents it as\nconnected — and the model now holds that rather than hoping for it: a call reaches `Active` only\nthrough `MediaConnected`, which only `Kernel` may issue, so `Answering` is where a call whose media\nfailed stops.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nWhat changed with the layering: the adapter no longer implements a domain. It **fills obligations**.\n`softphone.media` states the session and its signals; `softphone.sip` states the dialog; web\nsynthesis emits both as unfilled command behaviours answering with a typed refusal naming what is\nowed. This story fills the SIP binding's share of them with a real `RTCPeerConnection`.\n\nThe neutral port is what the adapter satisfies, not a shape of its own: `MediaProfile` is\n`pcm_s16le`/8000/1/20 ms/320 B, signals are DTMF only, and a session ends with one of the eight\n`softphone.media.TerminationReason` variants — so a media failure arrives as `MediaFailed` and never\nas a SIP cause.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788518622189,"causation":"cmd-protocol-artifact-body-section-story-browser-media-adapter-1788518622189","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:browser-media-adapter@6#0~dc7997abbb63f6d6","executor":null,"recorded_at":"2026-09-04T10:43:42Z"}}
{"entity":"story","version":1,"id":"browser-media-adapter","revision":7,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.control.FailCall` with\n`EndCause::Media` rather than into a SIP cause.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call with `softphone.control.EndCause::Media` and never presents it as\nconnected — and the model now holds that rather than hoping for it: a call reaches `Active` only\nthrough `MediaConnected`, which only `Kernel` may issue, so `Answering` is where a call whose media\nfailed stops.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nThe adapter does not implement a domain. It **fills obligations** — 68 of them across the\nspecification, 52 command behaviours and 16 view queries, listed on the generated *Obligations* page.\nThis story fills the SIP binding's share with a real `RTCPeerConnection`.\n\nThe neutral port is what it satisfies: `MediaProfile` is `pcm_s16le`/8000/1/20 ms/320 B and now says\nso as four checked invariants rather than as a comment; signals are DTMF only; and a call ends with\none of the six `softphone.control.EndCause` words, none of which is SIP's.\n\nThe offer/answer exchange it drives lives in `softphone.sip` as four commands —\n`RequestLocalMedia`, `OfferLocalMedia`, `ApplyRemoteMedia`, `FailLocalMedia` — and a dialog carrying\n`local_sdp` and `remote_sdp`. None of that existed when this story was first written.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"args":{"changes":{"body":"# Adapt browser-native audio\n\n## Outcome\n\nAn audio-only `RTCPeerConnection` adapter turns the kernel's negotiated-media report into a live\nmedia session, and turns browser media failures into `softphone.control.FailCall` with\n`EndCause::Media` rather than into a SIP cause.\n\n## Acceptance\n\nAn established call carries non-silent audio in both directions in both SIP roles. A denied\nmicrophone permission ends the call with `softphone.control.EndCause::Media` and never presents it as\nconnected — and the model now holds that rather than hoping for it: a call reaches `Active` only\nthrough `MediaConnected`, which only `Kernel` may issue, so `Answering` is where a call whose media\nfailed stops.\n\n## Scope\n\nThis is upstream story `M-52`, unshipped at the pinned revision. Written here against\n`docs/specs/browser-sdk.md` §5.4, §6.2 and §8.2/§8.5, which are normative and complete enough to\nimplement without the package. Where it lands is settled:\n`architecture-decision-record:media-adapter-lands-here`.\n\nThe adapter does not implement a domain. It **fills obligations** — 68 of them across the\nspecification, 52 command behaviours and 16 view queries, listed on the generated *Obligations* page.\nThis story fills the SIP binding's share with a real `RTCPeerConnection`.\n\nThe neutral port is what it satisfies: `MediaProfile` is `pcm_s16le`/8000/1/20 ms/320 B and now says\nso as four checked invariants rather than as a comment; signals are DTMF only; and a call ends with\none of the six `softphone.control.EndCause` words, none of which is SIP's.\n\nThe offer/answer exchange it drives lives in `softphone.sip` as four commands —\n`RequestLocalMedia`, `OfferLocalMedia`, `ApplyRemoteMedia`, `FailLocalMedia` — and a dialog carrying\n`local_sdp` and `remote_sdp`. None of that existed when this story was first written.\n\n## Cited from\n\n- `docs/specs/browser-sdk.md:854` — \"§5.4 media flow over `RTCPeerConnection`, §6.2 established\n  gate, §8.2/§8.5 at the media edge | browser media adapter | `M-52`\".\n- `docs/specs/browser-sdk.md:539` — `SipxMediaError` kinds: permission, device, autoplay,\n  negotiation, track-ended; \"browser media failures never masquerade as SIP failures\".\n- `docs/specs/browser-signalling.md:215` — \"Anything to do with media. `RTCPeerConnection` is\n  `M-52`'s.\"\n- `README.md` — \"The browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render\"; no TURN\n  relay, host and STUN-derived ICE candidates only.\n\n## Teardown\n\nMicrophone denial, a missing media API, negotiation failure, connection loss and a remote BYE all\nconverge on one idempotent teardown: stop source tracks, disconnect the graph, close the\n`AudioContext`, clear queued playback, and ignore callbacks arriving from a superseded connection\ngeneration.\n\nRead from `~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` §5, which is the\nimplemented version of this for RTVBP, and whose §7 records that jitter behaviour is bounded by a ring\nbuffer policy rather than solved.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788518622276,"causation":"cmd-protocol-artifact-body-section-story-browser-media-adapter-1788518622276","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:browser-media-adapter@7#0~36ede909d14f5d02","executor":null,"recorded_at":"2026-09-04T10:43:42Z"}}
{"entity":"story","version":1,"id":"call-lifecycle","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"summary":"Wire the softphone.control commands to kernel events in both directions."},"args":{"changes":{"summary":"Wire the softphone.control commands to kernel events in both directions."},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788518635819,"causation":"cmd-protocol-artifact-set-story-call-lifecycle-1788518635819","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:call-lifecycle@3#0~a66aae27d7611d50","executor":null,"recorded_at":"2026-09-04T10:43:55Z"}}
{"entity":"story","version":1,"id":"media-session-domain","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Model the neutral media session\n\n## Outcome\n\n`softphone.media` states what a media session is without naming a protocol, and `softphone.local`\nexists as a second carrier so that claim has evidence behind it.\n\n## Acceptance\n\n`ess validate --path ess` accepts the domain. **No construct in `softphone.media` references a\nbinding**, checked against the compiled IR rather than the source: `ess compile --format json`, then\nsearch `domains.\"softphone.media\"` for `softphone.sip.*` or `softphone.local.*` — it returns nothing.\n\nThat replaces the acceptance this story shipped with, which was a `grep` over the YAML for `Sip` and\na claim that it returned empty. It returned five lines. Two of them were the header comment asserting\nthe very thing, and the other three were an `owns` relation naming `softphone.sip.SipDialog` and the\nprose explaining it. The relations moved to the binding side —\n`architecture-decision-record:media-session-owns-nothing` — and the claim is now narrower and\nmechanical.\n\nTwo bindings own a `MediaSession` reference, not one, so the neutrality has a second carrier behind\nit. `MediaProfile`'s four numbers are checked invariants rather than a comment.\n\n## What was built\n\n`ess/domains/media.yaml`, modelled construct-for-type from the Rust port that already existed:\n`connectors/crates/domain/src/voice.rs`.\n\n| ESS construct | Read from |\n|---|---|\n| `MediaProfile` | `MediaDescriptor` (`:68`); the single accepted value `pcm_s16le`/8000/1/20 ms/320 B (`:78-95`), so `SampleFormat` has one member |\n| `ChannelSignal` + `ChannelSignalKind` | `ChannelSignal::Dtmf { digits }` (`:114-135`); split in two because an ESS enum carries no fields |\n| `TerminationReason` | `:137-147`, all eight variants unchanged |\n| `Participant`, `ContextTrust` | `ParticipantContext` (`:57`), `ContextTrust::Untrusted` (`:49`) |\n| `SessionRef` | `VoiceRef` (`:33`); opacity modelled, the 128-byte cap left to the host |\n| the six commands | one per `TelephonySession` method that is not the frame path (`:163-229`) |\n\n`softphone.local` is `ess/domains/local.yaml`, cited from `connectors/crates/voice-local-audio` —\nthe sibling of `driver-sip` behind the same port.\n\n## One deliberate difference from the port it was read from\n\nUpstream, establishment happens *before* a neutral session exists: `RuntimeError` is documented as\n\"Failure before a neutral telephony session exists\"\n(`connectors/crates/voice-runtime/src/lib.rs:307-309`). Here it is the `Requested -> Live` move, so\nthe ESS session starts one state earlier than the Rust port's. Written into the domain `summary` so\nthe next reader does not find a contradiction and assume one side is wrong.\n\n## The limit that could not be modelled\n\nA `MediaSession` owns a `SipDialog` and a `LoopbackDevice`, one each, and at runtime exactly one\nexists. ESS cannot state that: `crates/specify/ess-domain/src/entity.rs:821-822` says an invariant\nreads only its own entity's fields, identity and `state`, so a predicate over a related entity's\nexistence is refused. `binding: BindingKind` is the discriminator and the two `creates` outcomes —\n`softphone.sip.OpenDialog` and `softphone.local.AttachLoopback` — are what hold it. No invariant\npretends otherwise.\n"},"args":{"changes":{"body":"# Model the neutral media session\n\n## Outcome\n\n`softphone.media` states what a media session is without naming a protocol, and `softphone.local`\nexists as a second carrier so that claim has evidence behind it.\n\n## Acceptance\n\n`ess validate --path ess` accepts the domain. **No construct in `softphone.media` references a\nbinding**, checked against the compiled IR rather than the source: `ess compile --format json`, then\nsearch `domains.\"softphone.media\"` for `softphone.sip.*` or `softphone.local.*` — it returns nothing.\n\nThat replaces the acceptance this story shipped with, which was a `grep` over the YAML for `Sip` and\na claim that it returned empty. It returned five lines. Two of them were the header comment asserting\nthe very thing, and the other three were an `owns` relation naming `softphone.sip.SipDialog` and the\nprose explaining it. The relations moved to the binding side —\n`architecture-decision-record:media-session-owns-nothing` — and the claim is now narrower and\nmechanical.\n\nTwo bindings own a `MediaSession` reference, not one, so the neutrality has a second carrier behind\nit. `MediaProfile`'s four numbers are checked invariants rather than a comment.\n\n## What was built\n\n`ess/domains/media.yaml`, modelled construct-for-type from the Rust port that already existed:\n`connectors/crates/domain/src/voice.rs`.\n\n| ESS construct | Read from |\n|---|---|\n| `MediaProfile` | `MediaDescriptor` (`:68`); the single accepted value `pcm_s16le`/8000/1/20 ms/320 B (`:78-95`), so `SampleFormat` has one member |\n| `ChannelSignal` + `ChannelSignalKind` | `ChannelSignal::Dtmf { digits }` (`:114-135`); split in two because an ESS enum carries no fields |\n| `TerminationReason` | `:137-147`, all eight variants unchanged |\n| `Participant`, `ContextTrust` | `ParticipantContext` (`:57`), `ContextTrust::Untrusted` (`:49`) |\n| `SessionRef` | `VoiceRef` (`:33`); opacity modelled, the 128-byte cap left to the host |\n| the six commands | one per `TelephonySession` method that is not the frame path (`:163-229`) |\n\n`softphone.local` is `ess/domains/local.yaml`, cited from `connectors/crates/voice-local-audio` —\nthe sibling of `driver-sip` behind the same port.\n\n## One deliberate difference from the port it was read from\n\nUpstream, establishment happens *before* a neutral session exists: `RuntimeError` is documented as\n\"Failure before a neutral telephony session exists\"\n(`connectors/crates/voice-runtime/src/lib.rs:307-309`). Here it is the `Requested -> Live` move, so\nthe ESS session starts one state earlier than the Rust port's. Written into the domain `summary` so\nthe next reader does not find a contradiction and assume one side is wrong.\n\n## The limit that could not be modelled\n\nA `MediaSession` owns a `SipDialog` and a `LoopbackDevice`, one each, and at runtime exactly one\nexists. ESS cannot state that: `crates/specify/ess-domain/src/entity.rs:821-822` says an invariant\nreads only its own entity's fields, identity and `state`, so a predicate over a related entity's\nexistence is refused. `binding: BindingKind` is the discriminator and the two `creates` outcomes —\n`softphone.sip.OpenDialog` and `softphone.local.AttachLoopback` — are what hold it. No invariant\npretends otherwise.\n"},"command":"update-entity","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788518670280,"causation":"cmd-protocol-artifact-body-section-story-media-session-domain-1788518670280","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:media-session-domain@5#0~20aeb0588d7b14b1","executor":null,"recorded_at":"2026-09-04T10:44:30Z"}}
{"entity":"story","version":1,"id":"call-history","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Record the calls that finished\n\n## Outcome\n\n`softphone.history` holds one durable record per finished call, in the neutral termination\nvocabulary, optionally attributed to a contact.\n\n## Acceptance\n\n`ess validate` accepts the domain. `CallRecord.termination` is `softphone.control.EndCause` — six\nwords, none of them SIP's. A record survives its call: the relation to `softphone.control.Call` is\n`references`, so nothing in the model lets a call take its record with it. There is no bulk-erase\ncommand. `ended_at >= started_at` is a checked invariant.\n\n**A missed call is derivable**, which it was not when this story shipped: `answered_at` is `Optional`\nand `status` carries the SIP response where the far end sent one, so a call nobody took, a call the\nuser declined and a 486 from the far end are three different rows. Before, all three recorded\nidentically and this story's acceptance did not notice.\n\nThree bindings reach `RecordCall` — from `CallHungUp`, `CallRejected` and `CallFailed` — so a\nfinished call arrives in the log without anybody asking. Nothing reached it at all before.\n\n`termination` was `softphone.media.TerminationReason` and is not any more: that was the wrong layer.\nA declined call has an end cause and never had a media session.\n\n## What was built\n\n`ess/domains/history.yaml`. `CallRecord` — `record_id`, an optional `call_id`, an optional\n`contact_id`, `direction`, `remote`, `started_at`, `ended_at`, `termination`. Three commands:\n`RecordCall`, `AttributeRecord`, `DeleteRecord`. Two views: `CallRecordById`, `RecentCalls`.\n\nBridges 6 and 7 are its two `relations:` entries, both `references`/one, both `Optional` — a record\noutlives the call it came from, and a call to a number nobody saved has no contact.\n\n## Why this is a domain and not a view over ended calls\n\nA view would have been cheaper. Three things ruled it out.\n\n- **`owns` forbids the shape a view implies.** A live call cannot own its history entry, because\n  `owns` means the far side cannot survive its owner.\n- **The vocabulary.** Recording `RemoteHangup` rather than `BYE` is what lets the next binding write\n  into the same log unchanged.\n- **Retention is not call control.** Answering it inside `softphone.control` would give the surface\n  an agent drives a second job.\n\n`remote` is copied into the record rather than referenced, so a record of a call to a contact that\nhas since been deleted still says what was dialled.\n\n## Not built\n\n`ClearHistory`. Erasing an unbounded set on one press is a separate decision and does not arrive by\nimplication from *keep a call log*.\n"},"args":{"changes":{"body":"# Record the calls that finished\n\n## Outcome\n\n`softphone.history` holds one durable record per finished call, in the neutral termination\nvocabulary, optionally attributed to a contact.\n\n## Acceptance\n\n`ess validate` accepts the domain. `CallRecord.termination` is `softphone.control.EndCause` — six\nwords, none of them SIP's. A record survives its call: the relation to `softphone.control.Call` is\n`references`, so nothing in the model lets a call take its record with it. There is no bulk-erase\ncommand. `ended_at >= started_at` is a checked invariant.\n\n**A missed call is derivable**, which it was not when this story shipped: `answered_at` is `Optional`\nand `status` carries the SIP response where the far end sent one, so a call nobody took, a call the\nuser declined and a 486 from the far end are three different rows. Before, all three recorded\nidentically and this story's acceptance did not notice.\n\nThree bindings reach `RecordCall` — from `CallHungUp`, `CallRejected` and `CallFailed` — so a\nfinished call arrives in the log without anybody asking. Nothing reached it at all before.\n\n`termination` was `softphone.media.TerminationReason` and is not any more: that was the wrong layer.\nA declined call has an end cause and never had a media session.\n\n## What was built\n\n`ess/domains/history.yaml`. `CallRecord` — `record_id`, an optional `call_id`, an optional\n`contact_id`, `direction`, `remote`, `started_at`, `ended_at`, `termination`. Three commands:\n`RecordCall`, `AttributeRecord`, `DeleteRecord`. Two views: `CallRecordById`, `RecentCalls`.\n\nBridges 6 and 7 are its two `relations:` entries, both `references`/one, both `Optional` — a record\noutlives the call it came from, and a call to a number nobody saved has no contact.\n\n## Why this is a domain and not a view over ended calls\n\nA view would have been cheaper. Three things ruled it out.\n\n- **`owns` forbids the shape a view implies.** A live call cannot own its history entry, because\n  `owns` means the far side cannot survive its owner.\n- **The vocabulary.** Recording `RemoteHangup` rather than `BYE` is what lets the next binding write\n  into the same log unchanged.\n- **Retention is not call control.** Answering it inside `softphone.control` would give the surface\n  an agent drives a second job.\n\n`remote` is copied into the record rather than referenced, so a record of a call to a contact that\nhas since been deleted still says what was dialled.\n\n## Not built\n\n`ClearHistory`. Erasing an unbounded set on one press is a separate decision and does not arrive by\nimplication from *keep a call log*.\n"},"command":"update-entity","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788518670370,"causation":"cmd-protocol-artifact-body-section-story-call-history-1788518670370","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:call-history@5#0~57f93644135807c7","executor":null,"recorded_at":"2026-09-04T10:44:30Z"}}
{"entity":"story","version":1,"id":"phonebook","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Hold a phonebook\n\n## Outcome\n\n`softphone.directory` holds contacts and the addresses they are reachable at, and a live call tile\ncan show a name instead of a number.\n\n## Acceptance\n\n`ess validate` accepts the domain. A `ContactAddress.address` is exactly the type\n`softphone.control.Dial` takes, so nothing converts between choosing a contact and calling one.\n`softphone.control` names no contact — search the compiled `domains.\"softphone.control\"` for\n`softphone.directory` and it returns nothing.\n\n**What `owns` buys, stated correctly.** This story shipped claiming \"deleting a contact cannot orphan\nan address: the relation is `owns`\". That was wrong, and the domain's own comment said so eight lines\nin: what a delete *does* is a command outcome, not a property of the relation. `DeleteContact` moves\n`Contact.delete` and touches no address, so three `Active` addresses can point at a `Deleted` contact.\n\nWhat `owns` actually buys is three refusals at `ess validate`: a second entity claiming to own\n`ContactAddress`, a `via` field that is missing or mistyped, and a target nothing declares. Removing\nthe addresses is a host obligation, and it is one of the 68 on the *Obligations* page rather than a\nguarantee.\n\n`Contacts` and `ContactAddresses` now filter on `state == Active`, so a deleted contact stops\nappearing in the phonebook even while the row survives.\n\n## What was built\n\n`ess/domains/directory.yaml`. `Contact` — `contact_id`, `display_name`. `ContactAddress` —\n`address_id`, `contact_id`, `address: softphone.control.RemoteAddress`, `label`. Five commands:\n`AddContact`, `RenameContact`, `AddAddress`, `RemoveAddress`, `DeleteContact`. Three views.\n\nBridge 5 is the `owns`/many entry on `Contact`, carried by `contact_id` on the address — the\ncanonical shape from the relations design document §2.1, and the reason the compiler can refuse a\nsecond owner at all.\n\nBridge 8 is on `softphone.presentation.CallTile`, not here: the tile is the thing that wants a name,\nso the tile is what references a contact. That keeps name resolution out of the control surface.\n\n## One grant that is a statement, not an omission\n\n`softphone.directory` declares `Human` alone. An agent dials the address it was given —\n`softphone.control.Dial` takes a `RemoteAddress`, never a contact — so it needs no grant here.\nWidening this to an agent is a decision somebody should take deliberately, not a gap to fill in\npassing.\n\n## Not built\n\nContact import or export, and any address book the browser did not receive by hand.\n"},"args":{"changes":{"body":"# Hold a phonebook\n\n## Outcome\n\n`softphone.directory` holds contacts and the addresses they are reachable at, and a live call tile\ncan show a name instead of a number.\n\n## Acceptance\n\n`ess validate` accepts the domain. A `ContactAddress.address` is exactly the type\n`softphone.control.Dial` takes, so nothing converts between choosing a contact and calling one.\n`softphone.control` names no contact — search the compiled `domains.\"softphone.control\"` for\n`softphone.directory` and it returns nothing.\n\n**What `owns` buys, stated correctly.** This story shipped claiming \"deleting a contact cannot orphan\nan address: the relation is `owns`\". That was wrong, and the domain's own comment said so eight lines\nin: what a delete *does* is a command outcome, not a property of the relation. `DeleteContact` moves\n`Contact.delete` and touches no address, so three `Active` addresses can point at a `Deleted` contact.\n\nWhat `owns` actually buys is three refusals at `ess validate`: a second entity claiming to own\n`ContactAddress`, a `via` field that is missing or mistyped, and a target nothing declares. Removing\nthe addresses is a host obligation, and it is one of the 68 on the *Obligations* page rather than a\nguarantee.\n\n`Contacts` and `ContactAddresses` now filter on `state == Active`, so a deleted contact stops\nappearing in the phonebook even while the row survives.\n\n## What was built\n\n`ess/domains/directory.yaml`. `Contact` — `contact_id`, `display_name`. `ContactAddress` —\n`address_id`, `contact_id`, `address: softphone.control.RemoteAddress`, `label`. Five commands:\n`AddContact`, `RenameContact`, `AddAddress`, `RemoveAddress`, `DeleteContact`. Three views.\n\nBridge 5 is the `owns`/many entry on `Contact`, carried by `contact_id` on the address — the\ncanonical shape from the relations design document §2.1, and the reason the compiler can refuse a\nsecond owner at all.\n\nBridge 8 is on `softphone.presentation.CallTile`, not here: the tile is the thing that wants a name,\nso the tile is what references a contact. That keeps name resolution out of the control surface.\n\n## One grant that is a statement, not an omission\n\n`softphone.directory` declares `Human` alone. An agent dials the address it was given —\n`softphone.control.Dial` takes a `RemoteAddress`, never a contact — so it needs no grant here.\nWidening this to an agent is a decision somebody should take deliberately, not a gap to fill in\npassing.\n\n## Not built\n\nContact import or export, and any address book the browser did not receive by hand.\n"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788518670457,"causation":"cmd-protocol-artifact-body-section-story-phonebook-1788518670457","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:phonebook@5#0~eac8a12fb54725ac","executor":null,"recorded_at":"2026-09-04T10:44:30Z"}}
{"entity":"story","version":1,"id":"layer-bridges","revision":5,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Bridge the layers with checked relations\n\n## Outcome\n\nThe eight links between the seven domains are `relations:` entries, so `ess validate` refuses a\nbroken one. A layering described in prose is a layering nobody can check.\n\n## Acceptance\n\nAll eight appear in the compiled IR under `entities.*.relations`, and each of the five refusal rules\nfires when its defect is introduced. Both were run; the results are below.\n\n## The eight\n\n| # | Source | Kind / card. | Target | via |\n|---|---|---|---|---|\n| 1 | `control.Call` | references / one | `media.MediaSession` | `session_id` |\n| 2 | `sip.SipDialog` | references / one | `media.MediaSession` | `session_id` |\n| 3 | `local.LoopbackDevice` | references / one | `media.MediaSession` | `session_id` |\n| 4 | `presentation.CallTile` | references / one | `control.Call` | `call_id` |\n| 5 | `directory.Contact` | owns / many | `directory.ContactAddress` | `contact_id` |\n| 6 | `history.CallRecord` | references / one | `control.Call` | `call_id` |\n| 7 | `history.CallRecord` | references / one | `directory.Contact` | `contact_id` |\n| 8 | `presentation.CallTile` | references / one | `directory.Contact` | `contact_id` |\n\nSeven of the eight cross a domain boundary. That works because `build_registry` indexes every\ndomain's types into one registry and resolves each reference against it\n(`crates/specify/ess-domain/src/system.rs:481-495`); nothing scopes resolution to a domain.\n\n**Bridges 2 and 3 point the other way from how this story shipped.** They were\n`media.MediaSession owns sip.SipDialog` and `owns local.LoopbackDevice`, which put the two binding\ndomains inside the one domain whose claim is that it names neither.\n`architecture-decision-record:media-session-owns-nothing` records the flip and what `owns` bought\nthat `references` does not — and the two bindings `end-session-with-dialog` and\n`end-session-with-loopback` are what hold it now.\n\nBridge 6's carrier is bare rather than `Optional`, because the three bindings that invoke\n`RecordCall` map a bare `CallId` out of their events and ESS does not widen it:\n`upstream-blocker:ess-mapping-does-not-widen-optional`.\n\n## Evidence that the bridges are checked and not asserted\n\nFive defects, one at a time, each in a throwaway copy of `ess/` under `$TMPDIR` — never the\ncommitted tree. Every run exited 1.\n\n| Defect | Code returned |\n|---|---|\n| misspell a relation `target` | `undeclared_reference` |\n| `via` names a field that does not exist | `missing_declaration` |\n| retype a `via` field to `String` | `type_mismatch` |\n| a second `owns` targeting `SipDialog` | `conflicting_declaration` (with `type_mismatch`) |\n| two relations carried by one field | `duplicate_declaration` (with `type_mismatch`) |\n\nThe five expected codes are the five rules in the relations design document §3. The two secondary\n`type_mismatch` results are consequences of the same edit, not surprises: a stolen `owns` and a\nre-pointed `via` both leave the carrier holding the wrong identity type.\n\n## What no relation can hold\n\nTwo limits, both recorded in the domains rather than worked around.\n\n- **\"Exactly one of these two children exists\" is not expressible.** Bridges 2 and 3 are both\n  `owns`/one. Causation holds the correspondence instead — one `creates` outcome per binding.\n- **A command in one domain causing a command in another is not modelled.** Nothing declares that a\n  keypress in `softphone.presentation` eventually issues `softphone.control.Dial`. The relation is\n  checked; the causation is not, and a component accepting a command another component owns the\n  domain of is refused outright (`crates/specify/ess-domain/src/component.rs:921`).\n"},"args":{"changes":{"body":"# Bridge the layers with checked relations\n\n## Outcome\n\nThe eight links between the seven domains are `relations:` entries, so `ess validate` refuses a\nbroken one. A layering described in prose is a layering nobody can check.\n\n## Acceptance\n\nAll eight appear in the compiled IR under `entities.*.relations`, and each of the five refusal rules\nfires when its defect is introduced. Both were run; the results are below.\n\n## The eight\n\n| # | Source | Kind / card. | Target | via |\n|---|---|---|---|---|\n| 1 | `control.Call` | references / one | `media.MediaSession` | `session_id` |\n| 2 | `sip.SipDialog` | references / one | `media.MediaSession` | `session_id` |\n| 3 | `local.LoopbackDevice` | references / one | `media.MediaSession` | `session_id` |\n| 4 | `presentation.CallTile` | references / one | `control.Call` | `call_id` |\n| 5 | `directory.Contact` | owns / many | `directory.ContactAddress` | `contact_id` |\n| 6 | `history.CallRecord` | references / one | `control.Call` | `call_id` |\n| 7 | `history.CallRecord` | references / one | `directory.Contact` | `contact_id` |\n| 8 | `presentation.CallTile` | references / one | `directory.Contact` | `contact_id` |\n\nSeven of the eight cross a domain boundary. That works because `build_registry` indexes every\ndomain's types into one registry and resolves each reference against it\n(`crates/specify/ess-domain/src/system.rs:481-495`); nothing scopes resolution to a domain.\n\n**Bridges 2 and 3 point the other way from how this story shipped.** They were\n`media.MediaSession owns sip.SipDialog` and `owns local.LoopbackDevice`, which put the two binding\ndomains inside the one domain whose claim is that it names neither.\n`architecture-decision-record:media-session-owns-nothing` records the flip and what `owns` bought\nthat `references` does not — and the two bindings `end-session-with-dialog` and\n`end-session-with-loopback` are what hold it now.\n\nBridge 6's carrier is bare rather than `Optional`, because the three bindings that invoke\n`RecordCall` map a bare `CallId` out of their events and ESS does not widen it:\n`upstream-blocker:ess-mapping-does-not-widen-optional`.\n\n## Evidence that the bridges are checked and not asserted\n\nFive defects, one at a time, each in a throwaway copy of `ess/` under `$TMPDIR` — never the\ncommitted tree. Every run exited 1.\n\n| Defect | Code returned |\n|---|---|\n| misspell a relation `target` | `undeclared_reference` |\n| `via` names a field that does not exist | `missing_declaration` |\n| retype a `via` field to `String` | `type_mismatch` |\n| a second `owns` targeting `SipDialog` | `conflicting_declaration` (with `type_mismatch`) |\n| two relations carried by one field | `duplicate_declaration` (with `type_mismatch`) |\n\nThe five expected codes are the five rules in the relations design document §3. The two secondary\n`type_mismatch` results are consequences of the same edit, not surprises: a stolen `owns` and a\nre-pointed `via` both leave the carrier holding the wrong identity type.\n\n## What no relation can hold\n\nTwo limits, both recorded in the domains rather than worked around.\n\n- **\"Exactly one of these two children exists\" is not expressible.** Bridges 2 and 3 are both\n  `owns`/one. Causation holds the correspondence instead — one `creates` outcome per binding.\n- **A command in one domain causing a command in another is not modelled.** Nothing declares that a\n  keypress in `softphone.presentation` eventually issues `softphone.control.Dial`. The relation is\n  checked; the causation is not, and a component accepting a command another component owns the\n  domain of is refused outright (`crates/specify/ess-domain/src/component.rs:921`).\n"},"command":"update-entity","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788518670545,"causation":"cmd-protocol-artifact-body-section-story-layer-bridges-1788518670545","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:layer-bridges@5#0~f2fe2bcf7b1a53ce","executor":null,"recorded_at":"2026-09-04T10:44:30Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, keep a log of them and\na phonebook, and show it all in a browser page. Signalling and dialog state come from the sipx\nWebAssembly session kernel (`crates/sipx-wasm`, story S-41) driven over the WebSocket binding\n(`browser/src/`, story T-33), both present at the pinned revision. Media is negotiated by the\nbrowser's own `RTCPeerConnection`.\n\nSeven domains, one per concern, in system `softphone` v1:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the media session; references no binding, checked against the compiled IR |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | one durable record per finished call |\n| `softphone.directory` | contacts and the addresses they are reachable at |\n| `softphone.presentation` | the keypad, one tile per call, and which mode the screen is in |\n| `softphone.sip` | the SIP binding, including the offer/answer exchange |\n| `softphone.local` | the local-audio binding, whose existence makes the media layer's neutrality checkable |\n\nThey are joined by **eight `relations:`** entries, seven crossing a domain boundary, and by **seven\n`bindings:`** — one event causing one command — which is what makes a layer react to another.\n\nCounts, from `ess compile`: 52 commands, 52 events, 16 views, 12 entities, 7 invariants, 7 workloads,\n13 `refs`. `ess validate --path ess` reports `softphone v1 — 10 file(s), valid`.\n`ess generate synthesize --target rust` reports `307 capabilities: 221 generated, 68 obligation(s),\n18 refused` and the emitted crates build clean.\n\n`architecture-decision-record:layered-softphone-domains` holds why this shape rather than three\nsystems; `architecture-decision-record:media-session-owns-nothing` holds why bridges 2 and 3 point at\nthe media session rather than away from it.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n"},"args":{"changes":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, keep a log of them and\na phonebook, and show it all in a browser page. Signalling and dialog state come from the sipx\nWebAssembly session kernel (`crates/sipx-wasm`, story S-41) driven over the WebSocket binding\n(`browser/src/`, story T-33), both present at the pinned revision. Media is negotiated by the\nbrowser's own `RTCPeerConnection`.\n\nSeven domains, one per concern, in system `softphone` v1:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the media session; references no binding, checked against the compiled IR |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | one durable record per finished call |\n| `softphone.directory` | contacts and the addresses they are reachable at |\n| `softphone.presentation` | the keypad, one tile per call, and which mode the screen is in |\n| `softphone.sip` | the SIP binding, including the offer/answer exchange |\n| `softphone.local` | the local-audio binding, whose existence makes the media layer's neutrality checkable |\n\nThey are joined by **eight `relations:`** entries, seven crossing a domain boundary, and by **seven\n`bindings:`** — one event causing one command — which is what makes a layer react to another.\n\nCounts, from `ess compile`: 52 commands, 52 events, 16 views, 12 entities, 7 invariants, 7 workloads,\n13 `refs`. `ess validate --path ess` reports `softphone v1 — 10 file(s), valid`.\n`ess generate synthesize --target rust` reports `307 capabilities: 221 generated, 68 obligation(s),\n18 refused` and the emitted crates build clean.\n\n`architecture-decision-record:layered-softphone-domains` holds why this shape rather than three\nsystems; `architecture-decision-record:media-session-owns-nothing` holds why bridges 2 and 3 point at\nthe media session rather than away from it.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788518695974,"causation":"cmd-protocol-artifact-body-section-epic-browser-softphone-1788518695974","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:browser-softphone@5#0~0f832c6a47599477","executor":null,"recorded_at":"2026-09-04T10:44:55Z"}}
{"entity":"epic","version":1,"id":"browser-softphone","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, keep a log of them and\na phonebook, and show it all in a browser page. Signalling and dialog state come from the sipx\nWebAssembly session kernel (`crates/sipx-wasm`, story S-41) driven over the WebSocket binding\n(`browser/src/`, story T-33), both present at the pinned revision. Media is negotiated by the\nbrowser's own `RTCPeerConnection`.\n\nSeven domains, one per concern, in system `softphone` v1:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the media session; references no binding, checked against the compiled IR |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | one durable record per finished call |\n| `softphone.directory` | contacts and the addresses they are reachable at |\n| `softphone.presentation` | the keypad, one tile per call, and which mode the screen is in |\n| `softphone.sip` | the SIP binding, including the offer/answer exchange |\n| `softphone.local` | the local-audio binding, whose existence makes the media layer's neutrality checkable |\n\nThey are joined by **eight `relations:`** entries, seven crossing a domain boundary, and by **seven\n`bindings:`** — one event causing one command — which is what makes a layer react to another.\n\nCounts, from `ess compile`: 52 commands, 52 events, 16 views, 12 entities, 7 invariants, 7 workloads,\n13 `refs`. `ess validate --path ess` reports `softphone v1 — 10 file(s), valid`.\n`ess generate synthesize --target rust` reports `307 capabilities: 221 generated, 68 obligation(s),\n18 refused` and the emitted crates build clean.\n\n`architecture-decision-record:layered-softphone-domains` holds why this shape rather than three\nsystems; `architecture-decision-record:media-session-owns-nothing` holds why bridges 2 and 3 point at\nthe media session rather than away from it.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n\n## What is out of scope, and what merely is not built\n\nTwo kinds of absence, and they are not the same. The first was never in scope. The second is absent\nbecause the **target contract** excludes it, and until now nothing in this store said so — the only\nrecorded exclusion was \"video and data channels\".\n\n## Excluded by the sipx browser SDK v1 contract\n\n`docs/specs/browser-sdk.md:830` §10, prefaced \"this contract does not include, and completing the\nepic MUST NOT imply\": hold and resume re-INVITEs, transfer (REFER), **DTMF sending**, MESSAGE,\nSUBSCRIBE/NOTIFY, PUBLISH.\n\nFour of those the specification does not model at all. **DTMF it does** — six constructs, kept\ndeliberately: `softphone.media` is modelled from a Rust port where DTMF is real, and the next\nbinding may carry it. What that means concretely is that `softphone.control.SendDigits` has no SIP\npath until the contract changes, and `domains/sip.yaml` says so where somebody implementing it will\nread it.\n\nsipx's *native* stack ships hold, resume and transfer (`README.md`, \"Does it fit?\"), so their absence\nhere is a browser-SDK constraint and not a limit of the stack.\n\n## Not in scope for 0.1, and nothing pretends otherwise\n\nConference, DND, voicemail and MWI, duration, audio-device selection for a SIP call (`DeviceRef`\nexists only in the loopback binding), volume, ringtone and ringback, call quality, redial, park,\npresence, outbound proxy, a call limit, call recording. A service, an event log, multi-device sync.\nContact import or export. Video, data channels, TURN relay — sipx offers host and STUN-derived ICE\ncandidates and no relay.\n\n## In scope and not built: two concurrent calls\n\nsipx allows eight concurrent calls (`browser-sdk.md:281`). `softphone.presentation.Console` now holds\none active tile and four modes, which is the beginning of call focus, and there is **no held state**:\ntwo calls can both be `Active` with both capture paths open. Local media hold and call focus are not\nexcluded by the contract and two concurrent calls need them. That is the next story, not a gap in\nthis one.\n"},"args":{"changes":{"body":"# Browser softphone\n\n## Scope\n\nRegister one SIP account over secure WebSocket, place and answer audio calls, keep a log of them and\na phonebook, and show it all in a browser page. Signalling and dialog state come from the sipx\nWebAssembly session kernel (`crates/sipx-wasm`, story S-41) driven over the WebSocket binding\n(`browser/src/`, story T-33), both present at the pinned revision. Media is negotiated by the\nbrowser's own `RTCPeerConnection`.\n\nSeven domains, one per concern, in system `softphone` v1:\n\n| Domain | Concern |\n|---|---|\n| `softphone.media` | the media session; references no binding, checked against the compiled IR |\n| `softphone.control` | the surface a human or an agent drives, identically |\n| `softphone.history` | one durable record per finished call |\n| `softphone.directory` | contacts and the addresses they are reachable at |\n| `softphone.presentation` | the keypad, one tile per call, and which mode the screen is in |\n| `softphone.sip` | the SIP binding, including the offer/answer exchange |\n| `softphone.local` | the local-audio binding, whose existence makes the media layer's neutrality checkable |\n\nThey are joined by **eight `relations:`** entries, seven crossing a domain boundary, and by **seven\n`bindings:`** — one event causing one command — which is what makes a layer react to another.\n\nCounts, from `ess compile`: 52 commands, 52 events, 16 views, 12 entities, 7 invariants, 7 workloads,\n13 `refs`. `ess validate --path ess` reports `softphone v1 — 10 file(s), valid`.\n`ess generate synthesize --target rust` reports `307 capabilities: 221 generated, 68 obligation(s),\n18 refused` and the emitted crates build clean.\n\n`architecture-decision-record:layered-softphone-domains` holds why this shape rather than three\nsystems; `architecture-decision-record:media-session-owns-nothing` holds why bridges 2 and 3 point at\nthe media session rather than away from it.\n\n## What upstream has not shipped\n\nAt the pinned revision two of M15's stories are pending, and both sit on the path to a working\nsoftphone:\n\n| Upstream story | What it would deliver | Cited |\n|---|---|---|\n| `M-52` | audio-only `RTCPeerConnection` adapter | `docs/specs/browser-sdk.md:854`, `browser/README.md` |\n| `A-17` | the installable `@sipx/browser` package and the `SipxClient` lifecycle layer | `docs/specs/browser-sdk.md:855`, `docs/specs/browser-signalling.md:212` |\n\n`docs/roadmap.md` states M15 \"is tracked but is **not** part of the selected M13 wave\", so neither is\nscheduled. This project therefore writes the media adapter against the normative contract in\n`docs/specs/browser-sdk.md` §5.4 and §6.2 rather than consuming a package, and vendors the\n`browser/src/` binding rather than installing it.\n\nThat the adapter lands here rather than upstream is decided:\n`architecture-decision-record:media-adapter-lands-here`.\n\n## Done When\n\nA browser page registers the configured address-of-record over WSS, places and answers an audio call\nagainst a SIP endpoint, carries non-silent audio in both directions, and ends every call with every\nsocket, timer and media track observed closed.\n\n## Prior art\n\nThis organisation has already shipped a browser phone, on a different protocol. It is worth reading\nbefore this one is built, and it is cited rather than copied.\n\n`~/babelforce/projects/ai-agent-platform/docs/designs/browser-voice.md` — status *Implemented*,\nbacking stories F-07, E-09, F-83. Its phone speaks RTVBP over WebSocket/WebRTC, not SIP, so none of\nits protocol work transfers. Its shape does:\n\n| What it settled | Where | Bearing on this epic |\n|---|---|---|\n| A thin product adapter over an authoritative SDK; the console holds no protocol, envelope, codec, resampler or worklet code | §1, §2 | the same split is `story:signalling-over-wss` (transport, vendored) against `story:call-lifecycle` (product) |\n| Browsers cannot set an `Authorization` header on a WebSocket upgrade; the token rides the negotiated subprotocol list | §4 | a gap in `story:signalling-over-wss` until this was read |\n| ICE/TURN reachability is the deployment's; the SDK ships no relay | §4, §7 | agrees with sipx `README.md`, which offers host and STUN-derived candidates and no TURN relay |\n| One idempotent teardown path that every failure converges on, ignoring callbacks from an obsolete connection generation | §5 | `story:browser-media-adapter` and `story:call-lifecycle` |\n| Microphone permission and audio autoplay need an explicit user gesture and a secure context | §3, §7 | `story:softphone-page` |\n| Evidence shape: clean registry install, a static test that the adapter holds no protocol code, an in-memory session test, a real-browser non-silent echo, one bounded device smoke | §6 | the *Done When* above |\n\nThe code is at `sdk/typescript/src/rtvbp.ts` and\n`web/packages/console/src/composables/useVoiceSession.ts` in that repository.\n\n**One rule of theirs this epic cannot follow.** §1 says generated SDK output is never copied and the\nlock must resolve the exact public npm version. sipx has no published browser package — `A-17` is\nunshipped — so `story:signalling-over-wss` vendors `browser/src/` from a pinned Git revision instead.\nThat is the same substitution\n`architecture-decision-record:media-adapter-lands-here` records for the media adapter, and it retires\nthe same way, when upstream ships.\n\n## What is out of scope, and what merely is not built\n\nTwo kinds of absence, and they are not the same. The first was never in scope. The second is absent\nbecause the **target contract** excludes it, and until now nothing in this store said so — the only\nrecorded exclusion was \"video and data channels\".\n\n## Excluded by the sipx browser SDK v1 contract\n\n`docs/specs/browser-sdk.md:830` §10, prefaced \"this contract does not include, and completing the\nepic MUST NOT imply\": hold and resume re-INVITEs, transfer (REFER), **DTMF sending**, MESSAGE,\nSUBSCRIBE/NOTIFY, PUBLISH.\n\nFour of those the specification does not model at all. **DTMF it does** — six constructs, kept\ndeliberately: `softphone.media` is modelled from a Rust port where DTMF is real, and the next\nbinding may carry it. What that means concretely is that `softphone.control.SendDigits` has no SIP\npath until the contract changes, and `domains/sip.yaml` says so where somebody implementing it will\nread it.\n\nsipx's *native* stack ships hold, resume and transfer (`README.md`, \"Does it fit?\"), so their absence\nhere is a browser-SDK constraint and not a limit of the stack.\n\n## Not in scope for 0.1, and nothing pretends otherwise\n\nConference, DND, voicemail and MWI, duration, audio-device selection for a SIP call (`DeviceRef`\nexists only in the loopback binding), volume, ringtone and ringback, call quality, redial, park,\npresence, outbound proxy, a call limit, call recording. A service, an event log, multi-device sync.\nContact import or export. Video, data channels, TURN relay — sipx offers host and STUN-derived ICE\ncandidates and no relay.\n\n## In scope and not built: two concurrent calls\n\nsipx allows eight concurrent calls (`browser-sdk.md:281`). `softphone.presentation.Console` now holds\none active tile and four modes, which is the beginning of call focus, and there is **no held state**:\ntwo calls can both be `Active` with both capture paths open. Local media hold and call focus are not\nexcluded by the contract and two concurrent calls need them. That is the next story, not a gap in\nthis one.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788518696079,"causation":"cmd-protocol-artifact-body-section-epic-browser-softphone-1788518696079","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:browser-softphone@6#0~5f133ec218d4bc41","executor":null,"recorded_at":"2026-09-04T10:44:56Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Make the specification say only true things, and use the constructs it has\n\n## Outcome\n\nThe seven domains use the ESS constructs they were missing, and every claim written in them is\ncheckable. Two independent reviews — one on the phone as a product, one on the ESS usage — returned\n21 and 16 findings against a specification that validated, compiled and synthesised\nbyte-deterministically. Neither disputed the layering; both found the same root cause from opposite\nsides.\n\n**`ess validate` checks that references resolve and that types match. It does not check that a\ndeclared field can ever be written, that an event has a reader, or that a comment is true.**\n\n## Acceptance\n\nAll assertions read from the compiled IR, not the source:\n\n| Claim | Before | After |\n|---|---|---|\n| `creates`/`updates` outcomes writing no field | 33 | **0** |\n| outcomes carrying `sets:` | 0 | 16 |\n| `bindings:` — events with a reader | 0 | 7 |\n| byte-identical view pairs | 4 | **0** |\n| views with `params`, `filter` or `order_by` | 0 | 16 of 16 have at least one |\n| `invariants:` | 0 | 3 on entities, 4 on a type |\n| components under \"components that run nowhere\" | 7 | **0** |\n| `refs:` carrying provenance a projection reads | 0 | 13 |\n| `Human` / `Agent` grants identical | yes | yes, 7 commands each |\n| generated Rust builds | clean | clean, `Finished release profile in 1.21s` |\n\n`ess validate --path ess` → `softphone v1 — 10 file(s), valid`. Two `synthesize` runs to different\ndirectories diff clean.\n\n## What was wrong, and is not now\n\n- **`Call.session_id` had no writer.** Bridge 1 was a relation nothing could establish, so no call\n  could be joined to its media. `AttachMedia` writes it.\n- **Nothing on the network could end a call.** `Kernel` held only `OfferCall`, `RingCall` and\n  `ConfirmAnswer`, so a cancelled inbound call rang until a person hung up a call already gone.\n  `FailCall` carries one of six `EndCause` words.\n- **`Active` was asserted a step too early.** `Answer` moved straight to `Active`, where the contract\n  requires the `RTCPeerConnection` be connected first and a failure be \"presented as `failed`, not\n  as connected\". `Answering` is that step, and only `MediaConnected` leaves it.\n- **`Call.direction` was undetermined.** An implementation recording every call `Inbound` conformed.\n  `Dial` and `OfferCall` now set it as an enum literal.\n- **The offer/answer exchange had no home.** Four commands in `softphone.sip` and two descriptions on\n  the dialog.\n- **`Registration.Failed` was terminal.** One transient WSS drop killed the registration for good.\n  `RetryRegistration` is the way back, and `expires` exists at all now.\n- **A ringing call could not show itself.** `softphone.presentation` grants only `Human` and\n  `ShowCall` was in that grant alone. `show-incoming-call` is the binding.\n- **Four claims in the YAML were false**, one of them the acceptance statement of a story already at\n  `implemented`. All four are corrected, and the media-neutrality claim is now an IR query rather\n  than a `grep` that returned five comment lines.\n\n## Three ESS defects found doing it\n\nEach is filed, worked around, and the workaround is written where somebody will read it.\n\n| Blocker | What it is |\n|---|---|\n| `upstream-blocker:ess-system-level-type-breaks-synthesis` | a system-level `types:` entry validates and panics all three synthesis targets |\n| `upstream-blocker:ess-mapping-does-not-widen-optional` | a binding mapping `T` into `Optional<T>` validates and emits Rust that does not compile |\n| `upstream-blocker:ess-web-target-redeliver` | the web target calls a `redeliver` method the rust target does not generate; still present at 0.14.0 |\n\n## Two limits that are not defects\n\n- **An invariant reads only its own entity.** So \"exactly one of these two children exists\" is not\n  expressible; causation holds it — one `creates` outcome per binding.\n- **A binding maps only from its event's fields or a literal.** No lookup, no clock. That is why\n  `RecordCall` takes a call id and reads the rest, why `ShowCall` lost its `contact_id` input in\n  favour of `AttributeTile`, and why no binding can move `Console`: no control event carries a\n  console id.\n","summary":"sets, bindings, invariants, view params and filters, refs, topology; and four false claims corrected.","title":"Make the specification say only true things, and use the constructs it has"},"args":{"command":"create-entity","data":{"body":"# Make the specification say only true things, and use the constructs it has\n\n## Outcome\n\nThe seven domains use the ESS constructs they were missing, and every claim written in them is\ncheckable. Two independent reviews — one on the phone as a product, one on the ESS usage — returned\n21 and 16 findings against a specification that validated, compiled and synthesised\nbyte-deterministically. Neither disputed the layering; both found the same root cause from opposite\nsides.\n\n**`ess validate` checks that references resolve and that types match. It does not check that a\ndeclared field can ever be written, that an event has a reader, or that a comment is true.**\n\n## Acceptance\n\nAll assertions read from the compiled IR, not the source:\n\n| Claim | Before | After |\n|---|---|---|\n| `creates`/`updates` outcomes writing no field | 33 | **0** |\n| outcomes carrying `sets:` | 0 | 16 |\n| `bindings:` — events with a reader | 0 | 7 |\n| byte-identical view pairs | 4 | **0** |\n| views with `params`, `filter` or `order_by` | 0 | 16 of 16 have at least one |\n| `invariants:` | 0 | 3 on entities, 4 on a type |\n| components under \"components that run nowhere\" | 7 | **0** |\n| `refs:` carrying provenance a projection reads | 0 | 13 |\n| `Human` / `Agent` grants identical | yes | yes, 7 commands each |\n| generated Rust builds | clean | clean, `Finished release profile in 1.21s` |\n\n`ess validate --path ess` → `softphone v1 — 10 file(s), valid`. Two `synthesize` runs to different\ndirectories diff clean.\n\n## What was wrong, and is not now\n\n- **`Call.session_id` had no writer.** Bridge 1 was a relation nothing could establish, so no call\n  could be joined to its media. `AttachMedia` writes it.\n- **Nothing on the network could end a call.** `Kernel` held only `OfferCall`, `RingCall` and\n  `ConfirmAnswer`, so a cancelled inbound call rang until a person hung up a call already gone.\n  `FailCall` carries one of six `EndCause` words.\n- **`Active` was asserted a step too early.** `Answer` moved straight to `Active`, where the contract\n  requires the `RTCPeerConnection` be connected first and a failure be \"presented as `failed`, not\n  as connected\". `Answering` is that step, and only `MediaConnected` leaves it.\n- **`Call.direction` was undetermined.** An implementation recording every call `Inbound` conformed.\n  `Dial` and `OfferCall` now set it as an enum literal.\n- **The offer/answer exchange had no home.** Four commands in `softphone.sip` and two descriptions on\n  the dialog.\n- **`Registration.Failed` was terminal.** One transient WSS drop killed the registration for good.\n  `RetryRegistration` is the way back, and `expires` exists at all now.\n- **A ringing call could not show itself.** `softphone.presentation` grants only `Human` and\n  `ShowCall` was in that grant alone. `show-incoming-call` is the binding.\n- **Four claims in the YAML were false**, one of them the acceptance statement of a story already at\n  `implemented`. All four are corrected, and the media-neutrality claim is now an IR query rather\n  than a `grep` that returned five comment lines.\n\n## Three ESS defects found doing it\n\nEach is filed, worked around, and the workaround is written where somebody will read it.\n\n| Blocker | What it is |\n|---|---|\n| `upstream-blocker:ess-system-level-type-breaks-synthesis` | a system-level `types:` entry validates and panics all three synthesis targets |\n| `upstream-blocker:ess-mapping-does-not-widen-optional` | a binding mapping `T` into `Optional<T>` validates and emits Rust that does not compile |\n| `upstream-blocker:ess-web-target-redeliver` | the web target calls a `redeliver` method the rust target does not generate; still present at 0.14.0 |\n\n## Two limits that are not defects\n\n- **An invariant reads only its own entity.** So \"exactly one of these two children exists\" is not\n  expressible; causation holds it — one `creates` outcome per binding.\n- **A binding maps only from its event's fields or a literal.** No lookup, no clock. That is why\n  `RecordCall` takes a call id and reads the rest, why `ShowCall` lost its `contact_id` input in\n  favour of `AttributeTile`, and why no binding can move `Console`: no control event carries a\n  console id.\n","status":"draft","summary":"sets, bindings, invariants, view params and filters, refs, topology; and four false claims corrected.","title":"Make the specification say only true things, and use the constructs it has"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/specification-remediation"},"payload":{"actor":"human:timo","at":1788518725638,"causation":"cmd-new-story-specification-remediation","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:specification-remediation@1#0~ee8032d29692e774","executor":null,"recorded_at":"2026-09-04T10:45:25Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000114","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788518725653,"causation":"cmd-story-specification-remediation-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:specification-remediation@1#0~9a41564233d6ff8b","executor":null,"recorded_at":"2026-09-04T10:45:25Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:layer-bridges"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000114","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788518725659,"causation":"cmd-story-specification-remediation-depends_on-story-layer-bridges","change":{"change":"related","relation":"depends_on","target":"story:layer-bridges"},"correlation":"protocol-artifact-relate","event_id":"story:specification-remediation@1#0~5d9449d6187949ba","executor":null,"recorded_at":"2026-09-04T10:45:25Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"ess validate --path ess -> softphone v1 - 10 file(s), valid; ess generate synthesize --target rust -> 307 capabilities, 221 generated, 68 obligations, 18 refused; cargo build --release -> Finished release profile in 1.21s; two synth runs diff clean","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788518725000,"causation":"cmd-evidence-story-specification-remediation-test_result-1788518725747","change":{"change":"evidence","kind":"test_result","source":"ess validate --path ess -> softphone v1 - 10 file(s), valid; ess generate synthesize --target rust -> 307 capabilities, 221 generated, 68 obligations, 18 refused; cargo build --release -> Finished release profile in 1.21s; two synth runs diff clean"},"correlation":"protocol-artifact-evidence","event_id":"story:specification-remediation@1#0~abf952d74d4d2842","executor":null,"recorded_at":"2026-09-04T10:45:25Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","source":"compiled IR: 0 creates/updates outcomes without sets, 0 byte-identical view pairs, 7 bindings, 7 invariants, 7 workloads, 13 refs, Human==Agent at 7 commands each","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788518725000,"causation":"cmd-evidence-story-specification-remediation-test_result-1788518725832","change":{"change":"evidence","kind":"test_result","source":"compiled IR: 0 creates/updates outcomes without sets, 0 byte-identical view pairs, 7 bindings, 7 invariants, 7 workloads, 13 refs, Human==Agent at 7 commands each"},"correlation":"protocol-artifact-evidence","event_id":"story:specification-remediation@1#0~5ecacc344aab64a1","executor":null,"recorded_at":"2026-09-04T10:45:25Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000043","to":"proposed"},"payload":{"actor":"human:timo","at":1788518725986,"causation":"cmd-move-story-specification-remediation-1788518725986","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:specification-remediation@2#0~271ce7c1a10f838d","executor":null,"recorded_at":"2026-09-04T10:45:25Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000043","to":"active"},"payload":{"actor":"human:timo","at":1788518726144,"causation":"cmd-move-story-specification-remediation-1788518726144","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:specification-remediation@3#0~4e87a330136c0cbd","executor":null,"recorded_at":"2026-09-04T10:45:26Z"}}
{"entity":"story","version":1,"id":"specification-remediation","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000043","to":"implemented"},"payload":{"actor":"human:timo","at":1788518726304,"causation":"cmd-move-story-specification-remediation-1788518726304","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:specification-remediation@4#0~c291e944c0579887","executor":null,"recorded_at":"2026-09-04T10:45:26Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-web-target-redeliver","revision":3,"type":"aep.entity.update/v1","from_state":"open","to_state":"open","changed":{"body":"# The generated web target does not compile at ESS 0.13.1\n\n## What was observed\n\n`ess generate synthesize --path ess --target web` emits a tree that fails to build:\n\n```\ncrates/softphone-web/src/lib.rs:377:14\n    self.redeliver(&event)?;\n         ^^^^^^^^^ method not found in `&mut System<CallHistoryBehaviors, ..., ...>`\nerror: could not compile `softphone-web` (lib) due to 1 previous error\n```\n\n`redeliver` appears **nowhere** in the rust target the web crate depends on — `grep -rn redeliver`\nover all 30 emitted Rust artifacts returns nothing. The web emitter emits a call to a method the\nrust emitter does not generate.\n\n## It is not this specification\n\nThe rust target from the same specification builds clean: `cargo build --release` →\n`Finished release profile [optimized] target(s) in 1.02s`, all nine crates. Both targets report the\nsame 231 capabilities — 168 generated, 52 obligations, 11 refused — and web synthesis is\nbyte-identical across two runs. The specification validates: `softphone v1 — 9 file(s), valid`.\n\nSo the defect is in `ess generate synthesize --target web` at `0.13.1`\n(`git+https://github.com/beyond10x/ess?tag=0.13.1#d1a66772a91b5411d942d7a45bbf08dfc5de4651`), in the\n`replay` function of the emitted bridge.\n\n## What it blocks\n\n`story:softphone-page`'s acceptance is *the generated web surface drives a call before any page is\nhand-written*. That surface cannot be loaded, so no browser observation of this specification can be\nproduced at all — which also leaves the open question of whether the page persists anything across\na reload unanswerable.\n\n## What would clear it\n\nA fix upstream in `beyond10x/ess`: either the web emitter stops calling `redeliver`, or the rust\nemitter generates it. One line of the emitted bridge names the seam.\n\nFiled as an `upstream-blocker` rather than a `decision-blocker` because there is nothing here to\ndecide. `aep artifact kinds` does not list this kind by name — the `<type>-blocker` family is open\nand `aep artifact lifecycle upstream-blocker` answers `open -> cleared`.\n\n## What is not blocked\n\nEverything specification-side. All eight bridges are in the compiled IR, the five refusal codes were\nobserved, and the rust target builds — so the layering is verified without the page.\n\n## Version, corrected\n\nThe version this record was filed against is wrong twice over, and the defect outlived both.\n\nFiled as ESS `0.13.1`. The installed binary was `0.13.5` at the time of the reviews, and was\nreinstalled to **`0.14.0`** (`git+https://github.com/beyond10x/ess?tag=0.14.0#a25090533d2dcaf8e3de6736e7cced236571b563`)\npartway through the work. The `redeliver` defect is present at 0.14.0: `redeliver` appears 12 times\nin the web output and 0 times in the rust output from the same specification, and the rust target\nbuilds clean.\n\nThe model source is byte-identical across 0.13.1 and 0.13.5 (`diff -rq` over\n`crates/specify/ess-domain/src` and `docs/`), so nothing read from either tree is invalidated —\nonly the version number in this record was.\n\n## Correction: this was never an ESS defect against this specification\n\nThe diagnosis above is wrong on its central fact. It says `redeliver` \"appears **nowhere** in the\nrust target\". It appears when the rust target emits it, and what decides that is the specification:\n\n- `crates/generate/ess-synth/src/rust/system.rs:515,525` — `retries` starts `false` and is set true\n  only by a binding whose `on_failure:` is `retry`.\n- `:775` — `pub fn redeliver` is emitted only `if retries`.\n- The web bridge calls `self.redeliver(&event)` from `fn replay` unconditionally.\n\nThis specification declared **no bindings at all** when the blocker was filed. So no binding asked\nfor `retry`, no `redeliver` was emitted, and the web bridge called a method that did not exist. The\nmodel was the cause, not the emitter.\n\nSeven bindings now exist, all with `on_failure: retry`. `pub fn redeliver` is emitted at\n`crates/softphone-system/src/lib.rs:412`, and the web target **builds**:\n\n```console\n$ cargo build --release --target wasm32-unknown-unknown\n    Finished `release` profile [optimized] target(s) in 2.48s\n$ ls -la target/wasm32-unknown-unknown/release/softphone_web.wasm\n-rwxr-xr-x 2 timo timo 472945 ... softphone_web.wasm\n```\n\n## What remains true, and is somebody else's\n\n`ess generate synthesize --target web` emits output that cannot compile for **any** specification\nwith no `on_failure: retry` binding — which includes every specification with no bindings, and the\n`--kind docs` page calls that state legal and worth seeing. A target that refuses to compile for a\nvalid document is still a defect; it is just not this project's blocker, and it is narrower than\nwhat is written above.\n\nCleared because the thing it withheld — a browser observation of this specification — is now\nproducible.\n"},"args":{"changes":{"body":"# The generated web target does not compile at ESS 0.13.1\n\n## What was observed\n\n`ess generate synthesize --path ess --target web` emits a tree that fails to build:\n\n```\ncrates/softphone-web/src/lib.rs:377:14\n    self.redeliver(&event)?;\n         ^^^^^^^^^ method not found in `&mut System<CallHistoryBehaviors, ..., ...>`\nerror: could not compile `softphone-web` (lib) due to 1 previous error\n```\n\n`redeliver` appears **nowhere** in the rust target the web crate depends on — `grep -rn redeliver`\nover all 30 emitted Rust artifacts returns nothing. The web emitter emits a call to a method the\nrust emitter does not generate.\n\n## It is not this specification\n\nThe rust target from the same specification builds clean: `cargo build --release` →\n`Finished release profile [optimized] target(s) in 1.02s`, all nine crates. Both targets report the\nsame 231 capabilities — 168 generated, 52 obligations, 11 refused — and web synthesis is\nbyte-identical across two runs. The specification validates: `softphone v1 — 9 file(s), valid`.\n\nSo the defect is in `ess generate synthesize --target web` at `0.13.1`\n(`git+https://github.com/beyond10x/ess?tag=0.13.1#d1a66772a91b5411d942d7a45bbf08dfc5de4651`), in the\n`replay` function of the emitted bridge.\n\n## What it blocks\n\n`story:softphone-page`'s acceptance is *the generated web surface drives a call before any page is\nhand-written*. That surface cannot be loaded, so no browser observation of this specification can be\nproduced at all — which also leaves the open question of whether the page persists anything across\na reload unanswerable.\n\n## What would clear it\n\nA fix upstream in `beyond10x/ess`: either the web emitter stops calling `redeliver`, or the rust\nemitter generates it. One line of the emitted bridge names the seam.\n\nFiled as an `upstream-blocker` rather than a `decision-blocker` because there is nothing here to\ndecide. `aep artifact kinds` does not list this kind by name — the `<type>-blocker` family is open\nand `aep artifact lifecycle upstream-blocker` answers `open -> cleared`.\n\n## What is not blocked\n\nEverything specification-side. All eight bridges are in the compiled IR, the five refusal codes were\nobserved, and the rust target builds — so the layering is verified without the page.\n\n## Version, corrected\n\nThe version this record was filed against is wrong twice over, and the defect outlived both.\n\nFiled as ESS `0.13.1`. The installed binary was `0.13.5` at the time of the reviews, and was\nreinstalled to **`0.14.0`** (`git+https://github.com/beyond10x/ess?tag=0.14.0#a25090533d2dcaf8e3de6736e7cced236571b563`)\npartway through the work. The `redeliver` defect is present at 0.14.0: `redeliver` appears 12 times\nin the web output and 0 times in the rust output from the same specification, and the rust target\nbuilds clean.\n\nThe model source is byte-identical across 0.13.1 and 0.13.5 (`diff -rq` over\n`crates/specify/ess-domain/src` and `docs/`), so nothing read from either tree is invalidated —\nonly the version number in this record was.\n\n## Correction: this was never an ESS defect against this specification\n\nThe diagnosis above is wrong on its central fact. It says `redeliver` \"appears **nowhere** in the\nrust target\". It appears when the rust target emits it, and what decides that is the specification:\n\n- `crates/generate/ess-synth/src/rust/system.rs:515,525` — `retries` starts `false` and is set true\n  only by a binding whose `on_failure:` is `retry`.\n- `:775` — `pub fn redeliver` is emitted only `if retries`.\n- The web bridge calls `self.redeliver(&event)` from `fn replay` unconditionally.\n\nThis specification declared **no bindings at all** when the blocker was filed. So no binding asked\nfor `retry`, no `redeliver` was emitted, and the web bridge called a method that did not exist. The\nmodel was the cause, not the emitter.\n\nSeven bindings now exist, all with `on_failure: retry`. `pub fn redeliver` is emitted at\n`crates/softphone-system/src/lib.rs:412`, and the web target **builds**:\n\n```console\n$ cargo build --release --target wasm32-unknown-unknown\n    Finished `release` profile [optimized] target(s) in 2.48s\n$ ls -la target/wasm32-unknown-unknown/release/softphone_web.wasm\n-rwxr-xr-x 2 timo timo 472945 ... softphone_web.wasm\n```\n\n## What remains true, and is somebody else's\n\n`ess generate synthesize --target web` emits output that cannot compile for **any** specification\nwith no `on_failure: retry` binding — which includes every specification with no bindings, and the\n`--kind docs` page calls that state legal and worth seeing. A target that refuses to compile for a\nvalid document is still a defect; it is just not this project's blocker, and it is narrower than\nwhat is written above.\n\nCleared because the thing it withheld — a browser observation of this specification — is now\nproducible.\n"},"command":"update-entity","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788525689579,"causation":"cmd-protocol-artifact-body-append-upstream-blocker-ess-web-target-redeliver-1788525689579","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"upstream-blocker:ess-web-target-redeliver@3#0~17e38101be324431","executor":null,"recorded_at":"2026-09-04T12:41:29Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-web-target-redeliver","revision":4,"type":"aep.status.move/v1","from_state":"open","to_state":"cleared","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000052","to":"cleared"},"payload":{"actor":"human:timo","at":1788525689737,"causation":"cmd-move-upstream-blocker-ess-web-target-redeliver-1788525689737","change":{"change":"moved","decided_on":{},"from":"open","to":"cleared"},"correlation":"protocol-artifact-move","event_id":"upstream-blocker:ess-web-target-redeliver@4#0~99de0a12e4119f85","executor":null,"recorded_at":"2026-09-04T12:41:29Z"}}
{"entity":"architecture-decision-record","version":1,"id":"browser-holds-no-sip-stack","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"proposed","changed":{"body":"# The browser holds no SIP stack; a server does, and the page bridges to it\n\n## Status\n\nAccepted for this project. It replaces the arrangement `story:signalling-over-wss` assumed, and\n`topology.yaml` is where the change is visible.\n\n## Context\n\nUntil now `sip-binding` declared `browser: secure-websocket` and `browser: rtc-peer-connection`, so\nthe specification said this page registers over SIP and negotiates its own dialogs. Two facts made\nthat the wrong model.\n\nA browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render, and it owns no SIP stack.\nThe one that would have served — the `sipx.browser.v1` kernel in `crates/sipx-wasm` — is real but\nunpackaged: `A-17` (generate and package the browser SDK) and `M-52` (browser-native WebRTC audio)\nare both `status: ready`, not `done`.\n\nSecond, the operator's requirement is that an engineer reach a telephone from a page in Devcenter.\nThat is a deployment with a server in it either way, and a server that already terminates SIP has\nno use for a second SIP stack in the browser.\n\n## Decision\n\nThe page opens **one media bridge** to a server it names: a control channel and one WebRTC audio\nleg. `softphone.bridge` is that half of the arrangement, and `softphone.media.BindingKind` gains\n`Bridge` beside `Sip` and `Loopback`.\n\nThe server — `widgets/phone/crates/phone-server` — holds the SIP leg. `softphone.sip` therefore\nstays in this specification and stops being a browser workload: its `requires:` entries are now\n`network: sip-signalling` and `network: rtp-media`.\n\n**The browser holds the phone's state; the server is a device.** `phone-server` runs no ESS system.\nIt reports facts about legs, and each fact arrives as a command the specification already grants to\na kernel actor — `RingCall`, `ConfirmAnswer`, `MediaConnected`, `FailCall`, `ConfirmBridge`,\n`FailBridge`. There is one authority over a call's state and no event log to reconcile across a\nprocess boundary.\n\n## Consequences\n\n- `softphone.bridge` declares no SIP construct and its own `SessionDescription`, rather than reusing\n  `softphone.sip`'s. Two carriers of one neutral session must not depend on each other, and a shared\n  type between them would be that dependency.\n- `activate-session-with-bridge` is a binding the `bindings:` comment had rejected for\n  `control.CallAnswered`, on the ground that the event carries no session id. `BridgeConfirmed`\n  carries one, so the reaction is legal here and the session no longer needs a person to activate it.\n- `story:signalling-over-wss` (\"Register over secure WebSocket\") describes work this decision moves\n  off the browser. It is not deleted: registration is still specified, and it is now the server's.\n- Nothing from any other organisation's code is used, and no gRPC appears. The control channel\n  carries the ESS command and event wire the specification already determines.\n\n## Alternatives considered\n\n**SIP over secure WebSocket from the page**, with `sipx-wasm` or a JavaScript SIP library. It needs\nno server of ours and the specification already modelled it. Rejected: the kernel's packaging and\nmedia adapter are unshipped, and a third-party SIP library in the page is a second protocol stack\nthis project would then own.\n\n**A vendor control API with a hosted media path.** Rejected by the operator: the phone is to be\nreplicated rather than assembled from somebody else's service.\n","title":"The browser holds no SIP stack; a server does, and the page bridges to it"},"args":{"command":"create-entity","data":{"body":"# The browser holds no SIP stack; a server does, and the page bridges to it\n\n## Status\n\nAccepted for this project. It replaces the arrangement `story:signalling-over-wss` assumed, and\n`topology.yaml` is where the change is visible.\n\n## Context\n\nUntil now `sip-binding` declared `browser: secure-websocket` and `browser: rtc-peer-connection`, so\nthe specification said this page registers over SIP and negotiates its own dialogs. Two facts made\nthat the wrong model.\n\nA browser owns `RTCPeerConnection`, ICE, DTLS-SRTP, capture and render, and it owns no SIP stack.\nThe one that would have served — the `sipx.browser.v1` kernel in `crates/sipx-wasm` — is real but\nunpackaged: `A-17` (generate and package the browser SDK) and `M-52` (browser-native WebRTC audio)\nare both `status: ready`, not `done`.\n\nSecond, the operator's requirement is that an engineer reach a telephone from a page in Devcenter.\nThat is a deployment with a server in it either way, and a server that already terminates SIP has\nno use for a second SIP stack in the browser.\n\n## Decision\n\nThe page opens **one media bridge** to a server it names: a control channel and one WebRTC audio\nleg. `softphone.bridge` is that half of the arrangement, and `softphone.media.BindingKind` gains\n`Bridge` beside `Sip` and `Loopback`.\n\nThe server — `widgets/phone/crates/phone-server` — holds the SIP leg. `softphone.sip` therefore\nstays in this specification and stops being a browser workload: its `requires:` entries are now\n`network: sip-signalling` and `network: rtp-media`.\n\n**The browser holds the phone's state; the server is a device.** `phone-server` runs no ESS system.\nIt reports facts about legs, and each fact arrives as a command the specification already grants to\na kernel actor — `RingCall`, `ConfirmAnswer`, `MediaConnected`, `FailCall`, `ConfirmBridge`,\n`FailBridge`. There is one authority over a call's state and no event log to reconcile across a\nprocess boundary.\n\n## Consequences\n\n- `softphone.bridge` declares no SIP construct and its own `SessionDescription`, rather than reusing\n  `softphone.sip`'s. Two carriers of one neutral session must not depend on each other, and a shared\n  type between them would be that dependency.\n- `activate-session-with-bridge` is a binding the `bindings:` comment had rejected for\n  `control.CallAnswered`, on the ground that the event carries no session id. `BridgeConfirmed`\n  carries one, so the reaction is legal here and the session no longer needs a person to activate it.\n- `story:signalling-over-wss` (\"Register over secure WebSocket\") describes work this decision moves\n  off the browser. It is not deleted: registration is still specified, and it is now the server's.\n- Nothing from any other organisation's code is used, and no gRPC appears. The control channel\n  carries the ESS command and event wire the specification already determines.\n\n## Alternatives considered\n\n**SIP over secure WebSocket from the page**, with `sipx-wasm` or a JavaScript SIP library. It needs\nno server of ours and the specification already modelled it. Rejected: the kernel's packaging and\nmedia adapter are unshipped, and a third-party SIP library in the page is a second protocol stack\nthis project would then own.\n\n**A vendor control API with a hosted media path.** Rejected by the operator: the phone is to be\nreplicated rather than assembled from somebody else's service.\n","status":"proposed","title":"The browser holds no SIP stack; a server does, and the page bridges to it"},"entity_type":"aep.architecture-decision-record/v1","locator":"ep://planning/store/architecture-decision-record/browser-holds-no-sip-stack"},"payload":{"actor":"human:timo","at":1788543775162,"causation":"cmd-new-architecture-decision-record-browser-holds-no-sip-stack","change":{"change":"created","status":"proposed"},"correlation":"protocol-artifact-new","event_id":"architecture-decision-record:browser-holds-no-sip-stack@1#0~c8c52b861610e7d9","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"architecture-decision-record","version":1,"id":"browser-holds-no-sip-stack","revision":1,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decides":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decides","source":"01MEM0000000000000121","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788543775170,"causation":"cmd-architecture-decision-record-browser-holds-no-sip-stack-decides-epic-browser-softphone","change":{"change":"related","relation":"decides","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"architecture-decision-record:browser-holds-no-sip-stack@1#0~cab9eb95623c664c","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"fill-the-obligations","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Fill every obligation so the specified phone runs\n\n## Outcome\n\nThe specification executes. Every command has behaviour, every declared view answers with rows, and\nthe eleven authored scenarios pass against the filled system rather than being replayed by a page.\n\n`docs/obligations.md` counts 344 capabilities: 248 generated, **75 obligations**, 21 refused. This\nstory is the 75.\n\n## Scope\n\n`widgets/phone` becomes one cargo workspace, `members = [\"crates/*\"]`, `exclude = [\".build\"]`.\n\n- `crates/softphone-behaviour` — one module per domain implementing every `*Behavior` and `*Query`\n  in `softphone_types::<domain>::obligations` over an in-memory store.\n- `crates/softphone-shell` — `softphone_system::System<…>` over those bundles, exporting\n  `ess_input_reserve`, `ess_dispatch` and `ess_output_len`, the three symbols the generated\n  `bridge.js` calls (`.build/synth/web/crates/softphone-web/src/lib.rs:1741-1768`).\n\nNothing generated is edited. The generated `Bound` trait is implemented for the *generic* `System`\n(`same file:169`), so the whole JSON wire is reused; only `serve()` hard-wires `Unimplemented`\n(`:1216`), which is why the exports are ours.\n\n## Acceptance\n\n`task scenarios` writes `.build/suite.json`; a test in `softphone-behaviour` reads it and drives\nevery step through `Bound::run`, asserting each declared outcome and each declared event. Green\nmeans the specification is executable, not that any deployment works.\n\nLoopback audio — `softphone.local.LoopbackDevice`, `BindingKind::Loopback` — is what makes the\nresult usable with no server: microphone to speaker, in a browser, with dialling, answering, mute,\nhold, the keypad, the log and the phonebook all working.\n","title":"Fill every obligation so the specified phone runs"},"args":{"command":"create-entity","data":{"body":"# Fill every obligation so the specified phone runs\n\n## Outcome\n\nThe specification executes. Every command has behaviour, every declared view answers with rows, and\nthe eleven authored scenarios pass against the filled system rather than being replayed by a page.\n\n`docs/obligations.md` counts 344 capabilities: 248 generated, **75 obligations**, 21 refused. This\nstory is the 75.\n\n## Scope\n\n`widgets/phone` becomes one cargo workspace, `members = [\"crates/*\"]`, `exclude = [\".build\"]`.\n\n- `crates/softphone-behaviour` — one module per domain implementing every `*Behavior` and `*Query`\n  in `softphone_types::<domain>::obligations` over an in-memory store.\n- `crates/softphone-shell` — `softphone_system::System<…>` over those bundles, exporting\n  `ess_input_reserve`, `ess_dispatch` and `ess_output_len`, the three symbols the generated\n  `bridge.js` calls (`.build/synth/web/crates/softphone-web/src/lib.rs:1741-1768`).\n\nNothing generated is edited. The generated `Bound` trait is implemented for the *generic* `System`\n(`same file:169`), so the whole JSON wire is reused; only `serve()` hard-wires `Unimplemented`\n(`:1216`), which is why the exports are ours.\n\n## Acceptance\n\n`task scenarios` writes `.build/suite.json`; a test in `softphone-behaviour` reads it and drives\nevery step through `Bound::run`, asserting each declared outcome and each declared event. Green\nmeans the specification is executable, not that any deployment works.\n\nLoopback audio — `softphone.local.LoopbackDevice`, `BindingKind::Loopback` — is what makes the\nresult usable with no server: microphone to speaker, in a browser, with dialling, answering, mute,\nhold, the keypad, the log and the phonebook all working.\n","status":"draft","title":"Fill every obligation so the specified phone runs"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/fill-the-obligations"},"payload":{"actor":"human:timo","at":1788543775327,"causation":"cmd-new-story-fill-the-obligations","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:fill-the-obligations@1#0~c7eeb44d8dcc666f","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"fill-the-obligations","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000126","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788543775335,"causation":"cmd-story-fill-the-obligations-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:fill-the-obligations@1#0~ecec874d0433e117","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"phone-server","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Bridge one WebRTC leg to one SIP leg\n\n## Outcome\n\nAn engineer dials from the page and talks to Asterisk. `widgets/phone/crates/phone-server` is the\nonly new service, and it holds no phone state.\n\n## Scope\n\n- **browser leg** — `sipx-media`: `bind`, `gather`, `key_with_dtls`, then `start_browser_audio`\n  against the offer the page sent (`crates/sipx-media/src/session.rs:1866,2033,1927,2133`). The\n  profile is fail-closed and is `docs/specs/webrtc-audio.md` §1: one `audio` section,\n  `UDP/TLS/RTP/SAVPF`, rtcp-mux on one ICE component, fingerprint verified before keys.\n  Non-trickle — one offer, one answer, candidates gathered before either crosses the wire.\n- **SIP leg** — a `sipx-call` outbound call, PCMU.\n- **the bridge** — `sipx_media::Bridge`, which forwards audio between two media sessions\n  (`crates/sipx-call/src/bridge.rs:1-25`). Both legs are sipx sessions, so no PCM is injected into\n  a live call and the read-side seam in `docs/specs/call-audio-seam.md` is not on this path.\n- **control** — one WebSocket per phone, JSON, carrying the ESS commands and events the\n  specification determines. No gRPC and no second protocol.\n\n`start_browser_audio` is on sipx `main` (`5c302380`); its stories `M-134` and `M-137` are recent. If\nthe published `=1.1.0` does not carry it, pin sipx by git rev and say so in the manifest.\n\n## Acceptance\n\nBefore any cluster: `phone-server` against `sipx answer` on the same machine — a real WebRTC leg\nfrom Brave, a real SIP leg to the CLI, audio both ways. Then leg B is repointed at the dev-cluster\nAsterisk and the same call is held.\n\n## Blocked on facts nobody here holds\n\nThe SIP coordinates of that Asterisk — host, port, transport, and whether it expects a registration\nor trusts an address — what to dial, and where `phone-server` runs for the test.\n","title":"Bridge one WebRTC leg to one SIP leg"},"args":{"command":"create-entity","data":{"body":"# Bridge one WebRTC leg to one SIP leg\n\n## Outcome\n\nAn engineer dials from the page and talks to Asterisk. `widgets/phone/crates/phone-server` is the\nonly new service, and it holds no phone state.\n\n## Scope\n\n- **browser leg** — `sipx-media`: `bind`, `gather`, `key_with_dtls`, then `start_browser_audio`\n  against the offer the page sent (`crates/sipx-media/src/session.rs:1866,2033,1927,2133`). The\n  profile is fail-closed and is `docs/specs/webrtc-audio.md` §1: one `audio` section,\n  `UDP/TLS/RTP/SAVPF`, rtcp-mux on one ICE component, fingerprint verified before keys.\n  Non-trickle — one offer, one answer, candidates gathered before either crosses the wire.\n- **SIP leg** — a `sipx-call` outbound call, PCMU.\n- **the bridge** — `sipx_media::Bridge`, which forwards audio between two media sessions\n  (`crates/sipx-call/src/bridge.rs:1-25`). Both legs are sipx sessions, so no PCM is injected into\n  a live call and the read-side seam in `docs/specs/call-audio-seam.md` is not on this path.\n- **control** — one WebSocket per phone, JSON, carrying the ESS commands and events the\n  specification determines. No gRPC and no second protocol.\n\n`start_browser_audio` is on sipx `main` (`5c302380`); its stories `M-134` and `M-137` are recent. If\nthe published `=1.1.0` does not carry it, pin sipx by git rev and say so in the manifest.\n\n## Acceptance\n\nBefore any cluster: `phone-server` against `sipx answer` on the same machine — a real WebRTC leg\nfrom Brave, a real SIP leg to the CLI, audio both ways. Then leg B is repointed at the dev-cluster\nAsterisk and the same call is held.\n\n## Blocked on facts nobody here holds\n\nThe SIP coordinates of that Asterisk — host, port, transport, and whether it expects a registration\nor trusts an address — what to dial, and where `phone-server` runs for the test.\n","status":"draft","title":"Bridge one WebRTC leg to one SIP leg"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/phone-server"},"payload":{"actor":"human:timo","at":1788543775493,"causation":"cmd-new-story-phone-server","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:phone-server@1#0~1ea09173b54a32c7","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"phone-server","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000131","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788543775502,"causation":"cmd-story-phone-server-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:phone-server@1#0~c190f2316d0fc663","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"phone-server","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:fill-the-obligations"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000131","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788543775510,"causation":"cmd-story-phone-server-depends_on-story-fill-the-obligations","change":{"change":"related","relation":"depends_on","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-relate","event_id":"story:phone-server@1#0~22472df1ed43edd3","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"devcenter-widget","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Mount the phone in Devcenter\n\n## Outcome\n\nAn engineer opens Devcenter and the phone is in it, driving the same wasm system and the same\n`phone-server`.\n\n## Scope\n\nDevcenter's frontend is Vue 3 + Vite + pnpm 11.25.0 (`beyond10x/devcenter/frontend/package.json`)\nand this project's `player/skin.js` is already a Vue component, so the surface is a component and a\nwasm asset rather than a rewrite.\n\n## Acceptance\n\n`pnpm --dir frontend review` on `http://127.0.0.1:4173` renders it, and the journey the standalone\npage passes — dial, answer, mute, hold, hang up, with audio — passes inside Devcenter.\n\n## Depends on\n\nThe M1 shell and the M2 adapter. Nothing about this story is specification work.\n","title":"Mount the phone in Devcenter"},"args":{"command":"create-entity","data":{"body":"# Mount the phone in Devcenter\n\n## Outcome\n\nAn engineer opens Devcenter and the phone is in it, driving the same wasm system and the same\n`phone-server`.\n\n## Scope\n\nDevcenter's frontend is Vue 3 + Vite + pnpm 11.25.0 (`beyond10x/devcenter/frontend/package.json`)\nand this project's `player/skin.js` is already a Vue component, so the surface is a component and a\nwasm asset rather than a rewrite.\n\n## Acceptance\n\n`pnpm --dir frontend review` on `http://127.0.0.1:4173` renders it, and the journey the standalone\npage passes — dial, answer, mute, hold, hang up, with audio — passes inside Devcenter.\n\n## Depends on\n\nThe M1 shell and the M2 adapter. Nothing about this story is specification work.\n","status":"draft","title":"Mount the phone in Devcenter"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/devcenter-widget"},"payload":{"actor":"human:timo","at":1788543775666,"causation":"cmd-new-story-devcenter-widget","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:devcenter-widget@1#0~ac9f1443254181b6","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"devcenter-widget","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000138","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788543775674,"causation":"cmd-story-devcenter-widget-decomposes-epic-browser-softphone","change":{"change":"related","relation":"decomposes","target":"epic:browser-softphone"},"correlation":"protocol-artifact-relate","event_id":"story:devcenter-widget@1#0~a01f55e87fefd652","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"story","version":1,"id":"devcenter-widget","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:phone-server"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000138","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788543775681,"causation":"cmd-story-devcenter-widget-depends_on-story-phone-server","change":{"change":"related","relation":"depends_on","target":"story:phone-server"},"correlation":"protocol-artifact-relate","event_id":"story:devcenter-widget@1#0~d940ad23f6bcc486","executor":null,"recorded_at":"2026-09-04T17:42:55Z"}}
{"entity":"architecture-decision-record","version":1,"id":"browser-holds-no-sip-stack","revision":2,"type":"aep.status.move/v1","from_state":"proposed","to_state":"accepted","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"accepted"},"payload":{"actor":"human:timo","at":1788543782949,"causation":"cmd-move-architecture-decision-record-browser-holds-no-sip-stack-1788543782949","change":{"change":"moved","decided_on":{},"from":"proposed","to":"accepted"},"correlation":"protocol-artifact-move","event_id":"architecture-decision-record:browser-holds-no-sip-stack@2#0~c2833842ca4a242c","executor":null,"recorded_at":"2026-09-04T17:43:02Z"}}
{"entity":"story","version":1,"id":"fill-the-obligations","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"proposed"},"payload":{"actor":"human:timo","at":1788543783119,"causation":"cmd-move-story-fill-the-obligations-1788543783119","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:fill-the-obligations@2#0~1a89715c0ab4ce77","executor":null,"recorded_at":"2026-09-04T17:43:03Z"}}
{"entity":"story","version":1,"id":"fill-the-obligations","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"active"},"payload":{"actor":"human:timo","at":1788543783284,"causation":"cmd-move-story-fill-the-obligations-1788543783284","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:fill-the-obligations@3#0~2eb905fb95f71fd7","executor":null,"recorded_at":"2026-09-04T17:43:03Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Register the SIP leg, on the server that holds it\n\n## What changed about this story\n\nIt was \"Register over secure WebSocket\": the page would load the sipx WebAssembly session kernel,\nopen WSS to a SIP provider and drive REGISTER itself.\n`architecture-decision-record:browser-holds-no-sip-stack` moved that off the browser. Registration\nis still specified — `softphone.sip` is unchanged — and it is now `phone-server`'s, which\n`ess/topology.yaml` states: `sip-binding` requires `network: sip-signalling` and `network: rtp-media`\nrather than two browser facilities.\n\nThe kernel this story was built on is not the reason it moved, but it is why it could not have\nshipped as written: `sipx` `A-17` (package the browser SDK) and `M-52` (browser-native WebRTC audio)\nare both `status: ready`.\n\n## Outcome\n\n`phone-server` drives `softphone.sip.Register` to `Registered` or `Failed`, and back out of `Failed`\n— that state stopped being terminal for exactly this reason: one transient drop must not kill a\nregistration permanently.\n\n## Acceptance\n\nAgainst the SIP service the deployment names, a configured address-of-record and expiry produce a\n`softphone.sip.RegistrationConfirmed` fact and `softphone.sip.RegistrationById` reports `Registered`.\nA rejected REGISTER produces `RegistrationFailed` and no retry loop — `RetryRegistration` is a\ncommand somebody issues, not a loop the model implies. `RefreshRegistration` extends the lifetime\nwith no state change.\n\n## Scope\n\n`softphone.sip` only. `Registration`, `SipDialog`, the four SDP commands, and the server-side code\nthat drives them through `sipx-call`.\n\nTwo type members were narrowed to what a browser could do and now describe the server instead, so\neach is a question this story answers rather than an assumption it keeps:\n\n- `SignallingTransport` has one member, `SecureWebSocket`. A server can use UDP, TCP or TLS, so\n  either the member set grows or the deployment is stated to be WSS-only. Decide it here.\n- `MediaSecurity` has one member, `DtlsSrtp`. The browser leg keeps DTLS-SRTP; the SIP leg to a\n  dev-cluster Asterisk will most likely be plain RTP or SDES-keyed SRTP, which is the same question\n  with the same two answers.\n\n## What is no longer in scope\n\nThe vendored `browser/src/` binding, the `crates/sipx-wasm` module, the hand-written ABI glue, and\nthe WebSocket-subprotocol token trick a browser needs because it cannot set an `Authorization`\nheader on an upgrade. A server sets the header.\n"},"args":{"changes":{"body":"# Register the SIP leg, on the server that holds it\n\n## What changed about this story\n\nIt was \"Register over secure WebSocket\": the page would load the sipx WebAssembly session kernel,\nopen WSS to a SIP provider and drive REGISTER itself.\n`architecture-decision-record:browser-holds-no-sip-stack` moved that off the browser. Registration\nis still specified — `softphone.sip` is unchanged — and it is now `phone-server`'s, which\n`ess/topology.yaml` states: `sip-binding` requires `network: sip-signalling` and `network: rtp-media`\nrather than two browser facilities.\n\nThe kernel this story was built on is not the reason it moved, but it is why it could not have\nshipped as written: `sipx` `A-17` (package the browser SDK) and `M-52` (browser-native WebRTC audio)\nare both `status: ready`.\n\n## Outcome\n\n`phone-server` drives `softphone.sip.Register` to `Registered` or `Failed`, and back out of `Failed`\n— that state stopped being terminal for exactly this reason: one transient drop must not kill a\nregistration permanently.\n\n## Acceptance\n\nAgainst the SIP service the deployment names, a configured address-of-record and expiry produce a\n`softphone.sip.RegistrationConfirmed` fact and `softphone.sip.RegistrationById` reports `Registered`.\nA rejected REGISTER produces `RegistrationFailed` and no retry loop — `RetryRegistration` is a\ncommand somebody issues, not a loop the model implies. `RefreshRegistration` extends the lifetime\nwith no state change.\n\n## Scope\n\n`softphone.sip` only. `Registration`, `SipDialog`, the four SDP commands, and the server-side code\nthat drives them through `sipx-call`.\n\nTwo type members were narrowed to what a browser could do and now describe the server instead, so\neach is a question this story answers rather than an assumption it keeps:\n\n- `SignallingTransport` has one member, `SecureWebSocket`. A server can use UDP, TCP or TLS, so\n  either the member set grows or the deployment is stated to be WSS-only. Decide it here.\n- `MediaSecurity` has one member, `DtlsSrtp`. The browser leg keeps DTLS-SRTP; the SIP leg to a\n  dev-cluster Asterisk will most likely be plain RTP or SDES-keyed SRTP, which is the same question\n  with the same two answers.\n\n## What is no longer in scope\n\nThe vendored `browser/src/` binding, the `crates/sipx-wasm` module, the hand-written ABI glue, and\nthe WebSocket-subprotocol token trick a browser needs because it cannot set an `Authorization`\nheader on an upgrade. A server sets the header.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788543809804,"causation":"cmd-protocol-artifact-body-story-signalling-over-wss-1788543809804","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:signalling-over-wss@6#0~2f05dc32ff8db771","executor":null,"recorded_at":"2026-09-04T17:43:29Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":7,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"summary":"Registration moved off the browser: phone-server drives REGISTER to a terminal state.","title":"Register the SIP leg, on the server that holds it"},"args":{"changes":{"summary":"Registration moved off the browser: phone-server drives REGISTER to a terminal state.","title":"Register the SIP leg, on the server that holds it"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788543809891,"causation":"cmd-protocol-artifact-set-story-signalling-over-wss-1788543809891","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:signalling-over-wss@7#0~b64002d2810c5f70","executor":null,"recorded_at":"2026-09-04T17:43:29Z"}}
{"entity":"story","version":1,"id":"signalling-over-wss","revision":7,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:media-session-domain"},{"informed_by":"architecture-decision-record:browser-holds-no-sip-stack"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000049","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788543813417,"causation":"cmd-story-signalling-over-wss-informed_by-architecture-decision-record-browser-holds-no-sip-stack","change":{"change":"related","relation":"informed_by","target":"architecture-decision-record:browser-holds-no-sip-stack"},"correlation":"protocol-artifact-relate","event_id":"story:signalling-over-wss@7#0~1f93c5818c18f9a1","executor":null,"recorded_at":"2026-09-04T17:43:33Z"}}
{"entity":"story","version":1,"id":"phone-server","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:browser-softphone"},{"depends_on":"story:fill-the-obligations"},{"implements":"architecture-decision-record:browser-holds-no-sip-stack"}]},"args":{"command":"create-relation","kind":"implements","source":"01MEM0000000000000043","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788543813515,"causation":"cmd-story-phone-server-implements-architecture-decision-record-browser-holds-no-sip-stack","change":{"change":"related","relation":"implements","target":"architecture-decision-record:browser-holds-no-sip-stack"},"correlation":"protocol-artifact-relate","event_id":"story:phone-server@1#0~9d74d4f34fd3e479","executor":null,"recorded_at":"2026-09-04T17:43:33Z"}}
{"entity":"story","version":1,"id":"fill-the-obligations","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"Taskfile.yml"},{"confidence":"cited","path":"crates/softphone-behaviour"},{"confidence":"cited","path":"crates/softphone-shell"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"Cargo.toml"},{"confidence":"cited","path":"Taskfile.yml"},{"confidence":"cited","path":"crates/softphone-behaviour"},{"confidence":"cited","path":"crates/softphone-shell"}]},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788543816868,"causation":"cmd-protocol-artifact-scope-story-fill-the-obligations-1788543816868","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:fill-the-obligations@4#0~3d70cbb267fb3ce0","executor":null,"recorded_at":"2026-09-04T17:43:36Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-bridge-refuses-a-compiled-module","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# `bridge.js` refuses the compiled `Module` its own documentation offers\n\n## What happens\n\n`crates/verify/…`/`ess-synth`'s web target emits `bridge.js`, whose `open()` doc says: \"`source` is\nanything `WebAssembly.instantiate` accepts: the bytes of a `.wasm`, or a compiled `Module`.\"\n\nThe implementation is `const { instance } = await WebAssembly.instantiate(source, {})`. That\ndestructuring is correct for a buffer, where the call resolves to `{ module, instance }`. For a\ncompiled `Module` the same call resolves to an **`Instance`**, so `instance` is `undefined` and the\nnext line fails with `Cannot read properties of undefined (reading 'exports')`.\n\n## Where it was hit\n\n`widgets/phone/tests/suite.mjs` compiles the module once and instantiates it per scenario, because a\nscenario starts from nothing and a shared store would make the running order part of the result.\nEleven scenarios failed identically before the runner was changed to hand over the bytes each time.\n\n## What it costs\n\nRecompiling an 845 KB module once per scenario. Nothing incorrect, and it does not grow with the\nspecification — but the documented API is unusable as documented.\n\n## The fix, upstream\n\nEither accept both in `open()` —\n\n```js\nconst result = await WebAssembly.instantiate(source, {});\nconst instance = result instanceof WebAssembly.Instance ? result : result.instance;\n```\n\n— or delete the sentence offering a `Module`.\n","title":"The generated bridge.js refuses the compiled Module its own doc offers"},"args":{"command":"create-entity","data":{"body":"# `bridge.js` refuses the compiled `Module` its own documentation offers\n\n## What happens\n\n`crates/verify/…`/`ess-synth`'s web target emits `bridge.js`, whose `open()` doc says: \"`source` is\nanything `WebAssembly.instantiate` accepts: the bytes of a `.wasm`, or a compiled `Module`.\"\n\nThe implementation is `const { instance } = await WebAssembly.instantiate(source, {})`. That\ndestructuring is correct for a buffer, where the call resolves to `{ module, instance }`. For a\ncompiled `Module` the same call resolves to an **`Instance`**, so `instance` is `undefined` and the\nnext line fails with `Cannot read properties of undefined (reading 'exports')`.\n\n## Where it was hit\n\n`widgets/phone/tests/suite.mjs` compiles the module once and instantiates it per scenario, because a\nscenario starts from nothing and a shared store would make the running order part of the result.\nEleven scenarios failed identically before the runner was changed to hand over the bytes each time.\n\n## What it costs\n\nRecompiling an 845 KB module once per scenario. Nothing incorrect, and it does not grow with the\nspecification — but the documented API is unusable as documented.\n\n## The fix, upstream\n\nEither accept both in `open()` —\n\n```js\nconst result = await WebAssembly.instantiate(source, {});\nconst instance = result instanceof WebAssembly.Instance ? result : result.instance;\n```\n\n— or delete the sentence offering a `Module`.\n","status":"open","title":"The generated bridge.js refuses the compiled Module its own doc offers"},"entity_type":"aep.upstream-blocker/v1","locator":"ep://planning/store/upstream-blocker/ess-bridge-refuses-a-compiled-module"},"payload":{"actor":"human:timo","at":1788544572297,"causation":"cmd-new-upstream-blocker-ess-bridge-refuses-a-compiled-module","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"upstream-blocker:ess-bridge-refuses-a-compiled-module@1#0~9e82abeabcb64ab9","executor":null,"recorded_at":"2026-09-04T17:56:12Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-bridge-refuses-a-compiled-module","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:fill-the-obligations"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000149","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788544572304,"causation":"cmd-upstream-blocker-ess-bridge-refuses-a-compiled-module-blocks-story-fill-the-obligations","change":{"change":"related","relation":"blocks","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-bridge-refuses-a-compiled-module@1#0~deb43b6f683a344e","executor":null,"recorded_at":"2026-09-04T17:56:12Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-conform-author-does-not-check-a-union-member","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"open","changed":{"body":"# `ess conform author` does not check a union member, and the web wire then refuses it\n\n## What happens\n\n`softphone.media.ChannelSignal` is `kind: union`, `tag: kind`, with one variant `dtmf` carrying a\n`DigitString`. A scenario wrote its input as the documentation and the domain comment spell it:\n\n```yaml\nsignal: { kind: dtmf, dtmf: '5' }\n```\n\n`ess conform author` compiled it with **0 refusals**. The emitted module then refused the step at run\ntime:\n\n```json\n{\"kind\":\"undecodable\",\"at\":\"input.signal.value\",\"expected\":\"a value\",\"found\":\"nothing\"}\n```\n\nThe web wire decodes a union as `{ \"kind\": <variant>, \"value\": <payload> }`. Rewriting the scenario\nto `{ kind: dtmf, value: '5' }` also compiles with 0 refusals, so **the author accepts either** — it\nchecks nothing inside the literal — and only one of the two runs.\n\n## Why it matters\n\n`ess conform author` is the step whose value is that \"a name the model does not declare is refused\nnow rather than at the first run\". For every other construct it holds. Here a scenario that names\nthe payload key wrongly is admitted, and the defect surfaces only once something executes the suite\n— which, until this project filled its obligations, nothing did.\n\n## The fix, upstream\n\nResolve a union literal against the declared variants in the authored-scenario compiler: refuse a\ntag no variant declares, and refuse a payload key that is not the wire's. Either spelling could be\nthe accepted one; what cannot stand is both compiling and one failing.\n\n## Worked around here\n\n`scenarios/media-session-lifecycle.yaml` uses the spelling the wire decodes, with a comment saying\nwhy the readable one is not there.\n","title":"ess conform author admits any object for a union input; the wire then refuses it"},"args":{"command":"create-entity","data":{"body":"# `ess conform author` does not check a union member, and the web wire then refuses it\n\n## What happens\n\n`softphone.media.ChannelSignal` is `kind: union`, `tag: kind`, with one variant `dtmf` carrying a\n`DigitString`. A scenario wrote its input as the documentation and the domain comment spell it:\n\n```yaml\nsignal: { kind: dtmf, dtmf: '5' }\n```\n\n`ess conform author` compiled it with **0 refusals**. The emitted module then refused the step at run\ntime:\n\n```json\n{\"kind\":\"undecodable\",\"at\":\"input.signal.value\",\"expected\":\"a value\",\"found\":\"nothing\"}\n```\n\nThe web wire decodes a union as `{ \"kind\": <variant>, \"value\": <payload> }`. Rewriting the scenario\nto `{ kind: dtmf, value: '5' }` also compiles with 0 refusals, so **the author accepts either** — it\nchecks nothing inside the literal — and only one of the two runs.\n\n## Why it matters\n\n`ess conform author` is the step whose value is that \"a name the model does not declare is refused\nnow rather than at the first run\". For every other construct it holds. Here a scenario that names\nthe payload key wrongly is admitted, and the defect surfaces only once something executes the suite\n— which, until this project filled its obligations, nothing did.\n\n## The fix, upstream\n\nResolve a union literal against the declared variants in the authored-scenario compiler: refuse a\ntag no variant declares, and refuse a payload key that is not the wire's. Either spelling could be\nthe accepted one; what cannot stand is both compiling and one failing.\n\n## Worked around here\n\n`scenarios/media-session-lifecycle.yaml` uses the spelling the wire decodes, with a comment saying\nwhy the readable one is not there.\n","status":"open","title":"ess conform author admits any object for a union input; the wire then refuses it"},"entity_type":"aep.upstream-blocker/v1","locator":"ep://planning/store/upstream-blocker/ess-conform-author-does-not-check-a-union-member"},"payload":{"actor":"human:timo","at":1788544572465,"causation":"cmd-new-upstream-blocker-ess-conform-author-does-not-check-a-union-member","change":{"change":"created","status":"open"},"correlation":"protocol-artifact-new","event_id":"upstream-blocker:ess-conform-author-does-not-check-a-union-member@1#0~30440e9b769d6f87","executor":null,"recorded_at":"2026-09-04T17:56:12Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-conform-author-does-not-check-a-union-member","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"blocks":"story:fill-the-obligations"}]},"args":{"command":"create-relation","kind":"blocks","source":"01MEM0000000000000154","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788544572474,"causation":"cmd-upstream-blocker-ess-conform-author-does-not-check-a-union-member-blocks-story-fill-the-obligations","change":{"change":"related","relation":"blocks","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-conform-author-does-not-check-a-union-member@1#0~3f25c919eae662f8","executor":null,"recorded_at":"2026-09-04T17:56:12Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-bridge-refuses-a-compiled-module","revision":1,"type":"aep.relation.remove/v1","from_state":"open","to_state":"open","changed":{"relations":null},"args":{"command":"remove-relation","relation":"rel-000000000149"},"payload":{"actor":"human:timo","at":1788544585064,"causation":"cmd-unrel-upstream-blocker-ess-bridge-refuses-a-compiled-module-blocks-story-fill-the-obligations","change":{"change":"unrelated","relation":"blocks","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-unrelate","event_id":"upstream-blocker:ess-bridge-refuses-a-compiled-module@1#0~f83490ca7e085359","executor":null,"recorded_at":"2026-09-04T17:56:25Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-bridge-refuses-a-compiled-module","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"informed_by":"story:fill-the-obligations"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000058","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788544585155,"causation":"cmd-upstream-blocker-ess-bridge-refuses-a-compiled-module-informed_by-story-fill-the-obligations","change":{"change":"related","relation":"informed_by","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-bridge-refuses-a-compiled-module@1#0~f061ed89c78c2f5f","executor":null,"recorded_at":"2026-09-04T17:56:25Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-conform-author-does-not-check-a-union-member","revision":1,"type":"aep.relation.remove/v1","from_state":"open","to_state":"open","changed":{"relations":null},"args":{"command":"remove-relation","relation":"rel-000000000151"},"payload":{"actor":"human:timo","at":1788544585245,"causation":"cmd-unrel-upstream-blocker-ess-conform-author-does-not-check-a-union-member-blocks-story-fill-the-obligations","change":{"change":"unrelated","relation":"blocks","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-unrelate","event_id":"upstream-blocker:ess-conform-author-does-not-check-a-union-member@1#0~251a40c20b1af8e0","executor":null,"recorded_at":"2026-09-04T17:56:25Z"}}
{"entity":"upstream-blocker","version":1,"id":"ess-conform-author-does-not-check-a-union-member","revision":1,"type":"aep.relation.create/v1","from_state":"open","to_state":"open","changed":{"relations":[{"informed_by":"story:fill-the-obligations"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000061","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788544585334,"causation":"cmd-upstream-blocker-ess-conform-author-does-not-check-a-union-member-informed_by-story-fill-the-obligations","change":{"change":"related","relation":"informed_by","target":"story:fill-the-obligations"},"correlation":"protocol-artifact-relate","event_id":"upstream-blocker:ess-conform-author-does-not-check-a-union-member@1#0~74a6fc6540cdeef3","executor":null,"recorded_at":"2026-09-04T17:56:25Z"}}
