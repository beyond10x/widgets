<!--
generated from softphone v1
model digest 522f372d9f45cae05577d8379ac02cac26b458beceb66dea739471ba4f91e59e
contract digest a716fbbbe26b7fdd24cf69b5441a8a7a57b304f349ef2e63e6705b0c329ffd20
do not edit: regenerate with `ess generate`
-->

# Topology

Runtime requirements, stated semantically. None of this is a deployment and nothing generates a manifest from it: a replica floor of two is a claim that the system is not correct with one instance, which is a fact about the design and survives every change of hosting.

## `bridge-binding`

Owns the bridge from this page to the server that holds its SIP leg.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `rtc-peer-connection`
- `browser` — `secure-websocket`

## `call-history`

Owns the durable record of every finished call.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `local-storage`

## `local-binding`

Owns the local-audio binding, the second carrier the media layer is proved against.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `audio-capture`
- `browser` — `audio-render`

## `media-session`

Owns the protocol-neutral media session and the signals crossing it.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `audio-capture`
- `browser` — `audio-render`

## `phone-console`

Owns what is on screen and what a gesture means.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `dom`

## `phone-control`

Owns the call surface a human or an agent drives identically.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires nothing beyond itself.

## `phone-directory`

Owns the phonebook.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `local-storage`

## `phone-presence`

Owns who else is there, as one roster this page holds and the server only reports.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `browser` — `secure-websocket`

## `sip-binding`

Owns the SIP registration and the dialogs carrying media sessions over it.

One instance is enough: nothing about the design needs a second. No ceiling is declared.

Stateful: an instance holds state that outlives a request, so instances are not interchangeable.

It requires:

- `network` — `rtp-media`
- `network` — `sip-signalling`

[Back to the index](index.md).

---

Generated from softphone v1 · model digest `522f372d9f45cae05577d8379ac02cac26b458beceb66dea739471ba4f91e59e` · contract digest `a716fbbbe26b7fdd24cf69b5441a8a7a57b304f349ef2e63e6705b0c329ffd20`. Do not edit this file; change the specification and regenerate it with `ess generate`.
